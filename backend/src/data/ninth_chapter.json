{
  "number": 9,
  "name": "The Trouble with Distributed Systems",
  "theory": [
    {
      "question": "Why are partial failures the defining difficulty of distributed systems, compared to a single machine?",
      "answer": "On one machine, software is usually either fully working or fully broken; in a distributed system, some parts can fail while others keep running, and failures are nondeterministic.\n\n\"In a distributed system, there may well be some parts of the system that are broken in an unpredictable way, even though other parts of the system are working fine. This is known as a partial failure.\"\n\nA single computer with good software tends toward determinism: the same operation produces the same result, and hardware problems usually cause a total crash rather than flaky in-between behavior. Distributed systems cannot hide the messy physical world behind an idealized model. A request involving multiple nodes over a network may sometimes work and sometimes fail unpredictably, and you may not even know whether it succeeded.\n\nThat nondeterminism is what makes distributed systems hard, but tolerating partial failures also enables powerful operations like rolling upgrades. The engineering mindset must flip from the happy path to asking what could go wrong, because in a large enough system, rare events happen every day.\n\nComment: Operations engineers report long-lived partitions, PDU failures, switch failures, whole-datacenter outages, and even physical damage to cooling systems \u2014 partial failures are not theoretical."
    },
    {
      "question": "Give examples of network failures when you send a request and get no response.",
      "answer": "When a client sends a request over an asynchronous packet network and hears nothing back, many distinct failures look identical from the sender's side.\n\n\"If you send a request to another node and don't receive a response, it is impossible to tell why.\"\n\nConcrete possibilities: the request was lost (unplugged cable, misconfigured switch); the request is queued and will be delivered later (overloaded network or recipient); the remote node crashed or was powered down; the remote node is paused (e.g., a long GC pause) but will respond later; the node processed the request but the response was lost; or the response is delayed and will arrive later. Timeouts are the usual coping mechanism, but after a timeout you still do not know whether the request was processed \u2014 it may still be queued and delivered after you gave up.\n\nStudies and anecdotes make this concrete in production: about 12 network faults per month in one datacenter (half single-machine, half whole-rack disconnects); redundant networking gear does not eliminate human misconfiguration; fiber links cut by animals or scavenging; round-trip times of minutes at high percentiles even within one datacenter; asymmetric partitions where A talks to B and B to C but not A to C; a NIC that drops inbound packets but sends outbound fine; and brief interruptions with long-lasting repercussions.\n\nComment: Whenever communication crosses a network, it may fail \u2014 there is no way around designing for that."
    },
    {
      "question": "Why can't we make network delays predictable on the internet and in typical datacenters?",
      "answer": "Because Ethernet and IP use packet switching with dynamic resource sharing, not circuit switching with reserved bandwidth \u2014 delays come from queueing and are therefore unbounded.\n\n\"Variable delays in networks are not a law of nature but simply the result of a cost/benefit trade-off.\"\n\nCircuit-switched networks (classic telephone calls) reserve fixed bandwidth for the duration of a call, so queueing is avoided and maximum end-to-end latency is bounded. Packet-switched networks opportunistically share bandwidth: senders compete for wire capacity, switches decide moment-to-moment which packet goes next, and bursts queue up. That maximizes utilization (cheaper per byte) but introduces variable delay. The same trade-off appears with CPUs: dynamic thread scheduling improves utilization but threads wait unpredictably in the run queue.\n\nLatency guarantees are achievable only with static partitioning \u2014 dedicated hardware, exclusive bandwidth \u2014 but that is more expensive and reduces utilization. Multitenant clouds optimize for cost, not bounded delay. QoS mechanisms (prioritization, admission control) and newer algorithms like L4S can mitigate queueing, but \"currently deployed technology does not allow us to make any guarantees about delays or reliability of the network.\" Consequently there is no theoretically correct timeout value; you must determine it experimentally.\n\nComment: InfiniBand and careful QoS can emulate circuit-like behavior in specialized environments, but general-purpose datacenter and internet paths assume unbounded delay."
    },
    {
      "question": "TCP is called 'reliable' \u2014 what network problems does it still not solve for application developers?",
      "answer": "TCP handles dropped packets, reordering, and checksum errors within one connection, but applications still face lost connections, unknown processing state, and duplication across reconnects.\n\n\"If TCP provides 'reliability,' does that mean we no longer need to worry about networks being unreliable? Unfortunately not.\"\n\nTCP retransmits when acknowledgments time out, but cannot tell whether the data packet or the ACK was lost. It cannot plug in an unplugged cable and eventually signals an error to the application. Critically, if a connection dies, \"you unfortunately have no way of knowing how much data was actually processed by the remote node.\" A kernel-level ACK means the OS received the bytes, not that the application handled them \u2014 you need an application-level success response for certainty. Retransmission and deduplication apply only within one connection; reconnecting can duplicate data.\n\nSending via a socket also does not mean immediate transmission: data sits in OS buffers until congestion control allows sending, adding queueing delay before packets even enter the network. Congestion control, flow control, and backpressure adapt send rate to capacity, which is good for throughput but contributes to variable latency.\n\nComment: The same limitations apply in spirit to QUIC, SCTP, and similar transports \u2014 convenient message framing, not end-to-end application correctness."
    },
    {
      "question": "Distinguish monotonic clocks from time-of-day (wall-clock) clocks and say which to use for timeouts versus event ordering across nodes.",
      "answer": "Time-of-day clocks return calendar timestamps and can jump backward; monotonic clocks measure elapsed duration and always move forward but are meaningless across machines.\n\n\"It is important to distinguish between them, since they serve different purposes.\"\n\nTime-of-day clocks (e.g., `clock_gettime(CLOCK_REALTIME)`, `System.currentTimeMillis`) return seconds since the Unix epoch and are usually NTP-synchronized so timestamps ideally mean the same across machines. But NTP can forcibly reset a clock backward, leap seconds and DST cause jumps, and resolution was historically coarse. They are unsuitable for measuring elapsed time. Use them for human-facing timestamps and scheduled jobs, not for duration measurement.\n\nMonotonic clocks (e.g., `CLOCK_MONOTONIC`, `System.nanoTime`) are stopwatches: the difference between two readings is a trustworthy duration on one machine. They never jump backward (NTP may slew the rate slightly). The absolute value is arbitrary and cannot be compared across computers. For timeouts and response-time measurement in distributed systems, monotonic clocks on each node are usually fine because they need no cross-node synchronization.\n\nFor ordering events across nodes, neither physical clock is safe alone \u2014 NTP skew can make a causally later write look earlier (the LWW problem). Logical clocks (version vectors, transaction IDs) are safer for causality.\n\nComment: Google Spanner's TrueTime returns a confidence interval [earliest, latest] for wall-clock reads rather than pretending nanosecond digits are meaningful."
    },
    {
      "question": "List reasons a process can pause without noticing, and explain why that breaks leader leases and distributed locks.",
      "answer": "A node can be preempted for seconds or minutes at arbitrary points in code; during the pause the rest of the cluster may declare it dead and elect a new leader, yet the paused node resumes thinking it is still leader.\n\n\"A node in a distributed system must assume that its execution can be paused for a significant length of time at any point, even in the middle of a function.\"\n\nDocumented pause causes: lock/queue contention (worse on many-core machines); stop-the-world garbage collection; VM suspend during live migration; laptop sleep; hypervisor steal time under heavy load; synchronous disk or network I/O (including surprise classloader reads); swapping/page faults and thrashing; SIGSTOP from an operator. The thread does not know it was paused \u2014 it checks the clock later and discovers time jumped.\n\nThis breaks lease-based leadership. A leader checks `lease.isValid()` then processes a request, assuming little time passed. But a 15-second GC pause between the check and `process(request)` means the lease expired and another node became leader \u2014 yet the old leader still writes, causing split brain and corruption (the HBase lock bug pattern). The problem is like multithreaded race conditions, but worse: there is no shared memory, only messages over an unreliable network, so mutexes do not translate.\n\nComment: Fencing tokens (monotonically increasing lease numbers checked by storage) protect against zombies after a pause; comparing wall-clock lease expiry to local time adds clock-skew risk on top."
    },
    {
      "question": "How does garbage collection cause pauses, and what can you do to limit its impact on latency-sensitive services?",
      "answer": "GC can stop all application threads for noticeable periods; modern collectors reduce this, but pauses remain a real tail-latency source unless you architect around them.\n\n\"Many programming language runtimes (such as the JVM) have a garbage collector that occasionally needs to stop all running threads.\"\n\nHistorically stop-the-world GC could last minutes; modern algorithms (CMS, G1, ZGC, Shenandoah in Java; concurrent mark-sweep in Go) typically pause for milliseconds when tuned. Still, a GC pause at the wrong moment makes a node look dead to peers, triggers false failover, or lets an expired lease holder corrupt data.\n\nMitigations: tune the collector for your allocation pattern; avoid GC entirely with Rust/Swift-style manual lifetime management; pool and reuse objects to reduce allocation pressure; allocate off-heap; treat GC like a brief planned outage \u2014 drain requests from a node before GC, collect with no in-flight work, then resume (hiding pauses from clients and improving tail latencies); or restart processes periodically before long-lived heaps need full GC, one node at a time in a rolling pattern.\n\nNone of these fully eliminates pauses in GC'd runtimes, but they reduce impact. For most server-side data systems, hard real-time guarantees are uneconomical, so GC pauses must be designed for, not ignored.\n\nComment: GC and slow disk I/O can compound \u2014 they may stack their delays, which is especially painful on network block devices like Amazon EBS."
    },
    {
      "question": "What is a zombie in distributed locking, and how do fencing tokens protect against zombies and delayed messages?",
      "answer": "A zombie is a former leaseholder that still believes it holds the lease after expiry; without fencing, it can corrupt shared state alongside the new leaseholder.\n\n\"The term zombie is sometimes used to describe a former leaseholder that has not yet found out that it lost the lease and is still acting as if it were the current leaseholder.\"\n\nTwo corruption paths: (1) the old client is paused, its lease expires, a new client writes, then the old client resumes and writes too (split brain); (2) the old client crashes after sending a write, the write is delayed in the network for a long time, the lease expires and a new client writes, then the stale write arrives. Shooting the zombie (STONITH \u2014 disconnect network, kill VM) is unreliable: it does not help delayed messages, nodes can kill each other, and detection may be too late.\n\nFencing: the lock service returns a monotonically increasing fencing token with each lease grant. Every write to storage must include the token; storage rejects any write whose token is less than or equal to the highest token already processed. When client 2 gets token 34 and writes, client 1's later write with token 33 is rejected even if client 1 was merely delayed, not malicious. The new leaseholder should write immediately after acquiring the lease to fence off zombies. ZooKeeper's zxid, etcd's revision number, Kafka epoch numbers, and Hazelcast's FencedLock all serve this role. Conditional writes on object storage (S3, GCS, Azure) provide the check.\n\nComment: Fencing is permanent rejection (unlike optimistic concurrency retry); it requires storage to track the highest seen token or equivalent compare-and-swap semantics."
    },
    {
      "question": "Define limping node, gray failure, and fail-slow; why are they harder to handle than a clean crash?",
      "answer": "A limping node is still alive and may pass health checks but is too degraded to do useful work \u2014 also called gray failure or fail-slow \u2014 and it is harder to detect and remediate than a node that simply stops.\n\n\"Such a situation is called a limping node, gray failure, or fail-slow, and it can be even more difficult to deal with than a cleanly failed node.\"\n\nExamples: a NIC throttled from 1 Gb/s to 1 Kb/s by a driver bug; a process spending most of its time in GC under memory pressure; worn-out SSDs with erratic latency; hardware affected by heat, vibration, or power issues. Related: a process where one background thread is deadlocked while the rest appears fine. The node responds to pings or lightweight health checks but cannot meet SLOs on real work.\n\nClean crashes are painful but at least detectable \u2014 TCP RST, no heartbeat, timeout. Limping nodes cause timeouts that look like network problems, trigger false suspicion of other nodes, waste resources retrying against a half-working peer, and resist simple failover because the node is not fully dead. Load balancers may keep sending traffic to a node that passes TCP checks but serves 30-second responses.\n\nComment: Timeouts cannot distinguish slow network from slow node; you need latency SLOs, outlier detection, and circuit breakers \u2014 not just alive/dead binary checks."
    },
    {
      "question": "Explain the difference between safety and liveness properties, what type of property is the fencing-token?",
      "answer": "Safety means nothing bad ever happens (and if violated, you can point to the exact moment it broke); liveness means something good eventually happens (may not hold now, but can be satisfied later).\n\n\"If a safety property is violated, we can point to the particular point in time that it was broken... After a safety property has been violated, the violation cannot be undone.\"\n\nFor a fencing-token service, uniqueness (no two grants return the same token) and monotonic sequence (if request x finished before y started, then tx < ty) are safety properties \u2014 violating them means wrong data was already written. Availability (a non-crashed requester eventually gets a token) is liveness \u2014 it might be waiting now, but a response may still arrive. Liveness definitions often include \"eventually.\"\n\nIn distributed algorithms, safety is required in all situations the model allows \u2014 even if every node crashes, the algorithm must not return a wrong result. Liveness may have caveats: a response is guaranteed only if a majority has not crashed and the network eventually recovers. The partially synchronous model assumes outages last finite duration before repair.\n\nInformal shorthand \"nothing bad / something good eventually\" is imprecise; the formal distinction matters for proofs and for deciding what your system guarantees under total failure.\n\nComment: Eventual consistency is a liveness property \u2014 replicas may be stale now but should converge eventually."
    },
    {
      "question": "What is L4S (Low Latency, Low Loss, and Scalable Throughput), and what problem does it try to solve?",
      "answer": "L4S is a newer congestion-control and queueing approach that aims to cut latency and loss on packet networks without giving up the high utilization of packet switching.\n\n\"New network algorithms like Low Latency, Low Loss, and Scalable Throughput (L4S) attempt to mitigate some of the queuing and congestion control problems both at the client and the router level.\"\n\nClassic TCP congestion control fills buffers before backing off, so even short queues add delay (bufferbloat). Packet-switched datacenter and internet paths inherently queue under burst traffic, which is why delay is unbounded today. L4S works with explicit congestion signaling so routers can mark congestion earlier and endpoints can react faster, keeping queues short while maintaining throughput. Linux traffic control (TC) also lets applications reprioritize packets for QoS.\n\nIn practice today, such QoS and L4S are not broadly enabled in multitenant datacenters and public clouds. Engineers must still assume congestion, queueing, and unbounded delays, and set timeouts experimentally rather than from theory.\n\nComment: L4S is an evolution path toward lower tail latency on shared networks, not a replacement for application-level timeout, retry, and backpressure design."
    },
    {
      "question": "Contrast crash-stop, crash-recovery, and Byzantine fault models; which model fits most datacenter services?",
      "answer": "Crash-stop nodes die forever; crash-recovery nodes may restart with stable storage; Byzantine nodes may lie arbitrarily. Most datacenter systems assume honest nodes with crash-recovery plus partial synchrony.\n\n\"For modeling real systems, the partially synchronous model with crash-recovery faults is generally the most useful.\"\n\nCrash-stop: a node halts and never returns \u2014 simple but pessimistic. Crash-recovery: nodes may crash and later restart; stable disk survives, memory is lost \u2014 matches reboots and rolling upgrades. Byzantine: nodes may send malicious or contradictory messages; protocols need supermajorities (>2/3) and are expensive \u2014 relevant for aerospace radiation, cryptocurrencies, and peer-to-peer, but not typical internal datacenters where nodes share your organization's software.\n\nTiming models layer on top: synchronous (bounded delay \u2014 unrealistic), partially synchronous (mostly bounded, occasionally arbitrary delays \u2014 realistic), asynchronous (no timing assumptions at all \u2014 very restrictive). Real systems also face limping nodes and amnesia (forgetting disk data), which pure models omit.\n\nMost datacenter designs assume nodes are unreliable but honest: slow, unresponsive, or outdated state, but not deliberately deceptive. Bugs replicated on all nodes are not fixed by Byzantine algorithms \u2014 you need diverse implementations for that.\n\nComment: Weak forms of lying (corrupted packets, bad user input, misconfigured NTP) are handled with checksums, input validation, and NTP outlier rejection, not full Byzantine consensus."
    },
    {
      "question": "How do fault detection, timeouts, and quorum decisions interact when a node stops responding?",
      "answer": "You cannot know a remote node's state without messages, so systems use timeouts to guess failure \u2014 but timeouts confuse network delay with node death, and quorums override a node's own belief about whether it is alive.\n\n\"If a remote node doesn't respond, there is no way of knowing its state, because problems in the network cannot reliably be distinguished from problems at a node.\"\n\nUseful signals when available: TCP RST/FIN if the process is gone; explicit error response from the service; ICMP unreachable. Otherwise you wait, retry, and eventually timeout. Variable delay causes false crash suspicion; limping nodes make this worse.\n\nA node cannot trust only itself \u2014 it may be asymmetrically partitioned (receives but cannot send), paused for a minute, or declared dead while still running. Distributed algorithms use quorums: if a majority declares a node dead, it must step down even if it feels fine. Majority quorums tolerate minority faults and cannot conflict (only one majority at a time). This is why lease holders must respect quorum eviction and why fencing tokens are needed when they do not.\n\nComment: The classic \"I'm not dead!\" scenario \u2014 a node can receive traffic but not send replies, or wake from a long pause already declared dead \u2014 is why leader election must combine timeouts, quorums, and fencing, not heartbeats alone."
    }
  ],
  "practice": [
    {
      "task": "[Linux] Test a client-server system's response to network failures with tc/netem and iptables (Docker Linux on macOS).",
      "question": "Use Linux traffic control (`tc` with `netem`) and firewall rules to inject delays, loss, and partitions without hardware changes \u2014 the same class of faults Jepsen nemeses simulate.\n\nPrerequisites: Linux network namespaces with `sudo`, `iproute2` (`tc` with netem), `iptables`, `python3`, and `curl`. On macOS, Docker Desktop (or Docker Engine) is the recommended primary path \u2014 run the lab inside a Linux container so `tc`/`iptables` actually work. Same-host `IFACE=lo` is valid only when you are already on a Linux host/VM. A full Linux VM also works.\n\n```bash\n# macOS \u2014 Docker Desktop (recommended primary path)\nbrew install --cask docker\nopen -a Docker   # wait until Docker is running\ndocker --version\n\n# Debian/Ubuntu native Linux (or inside the container after apt-get)\nsudo apt-get update\nsudo apt-get install -y iproute2 iptables python3 curl\n\n# Confirm tools (run on Linux host or inside the container)\ntc -V\niptables --version\npython3 --version\ncurl --version | head -n1\n\n# List interfaces (you will set IFACE to match the topology below)\nip -br link\n```\n\nRecommended on macOS \u2014 one privileged Linux container with a mounted workdir. Create the lab directory on the host, save `fault_client.py` there, then run server + client + netem/iptables inside the container (same netns, so `IFACE=lo` and `URL=http://127.0.0.1:8080/` apply):\n\n```bash\n# From the directory that will hold fault_client.py\nmkdir -p \"$HOME/fault-lab\" && cd \"$HOME/fault-lab\"\n\n# Start an interactive Debian container (NET_ADMIN for tc/iptables; mount this dir at /lab)\ndocker run --rm -it --name fault-lab \\\n  --cap-add=NET_ADMIN --cap-add=NET_RAW \\\n  -v \"$PWD\":/lab -w /lab \\\n  debian:bookworm-slim bash\n\n# Inside the container \u2014 install tools once per container start\n# The container runs as root, so sudo is not needed for apt-get / tc / iptables\napt-get update\napt-get install -y iproute2 iptables python3 curl\ntc -V && iptables --version && python3 --version\n```\n\nInside that container use two shells (`docker exec -it fault-lab bash` for a second terminal) or background the server with `&`. All fault-injection commands below run inside the container, not on the macOS host.\n\nOptional two-container Docker network (server and client on separate containers sharing a user-defined bridge) \u2014 apply netem/iptables on the client (or server) container\u2019s carrying iface toward the peer IP, not on the Mac host:\n\n```bash\ndocker network create faultnet\ndocker run -d --name fault-server --network faultnet \\\n  --cap-add=NET_ADMIN --cap-add=NET_RAW \\\n  -v \"$HOME/fault-lab\":/lab -w /lab debian:bookworm-slim \\\n  bash -lc 'apt-get update && apt-get install -y python3 && python3 -m http.server 8080 --bind 0.0.0.0'\ndocker run --rm -it --name fault-client --network faultnet \\\n  --cap-add=NET_ADMIN --cap-add=NET_RAW \\\n  -v \"$HOME/fault-lab\":/lab -w /lab debian:bookworm-slim bash\n# Inside fault-client: apt-get install -y iproute2 iptables python3 curl\n# SERVER_IP=$(getent hosts fault-server | awk '{print $1}')\n# URL=http://$SERVER_IP:8080/\n# IFACE=$(ip route get \"$SERVER_IP\" | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}')\n# CLIENT_IP=$(hostname -I | awk '{print $1}')\n```\n\nTopology (pick one and keep `URL`, `IFACE`, and `CLIENT_IP` consistent \u2014 do not pair `http://127.0.0.1:8080/` with an uplink like eth0/en*):\n\nSame-host / single-container loopback (simplest on native Linux or inside the one-container Docker path):\n- `URL=http://127.0.0.1:8080/`\n- `IFACE=lo` (required so netem shapes loopback HTTP; both directions may pick up delay)\n- `CLIENT_IP=127.0.0.1`\n\nTwo hosts / two-container Docker network:\n- Bind the server on `0.0.0.0` and connect via the peer IP (not 127.0.0.1)\n- `URL=http://SERVER_IP:8080/`\n- `IFACE=$(ip route get SERVER_IP | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}')` on the host/container where you apply netem\n- `CLIENT_IP` = client address as seen by the server (`hostname -I` or Docker network IP)\n\nSetup \u2014 two terminals (or one container + `docker exec`). Terminal A runs the server; Terminal B runs the client.\n\nStart the server (Terminal A):\n\n```bash\npython3 -m http.server 8080 --bind 0.0.0.0\n```\n\nSave the client below as `fault_client.py` and run it as given (Terminal B). It records per-request latency and can print p50/p99 from those samples. For Task 4, edit only `sleep_before_retry` at its `# implement` comment (the retry loop is already wired). Pass `--url` matching your topology (`http://127.0.0.1:8080/` only with `IFACE=lo`).\n\n```python\n# fault_client.py \u2014 HTTP client for network fault injection lab\n#!/usr/bin/env python3\nfrom __future__ import annotations\n\nimport argparse\nimport http.client\nimport random\nimport sys\nimport time\nfrom typing import List, Optional\nfrom urllib.parse import urlparse\n\n\ndef percentile(sorted_vals: List[float], p: float) -> float:\n    \"\"\"Nearest-rank percentile on a pre-sorted list (p in 0..100).\"\"\"\n    if not sorted_vals:\n        raise ValueError(\"empty sample\")\n    k = max(0, min(len(sorted_vals) - 1, int(round((p / 100.0) * (len(sorted_vals) - 1)))))\n    return sorted_vals[k]\n\n\ndef print_percentiles(latencies_s: List[float]) -> None:\n    \"\"\"Print p50 and p99 from client-recorded latencies (seconds).\"\"\"\n    if not latencies_s:\n        print(\"no samples\", file=sys.stderr)\n        return\n    s = sorted(latencies_s)\n    print(f\"n={len(s)} p50={percentile(s, 50):.4f}s p99={percentile(s, 99):.4f}s\")\n\n\ndef sleep_before_retry(attempt: int) -> None:\n    # implement exponential backoff with full jitter:\n    # base=0.1, factor=2, cap=5.0 \u2192 delay = random.uniform(0, min(cap, base * factor**attempt))\n    # Until Task 4, keep a fixed short pause so the loop still runs.\n    time.sleep(0.05)\n\n\ndef one_request(url: str, connect_timeout: float, read_timeout: float) -> tuple[bool, float, Optional[int]]:\n    parsed = urlparse(url)\n    host = parsed.hostname or \"127.0.0.1\"\n    port = parsed.port or (443 if parsed.scheme == \"https\" else 80)\n    path = parsed.path or \"/\"\n    if parsed.query:\n        path = f\"{path}?{parsed.query}\"\n    started = time.monotonic()\n    conn: Optional[http.client.HTTPConnection] = None\n    try:\n        conn = http.client.HTTPConnection(host, port, timeout=connect_timeout)\n        conn.connect()\n        if conn.sock is not None:\n            conn.sock.settimeout(read_timeout)\n        conn.request(\"GET\", path)\n        resp = conn.getresponse()\n        resp.read(64)\n        return True, time.monotonic() - started, int(resp.status)\n    except Exception:\n        return False, time.monotonic() - started, None\n    finally:\n        if conn is not None:\n            conn.close()\n\n\ndef main() -> None:\n    p = argparse.ArgumentParser(description=\"Fault-injection HTTP client\")\n    p.add_argument(\"--url\", default=\"http://127.0.0.1:8080/\")\n    p.add_argument(\"--requests\", type=int, default=40, help=\"number of attempts\")\n    p.add_argument(\"--connect-timeout\", type=float, default=2.0)\n    p.add_argument(\"--read-timeout\", type=float, default=2.0)\n    p.add_argument(\"--max-retries\", type=int, default=0, help=\"retries after first failure (Task 4)\")\n    args = p.parse_args()\n\n    latencies: List[float] = []\n    fails = 0\n    retries_total = 0\n\n    for i in range(args.requests):\n        attempt = 0\n        ok, elapsed, code = one_request(args.url, args.connect_timeout, args.read_timeout)\n        while not ok and attempt < args.max_retries:\n            # retries already wired; only edit sleep_before_retry for Task 4\n            attempt += 1\n            retries_total += 1\n            sleep_before_retry(attempt - 1)\n            ok, elapsed, code = one_request(args.url, args.connect_timeout, args.read_timeout)\n\n        latencies.append(elapsed)\n        if ok:\n            print(f\"OK {code} {elapsed:.4f}s\")\n        else:\n            fails += 1\n            print(f\"FAIL {elapsed:.4f}s\")\n\n    print_percentiles(latencies)\n    print(f\"fails={fails} retries_total={retries_total}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n```\n\nRun a baseline (clean network). Set `URL` from your chosen topology:\n\n```bash\nURL=http://127.0.0.1:8080/   # loopback path \u2014 only with IFACE=lo\n# URL=http://SERVER_IP:8080/  # two-host / Docker path\npython3 fault_client.py --url \"$URL\" --requests 40\n```\n\nThe client prints each attempt and a summary line `n=\u2026 p50=\u2026s p99=\u2026s` \u2014 use that line for Task 1. Increase `--max-retries` only after you implement backoff (Task 4).\n\nFault injection \u2014 set `IFACE` / `CLIENT_IP` / `URL` from the Topology section (same-host or single-container: `IFACE=lo` and `CLIENT_IP=127.0.0.1`; two-host/two-container: peer IP and the carrying iface). Run these commands on the Linux host or inside the Docker container \u2014 not on the macOS host network stack.\n\n```bash\n# Same-host / single-container loopback example (netem must use lo, not eth0/en*):\nIFACE=lo\nCLIENT_IP=127.0.0.1\nURL=http://127.0.0.1:8080/\n\n# Two-host / two-container Docker example (uncomment and fill in):\n# IFACE=$(ip route get SERVER_IP | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}')\n# CLIENT_IP=10.0.0.2\n# URL=http://SERVER_IP:8080/\n\n# 100% packet loss on the server port (simulate cable cut to the service)\n# inside Docker container as root \u2014 omit sudo\niptables -A INPUT -p tcp --dport 8080 -j DROP\n# native Linux with sudo\n# sudo iptables -A INPUT -p tcp --dport 8080 -j DROP\n\n# 50% packet loss + 200ms delay\n# inside Docker container as root \u2014 omit sudo\ntc qdisc add dev \"$IFACE\" root netem loss 50% delay 200ms\n# native Linux with sudo\n# sudo tc qdisc add dev \"$IFACE\" root netem loss 50% delay 200ms\n\n# Reorder 25% of packets, delay 500ms (replace any existing root qdisc)\n# inside Docker container as root \u2014 omit sudo\ntc qdisc replace dev \"$IFACE\" root netem delay 500ms reorder 25% 50%\n# native Linux with sudo\n# sudo tc qdisc replace dev \"$IFACE\" root netem delay 500ms reorder 25% 50%\n\n# Partition: block traffic between this host and CLIENT_IP\n# inside Docker container as root \u2014 omit sudo\niptables -A INPUT -s \"$CLIENT_IP\" -j DROP\niptables -A OUTPUT -d \"$CLIENT_IP\" -j DROP\n# native Linux with sudo\n# sudo iptables -A INPUT -s \"$CLIENT_IP\" -j DROP\n# sudo iptables -A OUTPUT -d \"$CLIENT_IP\" -j DROP\n\n# Cleanup after each experiment (delete the specific rules you added; do not flush the whole filter table on shared hosts)\n# inside Docker container as root \u2014 omit sudo\ntc qdisc del dev \"$IFACE\" root\n# After port DROP:\n# iptables -D INPUT -p tcp --dport 8080 -j DROP\n# After partition:\n# iptables -D INPUT -s \"$CLIENT_IP\" -j DROP\n# iptables -D OUTPUT -d \"$CLIENT_IP\" -j DROP\n# native Linux with sudo\n# sudo tc qdisc del dev \"$IFACE\" root\n# sudo iptables -D INPUT -p tcp --dport 8080 -j DROP\n# sudo iptables -D INPUT -s \"$CLIENT_IP\" -j DROP\n# sudo iptables -D OUTPUT -d \"$CLIENT_IP\" -j DROP\n# Prefer a disposable VM/container netns for this lab so leftover rules cannot break Docker/host firewalls.\n```\n\nApply only one fault at a time; run cleanup before switching faults. For delay-only measurements use `tc qdisc add dev \"$IFACE\" root netem delay 200ms` inside Docker as root (omit sudo), or `sudo tc qdisc add dev \"$IFACE\" root netem delay 200ms` on native Linux (no loss). On `IFACE=lo`, both directions can pick up the delay; on a one-way uplink/veth, expect closer to a single delay add.\n\nTasks:\n1. Measure client p50/p99 latency under a clean network vs `netem delay 200ms` using the `p50=` / `p99=` summary from `fault_client.py` (same `--requests` and topology both runs). Note how the rise depends on topology (lo both-ways vs one-way peer path). Record the measured summary lines (not only a theoretical guess).\n2. Under 100% DROP on port 8080, how long until the client times out (watch per-line FAIL latencies and your `--connect-timeout` / `--read-timeout`)? Does the stock client retry? What is the risk of a retry storm if many clients retry immediately?\n3. Under a partition (`iptables` DROP both ways for `CLIENT_IP`), apply the rules before starting the client run. Compare server access logs (Python `http.server` prints to Terminal A) with client FAIL lines: with a clean both-ways DROP already in place, do new requests reach the server? When might you still see a completed server log line paired with a client FAIL (in-flight request, asymmetric DROP, or lost response)?\n4. Implement exponential backoff with full jitter in `sleep_before_retry` only (the retry loop is already wired; enable it with `--max-retries`). Under `netem loss 50%`, compare (a) stub fixed `0.05s` sleep vs full-jitter at the same `--max-retries` (e.g. 5) for retry timing/burstiness, and (b) `--max-retries 0` vs `N` for FAIL rate (`retries_total` rises when retries are enabled; jitter only spreads sleep timing).\n5. Map each experiment to the indistinguishable failure modes you get with no response: lost request, lost response, delay, partition.",
      "answer": "Complete reference client with separate connect/read timeouts, exponential backoff with full jitter, percentile summary, and retry counters. Save as `fault_client.py` and run against `python3 -m http.server 8080 --bind 0.0.0.0`. On macOS, run server + client + `tc`/`iptables` inside the Docker Linux container from the question (mounted `/lab`); same-host `IFACE=lo` only when already on Linux. Pass `--url` for your topology (`http://127.0.0.1:8080/` only with `IFACE=lo`).\n\n```python\n# fault_client.py \u2014 reference solution (backoff + jitter applied)\n#!/usr/bin/env python3\nfrom __future__ import annotations\n\nimport argparse\nimport http.client\nimport random\nimport sys\nimport time\nfrom typing import List, Optional\nfrom urllib.parse import urlparse\n\n\ndef percentile(sorted_vals: List[float], p: float) -> float:\n    if not sorted_vals:\n        raise ValueError(\"empty sample\")\n    k = max(0, min(len(sorted_vals) - 1, int(round((p / 100.0) * (len(sorted_vals) - 1)))))\n    return sorted_vals[k]\n\n\ndef print_percentiles(latencies_s: List[float]) -> None:\n    if not latencies_s:\n        print(\"no samples\", file=sys.stderr)\n        return\n    s = sorted(latencies_s)\n    print(f\"n={len(s)} p50={percentile(s, 50):.4f}s p99={percentile(s, 99):.4f}s\")\n\n\ndef sleep_before_retry(attempt: int) -> None:\n    base = 0.1\n    factor = 2.0\n    cap = 5.0\n    delay = random.uniform(0.0, min(cap, base * (factor ** attempt)))\n    time.sleep(delay)\n\n\ndef one_request(url: str, connect_timeout: float, read_timeout: float) -> tuple[bool, float, Optional[int]]:\n    parsed = urlparse(url)\n    host = parsed.hostname or \"127.0.0.1\"\n    port = parsed.port or (443 if parsed.scheme == \"https\" else 80)\n    path = parsed.path or \"/\"\n    if parsed.query:\n        path = f\"{path}?{parsed.query}\"\n    started = time.monotonic()\n    conn: Optional[http.client.HTTPConnection] = None\n    try:\n        conn = http.client.HTTPConnection(host, port, timeout=connect_timeout)\n        conn.connect()\n        if conn.sock is not None:\n            conn.sock.settimeout(read_timeout)\n        conn.request(\"GET\", path)\n        resp = conn.getresponse()\n        resp.read(64)\n        return True, time.monotonic() - started, int(resp.status)\n    except Exception:\n        return False, time.monotonic() - started, None\n    finally:\n        if conn is not None:\n            conn.close()\n\n\ndef main() -> None:\n    p = argparse.ArgumentParser(description=\"Fault-injection HTTP client\")\n    p.add_argument(\"--url\", default=\"http://127.0.0.1:8080/\")\n    p.add_argument(\"--requests\", type=int, default=40)\n    p.add_argument(\"--connect-timeout\", type=float, default=2.0)\n    p.add_argument(\"--read-timeout\", type=float, default=2.0)\n    p.add_argument(\"--max-retries\", type=int, default=0)\n    args = p.parse_args()\n\n    latencies: List[float] = []\n    fails = 0\n    retries_total = 0\n\n    for _ in range(args.requests):\n        attempt = 0\n        ok, elapsed, code = one_request(args.url, args.connect_timeout, args.read_timeout)\n        while not ok and attempt < args.max_retries:\n            attempt += 1\n            retries_total += 1\n            sleep_before_retry(attempt - 1)\n            ok, elapsed, code = one_request(args.url, args.connect_timeout, args.read_timeout)\n\n        latencies.append(elapsed)\n        if ok:\n            print(f\"OK {code} {elapsed:.4f}s\")\n        else:\n            fails += 1\n            print(f\"FAIL {elapsed:.4f}s\")\n\n    print_percentiles(latencies)\n    print(f\"fails={fails} retries_total={retries_total}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n```\n\nDocker Linux path (macOS primary) \u2014 after `docker run \u2026 debian:bookworm-slim` and `apt-get install` from the question, work in `/lab` inside the container (runs as root, so omit sudo on `tc`/`iptables`):\n\n```bash\n# Terminal A (inside container, or: docker exec -it fault-lab bash)\npython3 -m http.server 8080 --bind 0.0.0.0\n\n# Terminal B (same container netns)\nIFACE=lo\nCLIENT_IP=127.0.0.1\nURL=http://127.0.0.1:8080/\n```\n\nFault-injection commands and sample measured-style observations from a single-container `IFACE=lo` Docker run (your exact numbers will vary slightly; record your own `p50=` / `p99=` / FAIL lines). Cleanup with `tc qdisc del dev \"$IFACE\" root` inside Docker as root (omit sudo), or `sudo tc qdisc del \u2026` on native Linux, and matching `iptables -D` for each rule you added (do not flush the whole filter table on shared Docker/host firewalls).\n\n```bash\n# Topology vars (same-host / single-container loopback)\nIFACE=lo\nCLIENT_IP=127.0.0.1\nURL=http://127.0.0.1:8080/\n# Two-host / two-container: URL=http://SERVER_IP:8080/; IFACE=$(ip route get SERVER_IP | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}'); CLIENT_IP as seen by server\n\n# Baseline (clean) \u2014 sample observation:\n# n=40 p50=0.0012s p99=0.0031s fails=0 retries_total=0\npython3 fault_client.py --url \"$URL\" --requests 40\n\n# Delay only \u2014 p50/p99 rise; magnitude is topology-dependent:\n# IFACE=lo can delay both directions (~400ms for delay 200ms); one-way uplink/veth closer to +200ms\n# inside Docker container as root \u2014 omit sudo\ntc qdisc add dev \"$IFACE\" root netem delay 200ms\n# native Linux with sudo\n# sudo tc qdisc add dev \"$IFACE\" root netem delay 200ms\npython3 fault_client.py --url \"$URL\" --requests 40\n# Sample observation on lo: n=40 p50\u22480.40s p99\u22480.42s (both-ways delay); fails=0\n# Sample on one-way veth: p50 closer to \u22480.20s\n# inside Docker container as root \u2014 omit sudo\ntc qdisc del dev \"$IFACE\" root\n# native Linux with sudo\n# sudo tc qdisc del dev \"$IFACE\" root\n\n# Task 4 under loss 50%\n# inside Docker container as root \u2014 omit sudo\ntc qdisc add dev \"$IFACE\" root netem loss 50% delay 200ms\n# native Linux with sudo\n# sudo tc qdisc add dev \"$IFACE\" root netem loss 50% delay 200ms\n\n# (a) before vs after backoff at the same --max-retries 5:\n# before (stub): fixed time.sleep(0.05) \u2014 retries burst tightly (retry gaps ~0.05s)\n# after (full jitter): sleeps spread in [0, min(cap, base * factor**attempt)]\n# Compare retry timing/burstiness; retries_total counts attempts and is similar at the same max-retries\n# Sample after jitter: fails drops vs max-retries 0; retries_total rises (e.g. retries_total\u224880\u2013120 for 40 requests \u00d7 up to 5 retries under 50% loss)\npython3 fault_client.py --url \"$URL\" --requests 40 --max-retries 5\n\n# (b) FAIL rate: --max-retries 0 vs 5 \u2014 retries help under loss; retries_total is higher when max-retries > 0\n# Sample: --max-retries 0 \u2192 fails often near half the attempts; --max-retries 5 \u2192 fails much lower, retries_total >> 0\npython3 fault_client.py --url \"$URL\" --requests 40 --max-retries 0\npython3 fault_client.py --url \"$URL\" --requests 40 --max-retries 5\n# inside Docker container as root \u2014 omit sudo\ntc qdisc del dev \"$IFACE\" root\n# native Linux with sudo\n# sudo tc qdisc del dev \"$IFACE\" root\n\n# 100% DROP \u2014 FAIL lines cluster near the app connect/read timeouts (~2s here); stock client does not retry unless --max-retries > 0\n# Sample: FAIL 2.00xxs on each line; n=10 p50\u22482.00s p99\u22482.01s fails=10 retries_total=0\n# Retry-storm risk: if many clients retry immediately (no backoff) when the DROP clears, they stampede the recovering server in one synchronized burst; full-jitter backoff spreads that retry load over time.\n# inside Docker container as root \u2014 omit sudo\niptables -A INPUT -p tcp --dport 8080 -j DROP\n# native Linux with sudo\n# sudo iptables -A INPUT -p tcp --dport 8080 -j DROP\npython3 fault_client.py --url \"$URL\" --requests 10 --connect-timeout 2 --read-timeout 2\n# inside Docker container as root \u2014 omit sudo\niptables -D INPUT -p tcp --dport 8080 -j DROP\n# native Linux with sudo\n# sudo iptables -D INPUT -p tcp --dport 8080 -j DROP\n\n# Partition both ways \u2014 apply DROP before starting the client run\n# Clean both-ways DROP already in place: client FAILs (timeouts ~2s); Terminal A usually shows no new access-log lines for those attempts\n# Server-logged 200 with client FAIL is mainly in-flight work that completed before the DROP finished, asymmetric DROP (e.g. OUTPUT-only lost response), or a response lost after the server wrote the log line \u2014 not the typical outcome of a strict bidirectional partition applied before the requests\n# inside Docker container as root \u2014 omit sudo\niptables -A INPUT -s \"$CLIENT_IP\" -j DROP\niptables -A OUTPUT -d \"$CLIENT_IP\" -j DROP\n# native Linux with sudo\n# sudo iptables -A INPUT -s \"$CLIENT_IP\" -j DROP\n# sudo iptables -A OUTPUT -d \"$CLIENT_IP\" -j DROP\npython3 fault_client.py --url \"$URL\" --requests 10\n# Sample: fails=10, FAIL ~2.00s each; no new \"GET /\" lines on Terminal A during that run\n# inside Docker container as root \u2014 omit sudo\niptables -D INPUT -s \"$CLIENT_IP\" -j DROP\niptables -D OUTPUT -d \"$CLIENT_IP\" -j DROP\n# native Linux with sudo\n# sudo iptables -D INPUT -s \"$CLIENT_IP\" -j DROP\n# sudo iptables -D OUTPUT -d \"$CLIENT_IP\" -j DROP\n\n# Limping / fail-slow \u2014 very high delay, no loss: client looks \"dead\" on aggressive timeouts while the process is fine\n# Sample: FAIL ~2.00s (timeouts fire before the delayed response returns)\n# inside Docker container as root \u2014 omit sudo\ntc qdisc add dev \"$IFACE\" root netem delay 5000ms\n# native Linux with sudo\n# sudo tc qdisc add dev \"$IFACE\" root netem delay 5000ms\npython3 fault_client.py --url \"$URL\" --requests 5 --connect-timeout 2 --read-timeout 2\n# inside Docker container as root \u2014 omit sudo\ntc qdisc del dev \"$IFACE\" root\n# native Linux with sudo\n# sudo tc qdisc del dev \"$IFACE\" root\n\n```\n\nTask mapping: delay \u2192 queueing delay (measured p50/p99 jump); loss \u2192 lost packets / TCP retransmit (higher fails, retries_total rises with `--max-retries`); DROP/partition \u2192 lost request indistinguishable from the client (FAIL near timeout; clean both-ways DROP before requests usually adds no new server access lines); asymmetric DROP on OUTPUT only or lost response after accept \u2192 server may log success while the client times out. After a partition heals, application-level success handling still matters \u2014 TCP alone does not tell you whether the remote application finished the work.\n\nThis client sets socket connect/read timeouts, so FAIL latency tracks those (~2s) first. Kernel sysctls (`tcp_retries2`, `tcp_syn_retries`) are secondary and matter mainly when app timeouts are unset or larger than the kernel give-up. netem covers IP-layer faults; for process pauses use `kill -STOP` on the server PID, and for clock skew use tools like `libfaketime`."
    },
    {
      "task": "[Linux] Observe TCP retransmits, RTT, and cwnd with ss and tcpdump.",
      "question": "TCP hides packet loss via retransmission, but retransmits add unpredictable delay \u2014 the gap between transport 'reliability' and application correctness.\n\nPrerequisites: Linux network namespaces with `sudo`, `iproute2` (`tc` with netem, `ss`, `ip`), `tcpdump`, `python3`, and `curl`. On macOS, Docker Desktop (or Docker Engine) is the recommended primary path \u2014 run the lab inside a Linux container so `tc`/`ss`/`tcpdump` actually work. Same-host `IFACE=lo` is valid only when you are already on a Linux host/VM. A full Linux VM also works.\n\n```bash\n# macOS \u2014 Docker Desktop (recommended primary path)\nbrew install --cask docker\nopen -a Docker   # wait until Docker is running\ndocker --version\n\n# Debian/Ubuntu native Linux (or inside the container after apt-get)\nsudo apt-get update\nsudo apt-get install -y iproute2 tcpdump curl python3 procps\n\n# Confirm tools (run on Linux host or inside the container)\ntc -V\nss --version | head -n1\ntcpdump --version 2>&1 | head -n1\ncurl --version | head -n1\npython3 --version\n\n# List interfaces (you will set IFACE to match the topology below)\nip -br link\n```\n\nRecommended on macOS \u2014 one privileged Linux container with a mounted workdir. Create the lab directory on the host, save `big_response_server.py` and `measure_download.py` there, then run server + client + netem/ss/tcpdump inside the container (same netns, so `IFACE=lo` and `SERVER=127.0.0.1` apply):\n\n```bash\n# From the directory that will hold the lab files\nmkdir -p \"$HOME/tcp-lab\" && cd \"$HOME/tcp-lab\"\n\n# Start an interactive Debian container (NET_ADMIN for tc/tcpdump; mount this dir at /lab)\ndocker run --rm -it --name tcp-lab \\\n  --cap-add=NET_ADMIN --cap-add=NET_RAW \\\n  -v \"$PWD\":/lab -w /lab \\\n  debian:bookworm-slim bash\n\n# Inside the container \u2014 install tools once per container start\n# The container runs as root, so sudo is not needed for apt-get / tc / tcpdump\napt-get update\napt-get install -y iproute2 tcpdump curl python3 procps\ntc -V && ss --version | head -n1 && tcpdump --version 2>&1 | head -n1 && python3 --version\n```\n\nInside that container use two shells (`docker exec -it tcp-lab bash` for a second terminal) or background the server with `&`. All `tc`/`ss`/`tcpdump` commands below run inside the container, not on the macOS host.\n\nOptional two-container Docker network (server and client on separate containers sharing a user-defined bridge) \u2014 apply netem/tcpdump on the client (or server) container\u2019s carrying iface toward the peer IP, not on the Mac host:\n\n```bash\ndocker network create tcpnet\ndocker run -d --name tcp-server --network tcpnet \\\n  --cap-add=NET_ADMIN --cap-add=NET_RAW \\\n  -v \"$HOME/tcp-lab\":/lab -w /lab debian:bookworm-slim \\\n  bash -lc 'apt-get update && apt-get install -y python3 && python3 big_response_server.py'\ndocker run --rm -it --name tcp-client --network tcpnet \\\n  --cap-add=NET_ADMIN --cap-add=NET_RAW \\\n  -v \"$HOME/tcp-lab\":/lab -w /lab debian:bookworm-slim bash\n# Inside tcp-client: apt-get install -y iproute2 tcpdump curl python3 procps\n# SERVER=$(getent hosts tcp-server | awk '{print $1}')\n# IFACE=$(ip route get \"$SERVER\" | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}')\n# URL=http://${SERVER}:8080/\n```\n\nTopology (pick one and keep `SERVER`, `IFACE`, and `URL` consistent \u2014 do not pair `SERVER=127.0.0.1` with an uplink like eth0/en*):\n\nSame-host / single-container loopback (simplest on native Linux or inside the one-container Docker path):\n- `SERVER=127.0.0.1`\n- `IFACE=lo` (required so netem and tcpdump see the loopback HTTP path)\n- `URL=http://127.0.0.1:8080/`\n\nTwo hosts / two-container Docker network:\n- Bind the server on `0.0.0.0` and connect via the peer IP (not 127.0.0.1)\n- `SERVER=<peer IP>`\n- `IFACE=$(ip route get \"$SERVER\" | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}')` on the client (or the host where you apply netem/tcpdump)\n- `URL=http://${SERVER}:8080/`\n\nSetup \u2014 two terminals (or one container + `docker exec`). Terminal A runs the server; Terminal B runs the client.\n\nSave the server below as `big_response_server.py` and start it (Terminal A):\n\n```python\n# big_response_server.py \u2014 serves a fixed 10 MB body on GET /\n#!/usr/bin/env python3\nfrom http.server import HTTPServer, BaseHTTPRequestHandler\n\nBODY = b\"x\" * 10_000_000\n\n\nclass H(BaseHTTPRequestHandler):\n    def do_GET(self):\n        self.send_response(200)\n        self.send_header(\"Content-Length\", str(len(BODY)))\n        self.end_headers()\n        self.wfile.write(BODY)\n\n\nif __name__ == \"__main__\":\n    HTTPServer((\"0.0.0.0\", 8080), H).serve_forever()\n```\n\n```bash\npython3 big_response_server.py\n```\n\nBaseline client download (Terminal B) \u2014 record `time_total`. Set `SERVER` from your chosen topology:\n\n```bash\nSERVER=127.0.0.1   # loopback path \u2014 only with IFACE=lo\n# SERVER=<peer IP>  # two-host / Docker path\ncurl -o /dev/null -w 'total:%{time_total}s\\n' \"http://${SERVER}:8080/\"\n```\n\nFault injection \u2014 start with 5% loss and 100ms delay. Edit the `loss` percentage at the marked comment for Task 3. Set `IFACE` / `SERVER` from the Topology section (same-host / single-container: `IFACE=lo`; two-host/two-container: peer IP and the carrying iface from `ip route get`). Run these commands on the Linux host or inside the Docker container \u2014 not on the macOS host network stack.\n\n```bash\n# Same-host / single-container loopback example (netem/tcpdump must use lo, not eth0/en*):\nIFACE=lo\nSERVER=127.0.0.1\n\n# Two-host / two-container Docker example (uncomment and fill in):\n# SERVER=<peer IP>\n# IFACE=$(ip route get \"$SERVER\" | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}')\n\n# Task 2/3 starting point: 5% loss + 100ms delay\n# increase loss to 20% for task 3 (e.g. loss 20% delay 100ms)\n# inside Docker as root \u2014 omit sudo\ntc qdisc add dev \"$IFACE\" root netem loss 5% delay 100ms\n# native Linux\n# sudo tc qdisc add dev \"$IFACE\" root netem loss 5% delay 100ms\n\n# Cleanup after each experiment (same device as above)\n# inside Docker as root \u2014 omit sudo\ntc qdisc del dev \"$IFACE\" root\n# native Linux\n# sudo tc qdisc del dev \"$IFACE\" root\n```\n\nSave the measurement stub below as `measure_download.py`. Edit only at the `# implement` comment for Task 4 (wrap the download with monotonic and wall-clock timings).\n\n```python\n# measure_download.py \u2014 compare CLOCK_MONOTONIC vs wall-clock around one download\n#!/usr/bin/env python3\nfrom __future__ import annotations\n\nimport argparse\nimport subprocess\nimport time\n\n\ndef download_once(url: str) -> None:\n    # implement: record t0_mono=time.monotonic(), t0_wall=time.time();\n    # run curl to url (discard body; print curl time_total via -w);\n    # then print mono_elapsed and wall_elapsed.\n    # SIGSTOP/GC does NOT freeze clocks \u2014 both mono and wall keep advancing\n    # while the process is paused; elapsed values include the pause.\n    # Compare inflated mono_elapsed/wall_elapsed to an uninterrupted baseline\n    # curl time_total (do not use mono\u2212wall as a pause signal).\n    # Until Task 4, just run curl without timing.\n    subprocess.run(\n        [\"curl\", \"-o\", \"/dev/null\", \"-sS\", url],\n        check=False,\n    )\n\n\ndef main() -> None:\n    p = argparse.ArgumentParser(description=\"Monotonic vs wall-clock download timing\")\n    p.add_argument(\"--url\", default=\"http://127.0.0.1:8080/\")\n    args = p.parse_args()\n    download_once(args.url)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nDuring a download you can also watch transport state and capture packets. `ss` must be run on the server host (Terminal A) to observe the sender socket; on the client host you would see the receiver side, which rarely shows retransmit/cwnd activity:\n\n```bash\n# Run on the SERVER host (Terminal A) while curl is running in Terminal B:\nss -ti \"src ${SERVER}:8080\"   # sender socket: shows retrans, rtt, cwnd\n# Alternatively: ss -ti 'sport = :8080'\n# inside Docker as root \u2014 omit sudo\ntcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn\n# native Linux\n# sudo tcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn\n```\n\nTasks:\n1. On the server host (Terminal A), run `ss -ti \"src ${SERVER}:8080\"` (or `ss -ti 'sport = :8080'`) during download \u2014 find `retrans`, `rtt`, `cwnd` fields. How do retransmits correlate with curl total time?\n2. Run `tcpdump -i \"$IFACE\" host ${SERVER} and port 8080 -c 200` \u2014 look for duplicate sequence numbers that suggest retransmits. Treat them as supporting evidence alongside live `ss retrans>0` on the server host (on `IFACE=lo`, loopback can show the same segment twice, so do not assume every duplicate seq maps 1:1 to an `ss` retransmit counter). Prefer the two-host / two-container path if you want a clearer wire capture.\n3. Increase loss to 20% at the `# increase loss to 20%` comment site; does throughput collapse due to congestion window shrinkage?\n4. Implement the `# implement` block in `measure_download.py`: print `mono_elapsed` (`time.monotonic()`), `wall_elapsed` (`time.time()`), and curl `time_total` around one download. On a normal run, expect `mono \u2248 wall`. Use monotonic for duration. Optionally demonstrate (a) a process pause: `kill -STOP` the `measure_download.py`/`curl` PID mid-transfer, then `kill -CONT`. SIGSTOP does not freeze `CLOCK_MONOTONIC` or wall clocks \u2014 both keep ticking; when the process resumes it reads later timestamps, so `mono_elapsed` and `wall_elapsed` both include the pause and stay roughly equal. Compare those inflated values to an uninterrupted baseline curl `time_total` (not `mono\u2212wall`). (b) wall skew: note that an NTP step (or system suspend on Linux) can make `wall_elapsed` diverge from `mono_elapsed`.\n5. Explain: kernel ACK received \u2260 application processed the request \u2014 a key TCP limitation for distributed systems.",
      "answer": "Complete reference artifacts: the 10 MB server, a measurement script that times the download with monotonic and wall clocks (plus curl `time_total`), and the full `tc` / `ss` / `tcpdump` sequences for baseline, 5% loss, and 20% loss. On macOS, run server + client + `tc`/`ss`/`tcpdump` inside the Docker Linux container from the question (mounted `/lab`); same-host `IFACE=lo` only when already on Linux.\n\nSave and run the server as `big_response_server.py`:\n\n```python\n# big_response_server.py \u2014 reference (unchanged from setup)\n#!/usr/bin/env python3\nfrom http.server import HTTPServer, BaseHTTPRequestHandler\n\nBODY = b\"x\" * 10_000_000\n\n\nclass H(BaseHTTPRequestHandler):\n    def do_GET(self):\n        self.send_response(200)\n        self.send_header(\"Content-Length\", str(len(BODY)))\n        self.end_headers()\n        self.wfile.write(BODY)\n\n\nif __name__ == \"__main__\":\n    HTTPServer((\"0.0.0.0\", 8080), H).serve_forever()\n```\n\n```bash\npython3 big_response_server.py\n```\n\nSave the completed measurement client as `measure_download.py` (Task 4 solved):\n\n```python\n# measure_download.py \u2014 reference (monotonic + wall-clock around curl)\n#!/usr/bin/env python3\nfrom __future__ import annotations\n\nimport argparse\nimport subprocess\nimport time\n\n\ndef download_once(url: str) -> None:\n    t0_mono = time.monotonic()\n    t0_wall = time.time()\n    subprocess.run(\n        [\"curl\", \"-o\", \"/dev/null\", \"-sS\", \"-w\", \"curl_total:%{time_total}s\\\\n\", url],\n        check=False,\n    )\n    mono_elapsed = time.monotonic() - t0_mono\n    wall_elapsed = time.time() - t0_wall\n    print(f\"mono_elapsed={mono_elapsed:.4f}s wall_elapsed={wall_elapsed:.4f}s\")\n    print(f\"gap_wall_minus_mono={wall_elapsed - mono_elapsed:.6f}s\")\n    print(\"note: mono\u2248wall unless wall stepped/slewed or suspend diverges REALTIME vs MONOTONIC;\")\n    print(\"SIGSTOP/GC does not freeze clocks \u2014 both keep advancing; mono_elapsed and\")\n    print(\"wall_elapsed both include the pause. Compare them to baseline curl time_total.\")\n\n\ndef main() -> None:\n    p = argparse.ArgumentParser(description=\"Monotonic vs wall-clock download timing\")\n    p.add_argument(\"--url\", default=\"http://127.0.0.1:8080/\")\n    args = p.parse_args()\n    download_once(args.url)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nDocker Linux path (macOS primary) \u2014 after `docker run \u2026 debian:bookworm-slim` and `apt-get install` from the question, work in `/lab` inside the container (runs as root, so omit sudo on `tc`/`tcpdump`):\n\n```bash\n# Terminal A (inside container, or: docker exec -it tcp-lab bash)\npython3 big_response_server.py\n\n# Terminal B (same container netns)\nSERVER=127.0.0.1\nIFACE=lo\nURL=\"http://${SERVER}:8080/\"\n```\n\nFull measurement sequences with sample measured-style observations from a single-container `IFACE=lo` Docker run (your exact numbers will vary; record your own). Use the same topology as the question: either `IFACE=lo` with `SERVER=127.0.0.1`, or a peer `SERVER` with netem/tcpdump on the carrying iface from `ip route get \"$SERVER\"` (never eth0/en* + 127.0.0.1). On `IFACE=lo`, tcpdump can show the same segment twice on loopback \u2014 treat delayed duplicate seq numbers under loss as supporting evidence of retransmits when live `ss` also shows `retrans>0`, not as a strict 1:1 map to the `ss` counter; prefer the two-host path for clearer wire captures. Start tcpdump before each transfer (background here; second terminal is fine). Poll `ss -ti \"src ${SERVER}:8080\"` on the server host (Terminal A) while curl/measure_download runs \u2014 never after the transfer finishes; the client-side receiver socket rarely shows retransmit/cwnd changes. Cleanup on that same device between runs:\n\n```bash\n# Same-host / single-container loopback example\nSERVER=127.0.0.1\nIFACE=lo\n# Two-host / two-container Docker: SERVER=<peer IP>; IFACE=$(ip route get \"$SERVER\" | awk '{for(i=1;i<=NF;i++) if($i==\"dev\"){print $(i+1); exit}}')\nURL=\"http://${SERVER}:8080/\"\n\n# --- Baseline (clean network) ---\n# Expect: retrans ~0, curl time_total near LAN RTT + transfer; mono \u2248 wall\n# Sample observation on lo Docker: total:~0.05\u20130.15s; ss retrans:0; mono_elapsed\u2248wall_elapsed\u2248time_total\n# Task 1 \u2014 start capture, run download, poll ss while transfer is live\n# inside Docker as root \u2014 omit sudo\ntcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn &\n# native Linux\n# sudo tcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn &\nTCPDUMP_PID=$!\ncurl -o /dev/null -w 'total:%{time_total}s\\n' \"$URL\" &\nDL_PID=$!\nwhile kill -0 \"$DL_PID\" 2>/dev/null; do\n  ss -ti \"src ${SERVER}:8080\"   # Task 1 (run on server host): retrans~0, rtt stable, cwnd grows\n  sleep 0.3\ndone\nwait \"$DL_PID\"\nwait \"$TCPDUMP_PID\" 2>/dev/null || true\n# Task 2 \u2014 baseline tcpdump: mostly unique seq numbers (no loss-induced retransmits)\npython3 measure_download.py --url \"$URL\"\n# Sample: curl_total:0.08s mono_elapsed=0.0850s wall_elapsed=0.0851s gap\u22480\n\n# --- 5% loss + 100ms delay (Tasks 1\u20132 under loss) ---\n# inside Docker as root \u2014 omit sudo\ntc qdisc add dev \"$IFACE\" root netem loss 5% delay 100ms\n# native Linux\n# sudo tc qdisc add dev \"$IFACE\" root netem loss 5% delay 100ms\n# inside Docker as root \u2014 omit sudo\ntcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn &\n# native Linux\n# sudo tcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn &\nTCPDUMP_PID=$!\ncurl -o /dev/null -w 'total:%{time_total}s\\n' \"$URL\" &\nDL_PID=$!\nwhile kill -0 \"$DL_PID\" 2>/dev/null; do\n  ss -ti \"src ${SERVER}:8080\"   # Task 1 (run on server host): retrans>0, higher rtt, cwnd may dip\n  sleep 0.3\ndone\nwait \"$DL_PID\"\nwait \"$TCPDUMP_PID\" 2>/dev/null || true\n# Task 2: on the live capture, look for duplicate TCP seq numbers under loss; use them with\n# live ss retrans>0 on the server host \u2014 not as a 1:1 count match (especially on lo)\npython3 measure_download.py --url \"$URL\"\n# Sample observation: total:~1\u20133s (higher than baseline); ss shows retrans>0 / rtt\u2248100ms+;\n# tcpdump may show some duplicate seqs; mono_elapsed\u2248wall_elapsed and both track the slower transfer\n# inside Docker as root \u2014 omit sudo\ntc qdisc del dev \"$IFACE\" root\n# native Linux\n# sudo tc qdisc del dev \"$IFACE\" root\n\n# --- 20% loss + 100ms delay (Task 3 + Tasks 1\u20132) ---\n# inside Docker as root \u2014 omit sudo\ntc qdisc add dev \"$IFACE\" root netem loss 20% delay 100ms\n# native Linux\n# sudo tc qdisc add dev \"$IFACE\" root netem loss 20% delay 100ms\n# inside Docker as root \u2014 omit sudo\ntcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn &\n# native Linux\n# sudo tcpdump -i \"$IFACE\" \"host ${SERVER} and port 8080\" -c 200 -nn &\nTCPDUMP_PID=$!\ncurl -o /dev/null -w 'total:%{time_total}s\\n' \"$URL\" &\nDL_PID=$!\nwhile kill -0 \"$DL_PID\" 2>/dev/null; do\n  ss -ti \"src ${SERVER}:8080\"   # Task 3 (run on server host): more retrans, cwnd shrinkage, lower throughput\n  sleep 0.3\ndone\nwait \"$DL_PID\"\nwait \"$TCPDUMP_PID\" 2>/dev/null || true\n# Task 2/3: more duplicate seq candidates and higher ss retrans; time_total inflates further as cwnd shrinks\npython3 measure_download.py --url \"$URL\"\n# Sample observation: total often several\u00d7 baseline; ss cwnd smaller / retrans higher than at 5%\n# inside Docker as root \u2014 omit sudo\ntc qdisc del dev \"$IFACE\" root\n# native Linux\n# sudo tc qdisc del dev \"$IFACE\" root\n\n```\n\nExpected observations: under 5% loss, live `ss -ti \"src ${SERVER}:8080\"` on the server host shows `retrans>0` and `time_total` / `mono_elapsed` inflate together; under 20% loss, `cwnd` shrinks and throughput collapses further. Duplicate seq numbers on a live tcpdump capture are useful supporting evidence of retransmits when they coincide with `ss retrans>0`, but on loopback they are not a strict 1:1 map to the `ss` counter. On an uninterrupted run, `mono_elapsed \u2248 wall_elapsed`; a non-zero `gap_wall_minus_mono` points to wall-clock step/slew (NTP) or REALTIME vs MONOTONIC divergence across suspend \u2014 not a client process pause. To see a pause, SIGSTOP the curl child (or its process group) mid-transfer: clocks keep advancing, so both `mono_elapsed` and `wall_elapsed` inflate by roughly the pause duration and stay near each other \u2014 compare them to the baseline uninterrupted curl `time_total`. Prefer monotonic for duration measurement.\n\nDesign takeaways: retransmits are indistinguishable from a slow server to the application \u2014 both inflate response time. Timeouts must come from measured p99 under fault, not LAN p50. A kernel ACK means the OS received bytes, not that the application finished processing \u2014 use application-level success responses. L4S/explicit congestion marking aims to keep queues short so retransmits are less common, but does not eliminate unpredictability.\n\nCleanup if a qdisc remains: `tc qdisc del dev \"$IFACE\" root` inside Docker as root (omit sudo), or `sudo tc qdisc del dev \"$IFACE\" root` on native Linux. Document `sysctl net.ipv4.tcp_*` values when sharing results \u2014 they vary by distribution."
    },
    {
      "task": "[DST] Explore deterministic simulation testing with a mocked clock and network.",
      "question": "Deterministic simulation testing (DST) runs your real code with mocked clocks, network, and I/O so failures are reproducible. FoundationDB and TigerBeetle pioneered this; you will build a minimal version.\n\nPrerequisites: Python 3.10+ (stdlib only; no extra packages)\n\nSetup \u2014 save the file below as `dst_sim.py`. The module parses and the entrypoint is `python3 dst_sim.py`; stubs raise `NotImplementedError` until you fill them in at the `# implement \u2026` comments. When Tasks 1\u20134 are done, the same command prints `ALL CHECKS PASSED` and exits 0.\n\n```python\n# dst_sim.py \u2014 minimal deterministic simulation for leader lease + fencing\n#!/usr/bin/env python3\nfrom __future__ import annotations\n\nimport heapq\nfrom dataclasses import dataclass, field\nfrom typing import Dict, List, Optional, Set, Tuple\n\n\n@dataclass(order=True)\nclass Event:\n    time: float\n    kind: str = field(compare=False)\n    node: int = field(compare=False)\n    payload: dict = field(compare=False, default_factory=dict)\n\n\nclass SimNetwork:\n    def __init__(self) -> None:\n        self.queue: List[Event] = []\n        self.clock = 0.0\n        self.dropped: Set[Tuple[int, int]] = set()\n        self.delays: Dict[Tuple[int, int], float] = {}\n        # timed partition windows: (src, dst) -> list of (start, end)\n        self.partition_windows: Dict[Tuple[int, int], List[Tuple[float, float]]] = {}\n        self.log: List[str] = []\n        self._nodes: List[\"Node\"] = []  # set by run_scenario for elect() peer checks\n\n    def note(self, msg: str) -> None:\n        self.log.append(f\"t={self.clock:.1f} {msg}\")\n        print(self.log[-1])\n\n    def send(self, src: int, dst: int, kind: str, payload: dict, delay: float = 0.05) -> None:\n        if self._is_dropped(src, dst):\n            return\n        d = self.delays.get((src, dst), delay)\n        heapq.heappush(self.queue, Event(self.clock + d, kind, dst, payload))\n\n    def _is_dropped(self, src: int, dst: int) -> bool:\n        if (src, dst) in self.dropped:\n            return True\n        for start, end in self.partition_windows.get((src, dst), []):\n            if start <= self.clock < end:\n                return True\n        return False\n\n    def run_until(self, t: float):\n        while self.queue and self.queue[0].time <= t:\n            ev = heapq.heappop(self.queue)\n            self.clock = ev.time\n            yield ev\n\n    def set_partition(self, src: int, dst: int, start: float, end: float) -> None:\n        # implement timed partition drop: append (start, end) for (src, dst)\n        # so send() drops packets while start <= clock < end\n        raise NotImplementedError(\"set_partition\")\n\n\nclass Storage:\n    \"\"\"Shared storage that enforces fencing tokens on writes.\"\"\"\n\n    def __init__(self) -> None:\n        self.value: Optional[str] = None\n        self.max_token: int = 0\n        self.writes: List[Tuple[int, str, int]] = []  # (node, value, token)\n\n    def write_with_token(self, node_id: int, value: str, token: int) -> bool:\n        # implement fencing token on write: accept only if token > max_token;\n        # on accept update value/max_token and append to writes; return True/False\n        raise NotImplementedError(\"write_with_token\")\n\n\nclass Node:\n    def __init__(self, node_id: int, sim: SimNetwork) -> None:\n        self.id = node_id\n        self.sim = sim\n        self.is_leader = False\n        self.lease_until = 0.0\n        self.paused_until = 0.0\n        self.fencing_token = 0\n\n    def schedule_pause(self, start: float, duration: float) -> None:\n        # implement GC pause schedule: set paused_until = start + duration\n        # (on_message ignores work while clock < paused_until)\n        raise NotImplementedError(\"schedule_pause\")\n\n    def elect(self, token: int) -> None:\n        # implement election + dual-leader check using peers in self.sim._nodes:\n        # become leader with fencing_token=token, lease_until = clock + 2.0;\n        # log dual_leader=True if another peer still has is_leader and lease_until > clock\n        raise NotImplementedError(\"elect\")\n\n    def on_message(self, kind: str, payload: dict) -> None:\n        if self.sim.clock < self.paused_until:\n            return  # GC pause \u2014 ignore messages\n        if kind == \"heartbeat\" and payload.get(\"leader\") == self.id:\n            self.is_leader = True\n            self.lease_until = self.sim.clock + 2.0\n\n\ndef leaders_with_valid_lease(nodes: List[Node], clock: float) -> List[int]:\n    return [n.id for n in nodes if n.is_leader and n.lease_until > clock]\n\n\ndef snapshot_state(nodes: List[Node], storage: Storage, clock: float) -> tuple:\n    \"\"\"Deterministic fingerprint of final state for replay checks.\"\"\"\n    node_parts = tuple(\n        (n.id, n.is_leader, round(n.lease_until, 6), round(n.paused_until, 6), n.fencing_token)\n        for n in nodes\n    )\n    return (round(clock, 6), node_parts, storage.value, storage.max_token, tuple(storage.writes))\n\n\ndef run_scenario() -> tuple:\n    \"\"\"Drive the fixed fault schedule for Tasks 1\u20134. Implement stubs first.\"\"\"\n    sim = SimNetwork()\n    storage = Storage()\n    nodes = [Node(i, sim) for i in range(3)]\n    sim._nodes = nodes\n\n    # Node 0 starts as leader with a long lease and token 1\n    nodes[0].is_leader = True\n    nodes[0].lease_until = 10.0\n    nodes[0].fencing_token = 1\n\n    # Task 1 \u2014 schedule GC pause on leader covering t in [1.0, 6.0)\n    nodes[0].schedule_pause(start=1.0, duration=5.0)\n\n    # Task 2 \u2014 register timed drop 0\u21921 for [3.0, 8.0) before traffic in that window\n    sim.set_partition(0, 1, start=3.0, end=8.0)\n\n    # Task 1 \u2014 inject election at t=2.0 while node 0 is paused\n    sim.clock = 2.0\n    nodes[1].elect(token=2)\n    sim.note(f\"after_elect leaders={leaders_with_valid_lease(nodes, sim.clock)}\")\n\n    # Task 2 \u2014 attempt heartbeat during the partition window (must not enqueue)\n    sim.clock = 3.0\n    sim.send(0, 1, \"heartbeat\", {\"leader\": 0}, delay=0.1)\n    delivered = list(sim.run_until(5.0))\n    sim.note(f\"partition_dropped={len(delivered) == 0}\")\n\n    # Task 1 \u2014 after pause ends, former leader is still a zombie with valid lease\n    sim.clock = 6.0\n    zombie = nodes[0].is_leader and nodes[0].lease_until > sim.clock\n    sim.note(f\"zombie_leader_0={zombie}\")\n    # dual_leader was logged during elect() at t=2.0 (node 0 lease still valid)\n\n    # Task 4 \u2014 fencing: stale token rejected after re-election\n    sim.clock = 9.0\n    # New leader fences first; zombie write with lower token must be rejected\n    ok_fresh = storage.write_with_token(1, \"from-leader\", token=2)\n    ok_stale = storage.write_with_token(0, \"from-zombie\", token=1)\n    sim.note(f\"stale_rejected={ok_stale is False} fresh_accepted={ok_fresh is True}\")\n\n    return snapshot_state(nodes, storage, sim.clock), list(sim.log)\n\n\ndef replay() -> bool:\n    # implement deterministic replay: call run_scenario() twice;\n    # return True iff both snapshots (and logs) are equal\n    raise NotImplementedError(\"replay\")\n\n\ndef main() -> None:\n    snap1, log1 = run_scenario()\n    # Task 3 \u2014 determinism via replay()\n    same = replay()\n    print(f\"replay_identical={same}\")\n    # Expected signals after implementing stubs:\n    #   dual_leader=True  (from elect() at t=2.0 while node 0 still holds a lease)\n    #   zombie_leader_0=True\n    #   partition_dropped=True\n    #   stale_rejected=True fresh_accepted=True\n    #   replay_identical=True\n    assert same, \"replay diverged \u2014 scenario is not deterministic\"\n    assert any(\"dual_leader=True\" in line for line in log1), log1\n    assert any(\"zombie_leader_0=True\" in line for line in log1), log1\n    assert any(\"partition_dropped=True\" in line for line in log1), log1\n    assert any(\"stale_rejected=True\" in line for line in log1), log1\n    assert any(\"fresh_accepted=True\" in line for line in log1), log1\n    print(\"ALL CHECKS PASSED\")\n    print(\"state=\", snap1)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nRun (after implementing all stubs):\n\n```bash\npython3 dst_sim.py\n```\n\nExpect log lines containing `dual_leader=True` (from `elect` at t=2.0), `zombie_leader_0=True`, `partition_dropped=True`, `stale_rejected=True fresh_accepted=True`, `replay_identical=True`, then `ALL CHECKS PASSED`.\n\nTasks:\n1. Implement `Node.schedule_pause` and `Node.elect`. Confirm the run logs `zombie_leader_0=True` (paused node 0 still has `is_leader` with a valid lease after t=6.0 even though node 1 was elected at t=2.0).\n2. Implement `SimNetwork.set_partition`. Confirm `partition_dropped=True` (0\u21921 heartbeat during [3,8) never delivers). Confirm `elect` logged `dual_leader=True` at t=2.0 (node 0 still had a valid lease).\n3. Implement `replay` so two back-to-back `run_scenario()` calls yield identical snapshots and logs; `main` must print `replay_identical=True`.\n4. Implement `Storage.write_with_token` so after the new leader writes with token 2, the zombie write with token 1 is rejected; confirm `stale_rejected=True fresh_accepted=True`.\n5. In a short paragraph (no code): compare DST to Jepsen \u2014 what does deterministic simulation give you that real-world fault injection does not?",
      "answer": "Complete `dst_sim.py` with pause scheduling, election, timed partitions, fencing writes, and deterministic replay. Save as `dst_sim.py` and run `python3 dst_sim.py` \u2014 expect `ALL CHECKS PASSED`.\n\n```python\n# dst_sim.py \u2014 reference solution (Tasks 1\u20134 implemented)\n#!/usr/bin/env python3\nfrom __future__ import annotations\n\nimport heapq\nfrom dataclasses import dataclass, field\nfrom typing import Dict, List, Optional, Set, Tuple\n\n\n@dataclass(order=True)\nclass Event:\n    time: float\n    kind: str = field(compare=False)\n    node: int = field(compare=False)\n    payload: dict = field(compare=False, default_factory=dict)\n\n\nclass SimNetwork:\n    def __init__(self) -> None:\n        self.queue: List[Event] = []\n        self.clock = 0.0\n        self.dropped: Set[Tuple[int, int]] = set()\n        self.delays: Dict[Tuple[int, int], float] = {}\n        self.partition_windows: Dict[Tuple[int, int], List[Tuple[float, float]]] = {}\n        self.log: List[str] = []\n        self._nodes: List[\"Node\"] = []\n\n    def note(self, msg: str) -> None:\n        self.log.append(f\"t={self.clock:.1f} {msg}\")\n        print(self.log[-1])\n\n    def send(self, src: int, dst: int, kind: str, payload: dict, delay: float = 0.05) -> None:\n        if self._is_dropped(src, dst):\n            return\n        d = self.delays.get((src, dst), delay)\n        heapq.heappush(self.queue, Event(self.clock + d, kind, dst, payload))\n\n    def _is_dropped(self, src: int, dst: int) -> bool:\n        if (src, dst) in self.dropped:\n            return True\n        for start, end in self.partition_windows.get((src, dst), []):\n            if start <= self.clock < end:\n                return True\n        return False\n\n    def run_until(self, t: float):\n        while self.queue and self.queue[0].time <= t:\n            ev = heapq.heappop(self.queue)\n            self.clock = ev.time\n            yield ev\n\n    def set_partition(self, src: int, dst: int, start: float, end: float) -> None:\n        self.partition_windows.setdefault((src, dst), []).append((start, end))\n\n\nclass Storage:\n    def __init__(self) -> None:\n        self.value: Optional[str] = None\n        self.max_token: int = 0\n        self.writes: List[Tuple[int, str, int]] = []\n\n    def write_with_token(self, node_id: int, value: str, token: int) -> bool:\n        if token <= self.max_token:\n            return False\n        self.max_token = token\n        self.value = value\n        self.writes.append((node_id, value, token))\n        return True\n\n\nclass Node:\n    def __init__(self, node_id: int, sim: SimNetwork) -> None:\n        self.id = node_id\n        self.sim = sim\n        self.is_leader = False\n        self.lease_until = 0.0\n        self.paused_until = 0.0\n        self.fencing_token = 0\n\n    def schedule_pause(self, start: float, duration: float) -> None:\n        self.paused_until = start + duration\n\n    def elect(self, token: int) -> None:\n        others = [\n            n.id\n            for n in self.sim._nodes\n            if n.id != self.id and n.is_leader and n.lease_until > self.sim.clock\n        ]\n        self.is_leader = True\n        self.fencing_token = token\n        self.lease_until = self.sim.clock + 2.0\n        self.sim.note(\n            f\"elect node={self.id} token={token} dual_leader={len(others) > 0} others={others}\"\n        )\n\n    def on_message(self, kind: str, payload: dict) -> None:\n        if self.sim.clock < self.paused_until:\n            return\n        if kind == \"heartbeat\" and payload.get(\"leader\") == self.id:\n            self.is_leader = True\n            self.lease_until = self.sim.clock + 2.0\n\n\ndef leaders_with_valid_lease(nodes: List[Node], clock: float) -> List[int]:\n    return [n.id for n in nodes if n.is_leader and n.lease_until > clock]\n\n\ndef snapshot_state(nodes: List[Node], storage: Storage, clock: float) -> tuple:\n    node_parts = tuple(\n        (n.id, n.is_leader, round(n.lease_until, 6), round(n.paused_until, 6), n.fencing_token)\n        for n in nodes\n    )\n    return (round(clock, 6), node_parts, storage.value, storage.max_token, tuple(storage.writes))\n\n\ndef run_scenario() -> tuple:\n    sim = SimNetwork()\n    storage = Storage()\n    nodes = [Node(i, sim) for i in range(3)]\n    sim._nodes = nodes\n\n    nodes[0].is_leader = True\n    nodes[0].lease_until = 10.0\n    nodes[0].fencing_token = 1\n    nodes[0].schedule_pause(start=1.0, duration=5.0)\n    sim.set_partition(0, 1, start=3.0, end=8.0)\n\n    sim.clock = 2.0\n    nodes[1].elect(token=2)\n    sim.note(f\"after_elect leaders={leaders_with_valid_lease(nodes, sim.clock)}\")\n\n    sim.clock = 3.0\n    sim.send(0, 1, \"heartbeat\", {\"leader\": 0}, delay=0.1)\n    delivered = list(sim.run_until(5.0))\n    sim.note(f\"partition_dropped={len(delivered) == 0}\")\n\n    sim.clock = 6.0\n    zombie = nodes[0].is_leader and nodes[0].lease_until > sim.clock\n    sim.note(f\"zombie_leader_0={zombie}\")\n\n    sim.clock = 9.0\n    # New leader fences first; zombie write with lower token must be rejected\n    ok_fresh = storage.write_with_token(1, \"from-leader\", token=2)\n    ok_stale = storage.write_with_token(0, \"from-zombie\", token=1)\n    sim.note(f\"stale_rejected={ok_stale is False} fresh_accepted={ok_fresh is True}\")\n\n    return snapshot_state(nodes, storage, sim.clock), list(sim.log)\n\n\ndef replay() -> bool:\n    snap_a, log_a = run_scenario()\n    snap_b, log_b = run_scenario()\n    return snap_a == snap_b and log_a == log_b\n\n\ndef main() -> None:\n    snap1, log1 = run_scenario()\n    same = replay()\n    print(f\"replay_identical={same}\")\n    assert same, \"replay diverged \u2014 scenario is not deterministic\"\n    assert any(\"dual_leader=True\" in line for line in log1), log1\n    assert any(\"zombie_leader_0=True\" in line for line in log1), log1\n    assert any(\"partition_dropped=True\" in line for line in log1), log1\n    assert any(\"stale_rejected=True\" in line for line in log1), log1\n    assert any(\"fresh_accepted=True\" in line for line in log1), log1\n    print(\"ALL CHECKS PASSED\")\n    print(\"state=\", snap1)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nDST vs Jepsen (Task 5): DST controls the scheduler, clock, and message delivery order, so the same schedule replays bit-for-bit and failing traces shrink to a minimal deterministic bug. Jepsen injects real faults (partitions, kills, delays) against a live cluster and catches implementation/ops bugs, but wall-clock scheduling and OS nondeterminism mean you usually cannot replay an exact interleaving. Use DST for exhaustive reproducible schedules inside your process; use Jepsen to validate the real binary under genuine network and process faults.\n\nSafety invariants this harness checks: a zombie can still believe it is leader after a pause ( motivating fencing); storage rejects stale tokens (`token <= max_token`); identical snapshots on replay. Production DST systems (FoundationDB, TigerBeetle) scale this idea to randomized fault schedules with seed-based minimization."
    },
    {
      "task": "[TLA+] Model-check a distributed lock with lease expiry and a zombie writer.",
      "question": "TLA+ lets you specify a protocol and use the TLC model checker to explore states and verify invariants. You will model a classic distributed lock bug: a client pauses, its lease expires, another client acquires the lease, and the first client resumes and writes (a zombie writer).\n\nPrerequisites: Java 11+ (JVM required by TLC); TLA+ tools providing `tlc2.TLC` (standalone `tla2tools.jar`, or the TLA+ Toolbox).\n\n```bash\n# macOS \u2014 JVM\nbrew install --cask temurin\n\n# Debian/Ubuntu \u2014 JVM\n# sudo apt-get update && sudo apt-get install -y openjdk-17-jre\n\n# Standalone TLC (recommended for this lab)\n# Download tla2tools.jar from https://github.com/tlaplus/tlaplus/releases into this directory\ncurl -fsSL -o tla2tools.jar \\\n  https://github.com/tlaplus/tlaplus/releases/latest/download/tla2tools.jar\n\n# Or use the TLA+ Toolbox GUI instead of the CLI:\n# macOS: brew install --cask tla+-toolbox\n# Other OS: https://github.com/tlaplus/tlaplus/releases (Toolbox zip)\n\n# Confirm JVM and TLC\njava -version\njava -XX:+UseParallelGC -jar tla2tools.jar -help | head -n 5\n```\n\nSetup \u2014 save both files in the same directory as `tla2tools.jar`. The unsafe model is runnable as pasted: TLC should report an invariant violation for `NoCorruption` (zombie overwrite). Edit only at the `\\* TODO` comments for Task 2.\n\nSave as `Lock.tla`:\n\n```\n---- MODULE Lock ----\nEXTENDS Naturals, Sequences, TLC\n\nCONSTANTS Clients, Values, MaxToken\n\nVARIABLES\n  leaseHolder,   \\* 0 = free, else client id\n  fencingToken,  \\* last token issued by the lock service\n  storageValue,  \\* shared storage contents\n  maxTokenSeen,  \\* highest fencing token storage has accepted\n  clientToken,   \\* token each client received with its last grant\n  believesHold,  \\* clients that still think they hold the lease (zombies stay here)\n  lastWriter     \\* who last wrote storage (0 = never)\n\nvars == <<leaseHolder, fencingToken, storageValue, maxTokenSeen,\n          clientToken, believesHold, lastWriter>>\n\nInit ==\n  /\\ leaseHolder = 0\n  /\\ fencingToken = 0\n  /\\ storageValue = \"\"\n  /\\ maxTokenSeen = 0\n  /\\ clientToken = [c \\in Clients |-> 0]\n  /\\ believesHold = {}\n  /\\ lastWriter = 0\n\nGrantLease(client) ==\n  /\\ leaseHolder = 0\n  /\\ fencingToken < MaxToken\n  /\\ leaseHolder' = client\n  /\\ fencingToken' = fencingToken + 1\n  /\\ clientToken' = [clientToken EXCEPT ![client] = fencingToken']\n  /\\ believesHold' = believesHold \\union {client}\n  /\\ lastWriter' = 0\n  /\\ UNCHANGED <<storageValue, maxTokenSeen>>\n\n\\* Lease times out while the client may still believe it holds (GC pause / zombie)\nExpireLease ==\n  /\\ leaseHolder # 0\n  /\\ leaseHolder' = 0\n  /\\ UNCHANGED <<fencingToken, storageValue, maxTokenSeen,\n                 clientToken, believesHold, lastWriter>>\n\n\\* Write WITHOUT fencing (buggy): trusts the client's belief alone\nWriteUnsafe(client, val) ==\n  /\\ client \\in believesHold\n  /\\ storageValue' = val\n  /\\ lastWriter' = client\n  /\\ UNCHANGED <<leaseHolder, fencingToken, maxTokenSeen, clientToken, believesHold>>\n\n\\* TODO: implement WriteSafe \u2014 accept write only if\n\\* clientToken[client] = fencingToken /\\ clientToken[client] > maxTokenSeen;\n\\* on accept update storageValue, maxTokenSeen, lastWriter\nWriteSafe(client, val) ==\n  /\\ FALSE\n  /\\ UNCHANGED vars\n\nNext ==\n  \\/ \\E c \\in Clients : GrantLease(c)\n  \\/ ExpireLease\n  \\/ \\E c \\in Clients, v \\in Values : WriteUnsafe(c, v)\n  \\* TODO: switch WriteUnsafe \u2192 WriteSafe after implementing fencing\n\nSpec == Init /\\ [][Next]_vars\n\n\\* Fails when a non-holder (zombie) overwrites storage while another client holds the lease\nNoCorruption ==\n  (leaseHolder # 0 /\\ lastWriter # 0) => lastWriter = leaseHolder\n\n\\* TODO: add FencingOK == maxTokenSeen <= fencingToken\n\\* (and/or: lastWriter # 0 => clientToken[lastWriter] = maxTokenSeen)\n====\n```\n\nSave as `Lock.cfg` (CONSTANTS and the small state bound live here \u2014 do not invent other bounds):\n\n```\nSPECIFICATION Spec\nINVARIANT NoCorruption\n\\* TODO: add INVARIANT FencingOK after you define it in Lock.tla\nCONSTANTS\n  Clients = {1, 2}\n  Values = {\"A\", \"B\"}\n  MaxToken = 3\n```\n\nRun TLC on the unsafe setup (expects an invariant violation; inspect the error trace for client 1 writing after client 2 holds the lease):\n\n```bash\njava -XX:+UseParallelGC -jar tla2tools.jar -config Lock.cfg Lock\n```\n\nToolbox alternative: File \u2192 Open Spec \u2192 `Lock.tla`, create a model with the same CONSTANTS / INVARIANT / SPECIFICATION as `Lock.cfg`, then run TLC.\n\nTasks:\n1. Run TLC on the unsafe setup with the provided `Lock.cfg` (CONSTANTS and `MaxToken = 3`). Find the error-trace state where client 1 writes after client 2 holds the lease (`NoCorruption` violated).\n2. Implement `WriteSafe` at its TODO, switch `Next` from `WriteUnsafe` to `WriteSafe`, add `FencingOK`, and list it as an `INVARIANT` in `Lock.cfg`. Re-run TLC and confirm it finds no violations.\n3. Write the safety property in one sentence: what must never happen?\n4. Write the liveness property: what should eventually happen?\n",
      "answer": "Complete fixed `Lock.tla` and `Lock.cfg` for Task 2 only: implement `WriteSafe`, use it in `Next` instead of `WriteUnsafe`, define `FencingOK`, and list it as an `INVARIANT`. Save both files next to `tla2tools.jar` and re-run TLC \u2014 with `MaxToken = 3` and the small client/value sets below, expect no invariant violations.\n\n```\n---- MODULE Lock ----\nEXTENDS Naturals, Sequences, TLC\n\nCONSTANTS Clients, Values, MaxToken\n\nVARIABLES\n  leaseHolder,   \\* 0 = free, else client id\n  fencingToken,  \\* last token issued by the lock service\n  storageValue,  \\* shared storage contents\n  maxTokenSeen,  \\* highest fencing token storage has accepted\n  clientToken,   \\* token each client received with its last grant\n  believesHold,  \\* clients that still think they hold the lease (zombies stay here)\n  lastWriter     \\* who last wrote storage (0 = never)\n\nvars == <<leaseHolder, fencingToken, storageValue, maxTokenSeen,\n          clientToken, believesHold, lastWriter>>\n\nInit ==\n  /\\ leaseHolder = 0\n  /\\ fencingToken = 0\n  /\\ storageValue = \"\"\n  /\\ maxTokenSeen = 0\n  /\\ clientToken = [c \\in Clients |-> 0]\n  /\\ believesHold = {}\n  /\\ lastWriter = 0\n\nGrantLease(client) ==\n  /\\ leaseHolder = 0\n  /\\ fencingToken < MaxToken\n  /\\ leaseHolder' = client\n  /\\ fencingToken' = fencingToken + 1\n  /\\ clientToken' = [clientToken EXCEPT ![client] = fencingToken']\n  /\\ believesHold' = believesHold \\union {client}\n  /\\ lastWriter' = 0\n  /\\ UNCHANGED <<storageValue, maxTokenSeen>>\n\n\\* Lease times out while the client may still believe it holds (GC pause / zombie)\nExpireLease ==\n  /\\ leaseHolder # 0\n  /\\ leaseHolder' = 0\n  /\\ UNCHANGED <<fencingToken, storageValue, maxTokenSeen,\n                 clientToken, believesHold, lastWriter>>\n\n\\* Write WITHOUT fencing (buggy): trusts the client's belief alone\nWriteUnsafe(client, val) ==\n  /\\ client \\in believesHold\n  /\\ storageValue' = val\n  /\\ lastWriter' = client\n  /\\ UNCHANGED <<leaseHolder, fencingToken, maxTokenSeen, clientToken, believesHold>>\n\n\\* Fenced write: current lease token only, and strictly newer than storage\nWriteSafe(client, val) ==\n  /\\ client \\in believesHold\n  /\\ clientToken[client] = fencingToken\n  /\\ clientToken[client] > maxTokenSeen\n  /\\ storageValue' = val\n  /\\ maxTokenSeen' = clientToken[client]\n  /\\ lastWriter' = client\n  /\\ UNCHANGED <<leaseHolder, fencingToken, clientToken, believesHold>>\n\nNext ==\n  \\/ \\E c \\in Clients : GrantLease(c)\n  \\/ ExpireLease\n  \\/ \\E c \\in Clients, v \\in Values : WriteSafe(c, v)\n\nSpec == Init /\\ [][Next]_vars\n\n\\* Fails when a non-holder (zombie) overwrites storage while another client holds the lease\nNoCorruption ==\n  (leaseHolder # 0 /\\ lastWriter # 0) => lastWriter = leaseHolder\n\nFencingOK ==\n  /\\ maxTokenSeen <= fencingToken\n  /\\ lastWriter # 0 => clientToken[lastWriter] = maxTokenSeen\n====\n```\n\n```\nSPECIFICATION Spec\nINVARIANT NoCorruption\nINVARIANT FencingOK\nCONSTANTS\n  Clients = {1, 2}\n  Values = {\"A\", \"B\"}\n  MaxToken = 3\n```\n\n```bash\njava -XX:+UseParallelGC -jar tla2tools.jar -config Lock.cfg Lock\n```\n\nUnsafe vs fixed: with `WriteUnsafe`, TLC finds a counterexample such as GrantLease(1) \u2192 ExpireLease \u2192 GrantLease(2) \u2192 WriteUnsafe(1, \u2026) where `lastWriter = 1` while `leaseHolder = 2`. With `WriteSafe` in `Next`, that zombie overwrite is impossible: client 1's token is no longer equal to `fencingToken` after the re-grant, so TLC reports no violations of `NoCorruption` or `FencingOK` on this finite model.\n\nSafety (task 3): a client with a stale fencing token must never successfully overwrite storage after a newer lease has been granted (no zombie corruption).\n\nLiveness (task 4): a non-crashed client that requests a lease should eventually be granted one (and be able to write) if the lock service and network eventually make progress \u2014 TLC does not prove this under unbounded failure; it is a liveness caveat, not a safety invariant.\n\nOperational notes: keep `MaxToken` and client/value sets small or the state space explodes. The model is not the implementation \u2014 re-check TLC when the real lock protocol changes, and still validate the binary with Jepsen or DST.\n"
    }
  ]
}
