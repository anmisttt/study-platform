{
  "number": 4,
  "name": "Storage and Retrieval",
  "theory": [
    {
      "question": "Explain how to build a key-value store on top of an append-only file using an in-memory hash map. Describe how lookups work and explain the two structural limitations of this design.",
      "answer": "Headline: an in-memory hash map of `key -> byte offset` over an append-only log file. Bitcask (the default storage engine in Riak) does exactly this. It is genuinely fast, but it only works when (a) all keys fit in RAM and (b) you never need range queries.\n\nHow it actually works:\n - Write: append `key,value` to the data file, update the hash map with the new offset.\n - Read: hash-lookup in RAM → seek to the offset on disk → read the value. If the data is in the OS page cache, the read is satisfied with zero disk I/O.\n - Values can be larger than RAM (only the index has to fit); the workload that fits best is 'lots of writes per key, not too many distinct keys' — think a `play_count` counter keyed by video URL.\n\nFollow-up:\n - 'How would you scale beyond one machine?' Shard by hash of key (consistent hashing), keep per-shard hash + log; that's how Riak does it.\n - 'How do you free disk space?' Compaction over segment files — that's the next question.\n - 'When would you actually choose this over an LSM-tree or B-tree?' Bounded key set, write-heavy, point lookups only — counters, sessions, caches with persistence.\n\nComment: Redis is essentially this design taken to the limit (everything in RAM, optional AOF persistence) — and the production rule 'never run `KEYS *` against Redis' is just the no-range-scan limitation showing up in practice."
    },
    {
      "question": "Your append-only data file grows without bound as users overwrite the same keys. Explain compaction and segment merging and why an append-only design beats overwriting values in place.",
      "answer": "Headline: rotate the log into fixed-size segments, and run a background compactor that rewrites each segment keeping only the latest value for each key. Append-only beats in-place because sequential writes are an order of magnitude faster than random ones, crash recovery is trivial (you never see a half-written value), and merging keeps the on-disk footprint from drifting.\n\nHow this plays out in practice:\n - The compactor runs in a background thread on frozen, immutable segments.\n - Segments are never modified after they have been written, so the merged output is written to a new file. Once it is durably written, reads switch to the new merged segment and the old ones are deleted.\n - Several segments can be merged in one pass, which is essential because compaction usually shrinks segments meaningfully.\n\nFollow-up:\n - 'Doesn't compaction kill performance?' It competes for disk bandwidth with foreground writes — the standard mitigation is rate-limiting compaction and monitoring backlog (RocksDB calls it 'pending compaction bytes').\n - 'What if compaction can't keep up?' Segment count grows, reads slow down because they have to check more files, eventually disk fills. You need explicit monitoring.\n - 'How is this different from a log-structured filesystem?' Same principle (turn random writes into sequential), one level up.\n\nComment: Kafka exposes both modes — `log.cleanup.policy=delete` (time-based retention) and `log.cleanup.policy=compact` (keeps only the latest value per key, same idea). Postgres's `pg_repack` extension rebuilds a fragmented heap by copying live rows into a fresh file in the background — same trick again."
    },
    {
      "question": "Name the practical issues that a production hash-indexed log must solve beyond a basic append-only log plus hash map.",
      "answer": "Headline: the toy version is missing five things — a real binary record format, soft delete, snapshot-based crash recovery, partial-write detection, and a concurrency model. These five together turn the homework version into something that doesn't lose data in production.\n\nFollow-up:\n - 'Why one writer thread instead of many?' All writes touch the same file; multi-writer adds locking that erodes the sequential-write advantage. One writer + group commit is the standard pattern.\n - 'How big should a segment be?' Trade off: larger segments mean less merging overhead and fewer hash maps; smaller segments mean faster compaction and shorter recovery. A few hundred MB is typical.\n - 'What goes in the on-disk hash map snapshot?' Key → (offset, value size, timestamp) — enough to do a read directly without re-scanning.\n\nComment: Kafka's broker log uses every one of these tricks — binary length-prefixed records, periodic index files (the equivalent of Bitcask's hash-map snapshot), CRC32C per batch, single-threaded log appender per partition."
    },
    {
      "question": "Describe the layout of a Sorted String Table (SSTable) and three concrete advantages it has over a hash-indexed log.",
      "answer": "Headline: an SSTable is a segment file where the key-value pairs are sorted by key and each key appears at most once. That single invariant unlocks merge-sort-style compaction, a sparse in-memory index (so you don't need to keep every key in RAM), and block-level compression. It also gives you cheap range scans for free.\n\nThree concrete wins:\n\nBonus: range queries (`WHERE key BETWEEN ... AND ...`) become a single sequential scan instead of N point lookups — the exact thing hash indexes couldn't do.\n\nFollow-up:\n - 'How do you build a sorted file when writes arrive unsorted?' Memtable + WAL — that's the next question.\n - 'How dense should the sparse index be?' Trade-off between index size and bytes scanned per lookup. LevelDB uses one entry per ~16 KB block by default.\n - 'How does an SSTable handle deletes?' Same tombstone trick as before — a deletion marker key that wins over any older value until compaction drops both.\n\nComment: Cassandra's `*.db` files and RocksDB's `*.sst` files are SSTables, and each one ships with a small 'index block' (a sparse index) and a 'filter block' (Bloom filter, covered later)."
    },
    {
      "question": "Writes arrive in any order, but SSTable files must be sorted by key. Describe how a memtable using a red-black or AVL tree solves this, including the crash-recovery path.",
      "answer": "Headline: keep writes in an in-memory self-balancing BST (red-black or AVL tree) so they are always sorted, flush it to disk as an SSTable when it gets big enough, and use a separate write-ahead log to recover the memtable after a crash.\n\nThree balanced-tree choices and their engineering trade-offs:\n\n1. AVL tree (Adelson-Velsky and Landis, 1962). A binary search tree where, for every node, the heights of the left and right subtrees differ by at most 1. After every insert or delete, the tree may rotate (a constant-time local pointer rewiring) until the invariant holds again. The strict balancing keeps height very close to log₂(n), so lookups are as fast as a BST can be, but writes do more rotations on average.\n   Shape after inserting 1..7 in order: a tree of height 3 with 4 as root, 2 and 6 as children, etc. — no degeneration into a linked list.\n   Use when: read-heavy in-memory index, no concurrent writers.\n\n2. Red-black tree (Bayer, 1972; Guibas & Sedgewick, 1978). Also a BST, but each node carries a single bit of color (red or black) and the tree maintains four invariants: the root is black, no red node has a red child, every root-to-leaf path passes through the same number of black nodes, and null leaves are black. These invariants are looser than AVL's, so the tree height is bounded by 2·log₂(n+1) — a bit taller — but rebalancing requires fewer rotations per write. \n   Use when: balanced read/write mix; this is the default ordered map in most standard libraries (`std::map` in C++, `TreeMap` in Java, `BTreeMap` is the exception in Rust — see below).\n\n3. B-tree (Bayer & McCreight, 1970). A multi-way generalization: every internal node holds up to k sorted keys and up to k+1 child pointers. When a node overflows, it splits and pushes the median up to the parent — propagating up only when the parent itself overflows, which is what keeps the tree balanced. The branching factor k is chosen so a node fits in one block of the storage medium (a CPU cache line for in-memory B-trees, a 4-16 KB disk page for on-disk ones).\n   Why it dominates on disk: a B-tree of fan-out 500 reaches 256 TB in 4 levels, so a key lookup is at most 4 page reads. A binary tree (red-black/AVL) over the same data would be ~31 levels deep — 31 random disk seeks per lookup, which is unusable.\n\nWhy not a B-tree for the memtable: B-trees pay off when the cost of moving a node into the working set (a disk page or a cache line) dominates the cost of comparisons inside the node. In RAM that's barely true — binary trees have simpler insertion logic, and a skip list (used by RocksDB and Redis sorted sets) is even easier to make lock-free for concurrent writers. B-trees are especially useful on disk, where wide fan-out reduces the number of page reads.\n\nReads scan in order of recency: memtable first, then the newest on-disk SSTable, then progressively older ones.\n\nSo a complete write is: append to WAL on disk → insert into memtable in RAM → ack the client. The WAL is not sorted and never read in steady state; it's purely insurance.\n\nFollow-up:\n - 'AVL or red-black, which would you pick?' Red-black for the standard library / mixed workload, AVL when reads dominate. In practice an LSM memtable uses a skip list — same asymptotics, much simpler lock-free implementation.\n - 'Why not a B-tree for the memtable?' In RAM, comparisons are cheap relative to pointer chases; the wide-fan-out advantage of a B-tree only shows up when a node fetch is expensive (disk page or cache line).\n - 'What happens if the memtable flush fails halfway through?' The flush writes to a new file; only after `fsync` does the engine switch the WAL pointer. Half-flushed files are discarded on recovery.\n - 'How do you avoid blocking writes during a flush?' Double-buffered memtables — the new one accepts writes while the old one is being serialized.\n\nComment: RocksDB and LevelDB both use a skip list memtable by default and expose the WAL as `WAL_ttl_seconds`. Cassandra also uses a skip list (`memtable_allocation_type=offheap_objects`) and calls the WAL the commitlog. The Linux kernel uses red-black trees inside the CFS scheduler and in `mm/rmap.c`; `std::map` and Java's `TreeMap` are red-black; PostgreSQL and MySQL InnoDB use B+ trees on disk."
    },
    {
      "question": "Describe the full read and write path of an LSM-tree and explain why Bloom filters and different compaction strategies are added in real implementations.",
      "answer": "Headline: an LSM-tree is memtable + WAL on the write path, and memtable → newest SSTable → older SSTables on the read path, with background compaction stitching things together. Bloom filters short-circuit reads for keys that don't exist; size-tiered vs leveled compaction trade write amplification against read fan-out.\n\nWrite path (in one breath): append to WAL → insert into the in-memory memtable (red-black tree / skip list) → ack. When the memtable hits a threshold, flush it as the newest on-disk SSTable, drop the corresponding WAL segment.\n\nFollow-up:\n - 'When does an LSM-tree fall over?' When compaction can't keep up with writes — segments pile up, reads slow down, eventually disk fills. The fix is rate-limiting writes or scaling out.\n - 'Why is the Bloom filter sized per SSTable, not global?' Each SSTable is immutable, so its membership set is fixed and the filter never needs to support deletes.\n - 'What about range queries?' SSTables are sorted, so a range scan reads each level in parallel and merges — but compaction artifacts and tombstones can hurt latency.\n\nComment: in RocksDB `bloom_filter` and `compaction_style` are the exact knobs that map onto this paragraph. ScyllaDB defaults to size-tiered; CockroachDB and TiDB sit on top of RocksDB with leveled compaction."
    },
    {
      "question": "Describe how a B-tree stores data, define branching factor, and explain how the algorithm keeps the tree balanced as keys are inserted.",
      "answer": "Headline: a B-tree is a sorted on-disk tree of fixed-size pages (usually 4 KB), where each interior page has hundreds of child pointers and the tree splits pages on insert to stay balanced. A 4-level B-tree with branching factor 500 indexes 256 TB — which is why almost every relational database uses one.\n\nFollow-up:\n - 'Why fixed-size pages?' They match the disk sector / SSD page boundary, so one read = one page = one I/O. Modern engines often run on 8 KB or 16 KB pages.\n - 'How is delete different from insert?' Deleting from a leaf is easy; rebalancing (page merge) when leaves get half-empty is more involved — that's why most engines defer it.\n - 'B-tree vs B+ tree?' B+ tree stores values only in leaves, keeps interior pages narrow (more children per page), and links leaves left-right for fast range scans. Almost every 'B-tree' in a real database is a B+ tree.\n\nComment: PostgreSQL's nbtree, MySQL InnoDB's clustered primary index, SQL Server's clustered indexes, Oracle's standard indexes — all B+ trees. The 256 TB number is why a single-machine relational database can plausibly hold a company's entire transactional dataset."
    },
    {
      "question": "B-tree updates overwrite pages in place. Describe what can go wrong on a crash, and how WAL, copy-on-write, and latches protect against it.",
      "answer": "Headline: a B-tree update can touch multiple pages (split + parent rewrite), and a crash mid-operation corrupts the index. Two production solutions: a write-ahead log that records each modification before it's applied, or copy-on-write (LMDB) that writes new pages to fresh locations and atomically swaps the root pointer. Latches handle in-memory concurrency, separately from user-visible row locks.\n\nFollow-up:\n - 'What is a torn write?' A page write that gets partially applied (e.g., 4 KB write across two physical sectors); PostgreSQL handles it by writing full-page images to WAL the first time a page is dirtied after a checkpoint (`full_page_writes=on`).\n - 'When is copy-on-write better?' Heavy snapshot workloads, embedded use, when you want no WAL — that's the LMDB pitch.\n - 'Why does InnoDB do a double-write buffer?' Same torn-write protection but at the storage engine layer instead of at the WAL layer.\n\nComment: PostgreSQL WAL + checkpointing implements ARIES-style recovery; LMDB-derived stores (BoltDB, MDBX) use pure CoW; SQLite added WAL mode in 2010 and that's why concurrent readers stopped blocking writers."
    },
    {
      "question": "Define write amplification, explain why both B-trees and LSM-trees suffer it, and list two B-tree optimizations.",
      "answer": "Headline: write amplification is the ratio of bytes the storage engine writes to disk vs bytes the application sent it. Both architectures pay it — B-trees from WAL + full-page writes + splits, LSM-trees from compaction. It matters because SSDs have finite erase cycles and because in write-heavy systems disk bandwidth is the throughput ceiling.\n\nFollow-up:\n - 'Which architecture has lower write amplification in practice?' Depends entirely on workload. Leveled LSM compaction can hit 10-30× WA; a B-tree can be 2-5× if rows are small relative to the page. Benchmark before claiming.\n - 'How do SSDs hide this from you?' The flash translation layer (FTL) does its own log-structured rewriting internally, so the OS-level write count understates the on-flash count. Tools like `smartctl` expose 'Total LBAs Written' and 'NAND Writes' to compare.\n - 'How to measure it?' RocksDB exposes `rocksdb.compact.write.bytes / rocksdb.bytes.written`. For Postgres, compare WAL bytes (`pg_stat_wal.wal_bytes`) to logical bytes written by clients.\n\nComment: this is exactly the metric that drove Facebook to invest in RocksDB tuning — at their scale, halving write amplification means halving the SSD fleet for write-heavy workloads."
    },
    {
      "question": "Compare LSM-trees and B-trees in terms of write throughput, read latency, tail latency, fragmentation, and transactional locking.",
      "answer": "Follow-up:\n - 'Which one do you pick for a metrics store?' LSM — InfluxDB, Prometheus's TSDB, M3DB are all LSM-style. Write-mostly, append-mostly, time-ordered keys.\n - 'Why does Discord use ScyllaDB instead of Postgres for messages?' Write-heavy fan-out + needs single-digit-ms reads at scale + no multi-row transactions needed. Same reason Cassandra was the prior choice.\n - 'Why does every fintech still use Postgres?' Predictable latency + strict serializable isolation via row locks attached to the B-tree.\n\nComment: see the RUM Conjecture (Athanassoulis et al.) — read, update, and memory overhead are a three-way trade-off; you can be good at two but not all three. B-trees pick read+memory; LSM-trees pick update+memory; main-memory stores pick read+update."
    },
    {
      "question": "How to build a secondary index on a non-unique column? Explain the two ways to handle non-unique keys and the tradeoff between heap-file, clustered, and covering indexes.",
      "answer": "Headline: a secondary index on a non-unique column either stores a postings list per key, or appends a row-id suffix to each key. Where the row data actually lives is a second decision: heap file (one copy of the row, indexes point in), clustered index (row inlined in the index — InnoDB primary), or covering index (some columns inlined). Each step trades read speed for write cost and storage.\n\nThree storage choices — the canonical trade-off:\n\nFollow-up:\n - 'When would you NOT add a covering index?' Write-heavy column, or many secondary indexes already (multiplies the storage and write cost).\n - 'Why does InnoDB make secondary index reads slower?' They store PK references, so a covering query is great but a non-covering one is two B-tree walks.\n - 'How does an LSM handle non-unique secondary indexes?' Same two options. Cassandra stores `<index_value, primary_key>` as the SSTable key — appending the PK to make it unique.\n\nComment: in PostgreSQL the covering-index syntax is `CREATE INDEX ... INCLUDE (col1, col2)`; in MySQL InnoDB you 'just get' a covering index when the SELECT list + WHERE columns all appear in some secondary index. Always check with `EXPLAIN` — 'Using index' (MySQL) or 'Index Only Scan' (Postgres) is the marker."
    },
    {
      "question": "Describe how full-text search and fuzzy indexes work, including the data structure Lucene uses for typo-tolerant search.",
      "answer": "Headline: a full-text index is an inverted index — `term -> postings list of document IDs` — stored in LSM-like immutable sorted segments. For typo tolerance, Lucene uses a finite-state automaton over the terms, then converts it on the fly into a Levenshtein automaton that emits every term within edit-distance k.\n\nSo at the storage layer, Lucene is just an LSM-tree with terms as keys. New documents go into an in-memory segment; the segment flushes to disk as an immutable inverted index; background merges combine segments. That's why `_refresh` in Elasticsearch is cheap (just makes the latest in-memory segment searchable) but `_forcemerge` is expensive (rewrites everything).\n\nFollow-up:\n - 'Why use Elasticsearch instead of `WHERE body LIKE '%foo%'`?' LIKE with a leading wildcard cannot use a B-tree (the index is sorted left-to-right). Even with a trigram index, ranking and tokenization features aren't there.\n - 'How does relevance ranking work?' BM25 / TF-IDF — score = function of term frequency in doc and inverse document frequency across the corpus. Each posting in the postings list stores positions and term frequency.\n - 'What about vector search?' Modern Lucene (and Elasticsearch / OpenSearch) added HNSW graphs alongside the inverted index, for k-NN over embeddings. Same engine, different index structure.\n\nComment: PostgreSQL approximates this with a GIN index over a `tsvector` column — same idea (postings lists), simpler tokenizer, lower ceiling. For true full-text + scale + relevance ranking, run a dedicated engine."
    },
    {
      "question": "Compare OLTP and OLAP across read pattern, write pattern, dataset size, and bottleneck. Why does running both on one engine usually fail?",
      "answer": "Headline: OLTP is small reads + small writes keyed by primary key, latency-sensitive, seek-bound; OLAP is huge scans over a few columns of many rows, throughput-bound, bandwidth-limited. A single engine struggles because (a) analytical queries blow out the OLTP buffer cache and lock contention spikes, and (b) the index structures that win for OLTP (B-tree on a primary key) are useless for full-table aggregates.\n\n| Property         | OLTP                                       | OLAP                                       |\n| Read pattern     | Small number of records per query, by key  | Aggregate over a large number of records   |\n| Write pattern    | Random-access, low-latency, user input     | Bulk import (ETL) or event stream          |\n| Primary user     | End user / customer via web app            | Internal analyst, for decision support     |\n| Data represents  | Latest state of data (current point in time)| History of events over time               |\n| Dataset size     | GB to TB                                    | TB to PB                                   |\n| Bottleneck       | Disk seek time                              | Disk bandwidth                             |\n\nComment: modern stacks usually look like Postgres / MySQL for OLTP + Snowflake / BigQuery / Redshift / ClickHouse for OLAP + dbt for transformations. HTAP databases (TiDB, SingleStore, Snowflake's Unistore) try to merge both, with mixed results in practice."
    },
    {
      "question": "Describe column-oriented storage and four optimizations that make it fast on analytical queries: bitmap encoding, vectorized processing, materialized views, and data cubes.",
      "answer": "Headline: a column store transposes the row store — each column gets its own file, with rows in the same order across files. Four optimizations stack on top: bitmap encoding (especially for low-cardinality columns), vectorized SIMD operators over compressed blocks, materialized views that precompute hot aggregates, and data cubes that precompute aggregates across multiple dimensions. This is the engine model behind Snowflake, BigQuery, Redshift, ClickHouse, and Parquet.\n\nFollow-up:\n - 'When is column store the wrong choice?' Anything user-facing, point-lookup, transactional. Don't put your shopping cart in ClickHouse.\n - 'How do you keep the cube fresh?' Incrementally — Snowflake and ClickHouse have `SUMMING`/aggregating materialized views that update on write. Or refresh on a schedule for hourly dashboards.\n - 'Why is Parquet so popular?' Same column-store ideas, but as a portable file format — column groups, dictionary encoding (bitmap-style), delta encoding for timestamps. Lets every engine (Spark, Trino, DuckDB, Snowflake) read the same files.\n\nComment: ClickHouse's `LowCardinality(String)` is bitmap encoding made into a column type; `SummingMergeTree` and `AggregatingMergeTree` are the cube engine. DuckDB makes the whole stack runnable on a laptop with the same column-store, vectorized-execution architecture."
    }
  ],
  "practice": [
    {
      "task": "[PostgreSQL vs ClickHouse] You have an e-commerce orders system. Pick the right database (or combination) for the workload below and write the CREATE TABLE plus indexes for each side.",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\n# Copy the lab files into the current directory:\n# docker-compose.yml, setup.sql (PostgreSQL OLTP stub), olap.sql (ClickHouse OLAP stub), seed.sql\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch4-p0 init\n\n# Start both engines: PostgreSQL 16 on 5432 (database lab, user postgres, password lab)\n# and ClickHouse 24.8 on 9000 (native) / 8123 (HTTP)\ndocker compose up -d\n\n# PostgreSQL — apply setup.sql (it re-creates the three tables and re-inserts the seed rows)\ndocker compose exec -T postgres psql -v ON_ERROR_STOP=1 -U postgres -d lab < setup.sql\n\n# PostgreSQL — interactive session\ndocker compose exec postgres psql -U postgres -d lab\n\n# ClickHouse — apply olap.sql\ndocker compose exec -T clickhouse clickhouse-client --multiquery < olap.sql\n\n# ClickHouse — interactive session\ndocker compose exec clickhouse clickhouse-client\n\n# Teardown\ndocker compose down -v\n```\n\nAn e-commerce platform has two very different query patterns hitting the same logical orders data. Split the work across the right engines, complete the DDL stubs, apply them, and run the sample queries.\n\n`docker compose up -d` loads `seed.sql` into the PostgreSQL database `lab`, so `products`, `orders`, and `order_items` already exist with the seed rows below before you change anything; ClickHouse starts empty and gets its schema and its four `fact_orders` rows when you apply `olap.sql`. Both stubs apply as given; complete every `-- choose ...` / `-- implement ...` site in the Tasks below before re-applying for verification. The commented demo SELECTs show what each side must support; they are not the only setup.\n\n```sql\n-- setup.sql — PostgreSQL operational schema (stubs)\nDROP TABLE IF EXISTS order_items CASCADE;\nDROP TABLE IF EXISTS orders CASCADE;\nDROP TABLE IF EXISTS products CASCADE;\n\nCREATE TABLE products (\n  id       BIGINT PRIMARY KEY,\n  name     TEXT NOT NULL,\n  category TEXT NOT NULL\n);\n\nCREATE TABLE orders (\n  id            BIGINT PRIMARY KEY,\n  customer_id   BIGINT        NOT NULL,\n  status        TEXT          NOT NULL,\n  total_amount  NUMERIC(10,2) NOT NULL,\n  created_at    TIMESTAMPTZ   NOT NULL DEFAULT now(),\n  updated_at    TIMESTAMPTZ   NOT NULL DEFAULT now()\n);\n\n-- choose indexes for:\n--   WHERE customer_id = $1 ORDER BY created_at DESC LIMIT 20\n-- optional: partial index for open statuses (pending/processing)\n-- (PRIMARY KEY on id already covers WHERE id = $1)\n\nCREATE TABLE order_items (\n  order_id    BIGINT  NOT NULL REFERENCES orders(id),\n  product_id  BIGINT  NOT NULL REFERENCES products(id),\n  quantity    INT     NOT NULL,\n  unit_price  NUMERIC(10,2) NOT NULL\n  -- choose PRIMARY KEY (order_id, product_id)\n);\n\nINSERT INTO products (id, name, category) VALUES\n  (1, 'Keyboard', 'Electronics'),\n  (2, 'Hoodie', 'Clothing');\nINSERT INTO orders (id, customer_id, status, total_amount, created_at) VALUES\n  (1001, 42, 'pending', 79.98, now() - interval '2 days'),\n  (1002, 42, 'shipped', 49.99, now() - interval '1 day'),\n  (1003, 7,  'pending', 29.99, now());\nINSERT INTO order_items (order_id, product_id, quantity, unit_price) VALUES\n  (1001, 1, 1, 49.99),\n  (1001, 2, 1, 29.99),\n  (1002, 2, 1, 49.99),\n  (1003, 1, 1, 29.99);\n\n-- Demo workload A (uncomment after apply)\n-- SELECT id, customer_id, status, total_amount, created_at FROM orders WHERE id = 1001;\n-- SELECT id, status, total_amount, created_at FROM orders WHERE customer_id = 42 ORDER BY created_at DESC LIMIT 20;\n```\n\n```sql\n-- olap.sql — ClickHouse analytics schema (stubs)\nDROP TABLE IF EXISTS fact_orders;\nDROP TABLE IF EXISTS fact_orders_monthly_by_category;\n\nCREATE TABLE fact_orders (\n  event_date    Date,\n  event_time    DateTime,\n  order_id      UInt64,\n  customer_id   UInt64,\n  product_id    UInt32,\n  category_id   String,          -- choose LowCardinality(String)\n  status        String,          -- choose LowCardinality(String)\n  country_code  FixedString(2),  -- choose LowCardinality(FixedString(2))\n  quantity      UInt32,\n  unit_price    Decimal(10, 2),\n  revenue       Decimal(12, 2) MATERIALIZED quantity * unit_price\n)\nENGINE = MergeTree               -- choose ENGINE (MergeTree is correct here)\nPARTITION BY tuple()             -- choose PARTITION BY toYYYYMM(event_date)\nORDER BY tuple()                 -- choose ORDER BY (event_date, category_id, customer_id)\nSETTINGS index_granularity = 8192;\n\n-- implement monthly-by-category MV with SummingMergeTree (sum revenue by month + category)\n-- CREATE MATERIALIZED VIEW fact_orders_monthly_by_category\n-- ENGINE = SummingMergeTree\n-- PARTITION BY ...\n-- ORDER BY ...\n-- AS SELECT ... FROM fact_orders GROUP BY ...;\n\nINSERT INTO fact_orders\n  (event_date, event_time, order_id, customer_id, product_id, category_id, status, country_code, quantity, unit_price)\nVALUES\n  (today() - 40, now() - INTERVAL 40 DAY, 1001, 42, 1, 'Electronics', 'shipped', 'DE', 1, 49.99),\n  (today() - 40, now() - INTERVAL 40 DAY, 1001, 42, 2, 'Clothing',    'shipped', 'DE', 1, 29.99),\n  (today() - 10, now() - INTERVAL 10 DAY, 1002, 42, 2, 'Clothing',    'shipped', 'DE', 1, 49.99),\n  (today() - 2,  now() - INTERVAL 2 DAY,  1003, 7,  1, 'Electronics', 'pending', 'US', 1, 29.99);\n\n-- Demo workload B-style checks (uncomment after apply)\n-- SELECT toStartOfMonth(event_date) AS month, category_id, sum(revenue) AS revenue\n-- FROM fact_orders GROUP BY month, category_id ORDER BY month, revenue DESC;\n-- SELECT toDate(event_time) AS day, status, count() AS orders\n-- FROM fact_orders WHERE event_time >= now() - INTERVAL 90 DAY\n-- GROUP BY day, status ORDER BY day, status;\n```\n\nWorkload A rates (OLTP): ~30k reads/s, ~5k writes/s, p99 under 50 ms, ~500 GB growing ~1 GB/day.\nWorkload B rates (OLAP): ~50 qps analytics, bulk ingest ~1M events/day, p99 under 5 s, ~5 TB.\n\nTasks:\n1. State which engine runs Workload A and which runs Workload B (one short sentence each, tied to the rates above).\n2. Edit `setup.sql`: add the indexes marked by the choose-comments, and set the PRIMARY KEY on `order_items`. Apply it with the `docker compose exec -T postgres psql ... < setup.sql` command above.\n3. Run the two demo Workload A SELECTs in the `psql` session. Success: order `1001` returns one row; customer `42` returns orders `1002` then `1001` (newest first).\n4. Edit `olap.sql`: switch low-cardinality columns, set `PARTITION BY` / `ORDER BY` (keep `MergeTree`), and implement the monthly-by-category materialized view. Apply it with the `docker compose exec -T clickhouse clickhouse-client --multiquery < olap.sql` command above.\n5. Run the two demo Workload B-style checks in the `clickhouse-client` session. Success: monthly revenue includes Electronics and Clothing; daily counts include pending and shipped. Optionally `SELECT * FROM fact_orders_monthly_by_category FINAL` shows pre-aggregated monthly rows.",
      "answer": "Run two engines side by side. PostgreSQL owns Workload A (OLTP): key-driven point lookups and short recent-order lists with p99 under 50 ms. ClickHouse owns Workload B (OLAP): large scans, GROUP BY aggregates, and bulk ingest over TB-scale history. In production, CDC (for example Debezium → Kafka → ClickHouse) keeps the analytical copy fresh; the order-details page always reads Postgres.\n\nComplete `setup.sql`:\n\n```sql\n-- setup.sql — PostgreSQL operational schema\nDROP TABLE IF EXISTS order_items CASCADE;\nDROP TABLE IF EXISTS orders CASCADE;\nDROP TABLE IF EXISTS products CASCADE;\n\nCREATE TABLE products (\n  id       BIGINT PRIMARY KEY,\n  name     TEXT NOT NULL,\n  category TEXT NOT NULL\n);\n\nCREATE TABLE orders (\n  id            BIGINT        PRIMARY KEY,\n  customer_id   BIGINT        NOT NULL,\n  status        TEXT          NOT NULL,\n  total_amount  NUMERIC(10,2) NOT NULL,\n  created_at    TIMESTAMPTZ   NOT NULL DEFAULT now(),\n  updated_at    TIMESTAMPTZ   NOT NULL DEFAULT now()\n);\n-- B-tree primary key covers WHERE id = $1.\nCREATE INDEX orders_customer_recent_idx ON orders (customer_id, created_at DESC);\nCREATE INDEX orders_open_idx ON orders (status, created_at)\n  WHERE status IN ('pending', 'processing');\n\nCREATE TABLE order_items (\n  order_id    BIGINT  NOT NULL REFERENCES orders(id),\n  product_id  BIGINT  NOT NULL REFERENCES products(id),\n  quantity    INT     NOT NULL,\n  unit_price  NUMERIC(10,2) NOT NULL,\n  PRIMARY KEY (order_id, product_id)\n);\n\nINSERT INTO products (id, name, category) VALUES\n  (1, 'Keyboard', 'Electronics'),\n  (2, 'Hoodie', 'Clothing');\nINSERT INTO orders (id, customer_id, status, total_amount, created_at) VALUES\n  (1001, 42, 'pending', 79.98, now() - interval '2 days'),\n  (1002, 42, 'shipped', 49.99, now() - interval '1 day'),\n  (1003, 7,  'pending', 29.99, now());\nINSERT INTO order_items (order_id, product_id, quantity, unit_price) VALUES\n  (1001, 1, 1, 49.99),\n  (1001, 2, 1, 29.99),\n  (1002, 2, 1, 49.99),\n  (1003, 1, 1, 29.99);\n\nSELECT id, customer_id, status, total_amount, created_at\nFROM orders WHERE id = 1001;\n\nSELECT id, status, total_amount, created_at\nFROM orders\nWHERE customer_id = 42\nORDER BY created_at DESC\nLIMIT 20;\n```\n\nApply and verify:\n\n```bash\ndocker compose exec -T postgres psql -v ON_ERROR_STOP=1 -U postgres -d lab < setup.sql\n# Expect: one row for id=1001; customer 42 returns 1002 then 1001\n```\n\nComplete `olap.sql`:\n\n```sql\n-- olap.sql — ClickHouse analytics schema\nDROP TABLE IF EXISTS fact_orders;\nDROP TABLE IF EXISTS fact_orders_monthly_by_category;\n\nCREATE TABLE fact_orders (\n  event_date    Date,\n  event_time    DateTime,\n  order_id      UInt64,\n  customer_id   UInt64,\n  product_id    UInt32,\n  category_id   LowCardinality(String),\n  status        LowCardinality(String),\n  country_code  LowCardinality(FixedString(2)),\n  quantity      UInt32,\n  unit_price    Decimal(10, 2),\n  revenue       Decimal(12, 2) MATERIALIZED quantity * unit_price\n)\nENGINE = MergeTree\nPARTITION BY toYYYYMM(event_date)\nORDER BY (event_date, category_id, customer_id)\nSETTINGS index_granularity = 8192;\n\nCREATE MATERIALIZED VIEW fact_orders_monthly_by_category\nENGINE = SummingMergeTree\nPARTITION BY toYYYYMM(month)\nORDER BY (month, category_id)\nAS\nSELECT toStartOfMonth(event_date) AS month,\n       category_id,\n       sum(revenue) AS revenue\nFROM   fact_orders\nGROUP  BY month, category_id;\n\nINSERT INTO fact_orders\n  (event_date, event_time, order_id, customer_id, product_id, category_id, status, country_code, quantity, unit_price)\nVALUES\n  (today() - 40, now() - INTERVAL 40 DAY, 1001, 42, 1, 'Electronics', 'shipped', 'DE', 1, 49.99),\n  (today() - 40, now() - INTERVAL 40 DAY, 1001, 42, 2, 'Clothing',    'shipped', 'DE', 1, 29.99),\n  (today() - 10, now() - INTERVAL 10 DAY, 1002, 42, 2, 'Clothing',    'shipped', 'DE', 1, 49.99),\n  (today() - 2,  now() - INTERVAL 2 DAY,  1003, 7,  1, 'Electronics', 'pending', 'US', 1, 29.99);\n\nSELECT toStartOfMonth(event_date) AS month,\n       category_id,\n       sum(revenue) AS revenue\nFROM fact_orders\nGROUP BY month, category_id\nORDER BY month, revenue DESC;\n\nSELECT toDate(event_time) AS day,\n       status,\n       count() AS orders\nFROM fact_orders\nWHERE event_time >= now() - INTERVAL 90 DAY\nGROUP BY day, status\nORDER BY day, status;\n\nSELECT * FROM fact_orders_monthly_by_category FINAL;\n```\n\nApply and verify:\n\n```bash\ndocker compose exec -T clickhouse clickhouse-client --multiquery < olap.sql\n# Expect: monthly Electronics/Clothing revenue rows; daily pending/shipped counts; MV rows present\n```\n\nWhy this layout matches the chapter:\n  - PostgreSQL B-tree indexes win on OLTP: small key-driven reads, predictable latency, locks on the B-tree for transactional status updates.\n  - ClickHouse is column-oriented: `LowCardinality` gives bitmap-style compression; `MergeTree` + monthly partitions prune scans; sort key aids compression and skipping; the `SummingMergeTree` MV keeps the hot monthly aggregate cheap to read.\n  - Two systems means eventual consistency between OLTP and OLAP — never serve the customer-facing order page from ClickHouse."
    },
    {
      "task": "[PostgreSQL] Choose the right secondary indexes for a chat messages table given three different read patterns.",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\n# Start the pre-seeded lab: PostgreSQL 16, database messages_index_lab,\n# table messages already loaded with ~100k rows and no secondary indexes\ndocker run -d --name lab-ch4-p1 -p 5432:5432 ghcr.io/anmisttt/ddia-practice:ch4-p1\n\n# Copy setup.sql (the DDL below plus the full seed INSERT) into the current directory\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch4-p1 init\n\n# Re-apply setup.sql after editing it (it re-creates and re-seeds the table)\ndocker exec -i lab-ch4-p1 psql -v ON_ERROR_STOP=1 -U postgres -d messages_index_lab < setup.sql\n\n# Interactive session for the EXPLAIN queries\ndocker exec -it lab-ch4-p1 psql -U postgres -d messages_index_lab\n\n# Teardown\ndocker rm -f lab-ch4-p1\n```\n\nA chat product stores messages in PostgreSQL. The table is small per row but grows quickly — currently 2 billion rows, ~1 TB on disk in production. This lab uses a local seed (~100k rows) so you can apply indexes and inspect plans.\n\n```sql\n-- setup.sql — messages table with seed data; indexes are stubs (implement at comment sites)\nDROP TABLE IF EXISTS messages CASCADE;\n\nCREATE TABLE messages (\n  id          BIGSERIAL    PRIMARY KEY,\n  channel_id  BIGINT       NOT NULL,\n  author_id   BIGINT       NOT NULL,\n  created_at  TIMESTAMPTZ  NOT NULL DEFAULT now(),\n  body        TEXT         NOT NULL,\n  edited_at   TIMESTAMPTZ,\n  deleted_at  TIMESTAMPTZ\n);\n\n-- Seed enough rows for EXPLAIN to prefer indexes: 3 channels, mixed authors, some soft-deletes.\n-- One rare phrase (~0.1% of rows) keeps Q3 selective so the planner can use the GIN index.\n-- The copied setup.sql holds the seed itself: INSERT INTO messages ... FROM generate_series(1, 100000) AS g;\n-- (elided here — it is already loaded in the lab image, and re-applying setup.sql restores it)\n\nANALYZE messages;\n\n-- Index section: leave empty until Tasks step 2.\n-- implement Q1 covering/partial index\n--   CREATE INDEX ... ON messages (channel_id, created_at DESC)\n--   INCLUDE (id, author_id, body) WHERE deleted_at IS NULL;\n\n-- implement Q2 composite\n--   CREATE INDEX ... ON messages (channel_id, author_id, created_at DESC)\n--   WHERE deleted_at IS NULL;\n\n-- implement Q3 GIN tsvector\n--   CREATE INDEX ... ON messages USING GIN (to_tsvector('english', body))\n--   WHERE deleted_at IS NULL;\n```\n\nHot queries (run under `EXPLAIN (ANALYZE, BUFFERS)` in the `psql` session before and after your indexes):\n\n```sql\n-- Q1 — channel timeline (extremely hot in production, ~100k qps)\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, author_id, created_at, body\nFROM   messages\nWHERE  channel_id = 1\n  AND  deleted_at IS NULL\nORDER  BY created_at DESC\nLIMIT  50;\n\n-- Q2 — author search inside a channel (~500 qps)\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, created_at, body\nFROM   messages\nWHERE  channel_id = 1 AND author_id = 105\n  AND  deleted_at IS NULL\nORDER  BY created_at DESC\nLIMIT  50;\n\n-- Q3 — keyword search across a channel (~200 qps); rare phrase matches ~0.1% of rows\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, author_id, created_at, ts_headline('english', body, q) AS snippet\nFROM   messages, plainto_tsquery('english', 'xenon quarantine') q\nWHERE  channel_id = 1\n  AND  deleted_at IS NULL\n  AND  to_tsvector('english', body) @@ q\nORDER  BY ts_rank(to_tsvector('english', body), q) DESC\nLIMIT  20;\n```\n\nExpected plan signals after indexes:\n  - Q1: uses your timeline index name; prefer `Index Only Scan` (covering INCLUDE) or Index Scan with Index Cond on `channel_id` and `created_at`; no separate Sort for the DESC order.\n  - Q2: uses your composite index name; Index Cond includes `channel_id` and `author_id` (not a filter-only walk of the Q1 index).\n  - Q3: uses your GIN index; look for `Bitmap Index Scan` on the GIN name under a `Bitmap Heap Scan` (selective `xenon quarantine` hits). If a tiny local instance still Seq Scans Q3, re-run that EXPLAIN after `SET enable_seqscan = off;` as a lab check only, then `RESET enable_seqscan;`.\n\nTasks:\n1. Run the three baseline `EXPLAIN (ANALYZE, BUFFERS)` queries above against the pre-seeded table. Without secondary indexes, expect Seq Scan (or a PK-driven path) plus an explicit Sort for Q1/Q2; Q3 typically Seq Scans while evaluating `to_tsvector` / `@@` on every live row.\n2. At the three `-- implement Q1/Q2/Q3` comment sites in `setup.sql`, add the CREATE INDEX statements (covering/partial for Q1, composite for Q2, GIN on `to_tsvector` for Q3). Re-apply the file with `docker exec -i lab-ch4-p1 psql -v ON_ERROR_STOP=1 -U postgres -d messages_index_lab < setup.sql` (it drops, re-creates and re-seeds `messages`, so no rows are lost), then `ANALYZE messages`.\n3. Re-run the three EXPLAIN queries and confirm the expected plan signals (index names / Index Only Scan / GIN Bitmap Index Scan as appropriate). In one sentence per index, state which column ordering (or GIN expression) you picked and why.",
      "answer": "Complete `setup.sql` with all three secondary indexes applied. Q1 uses a covering partial B-tree on the channel timeline; Q2 adds a composite for author-within-channel; Q3 uses a GIN inverted index over `to_tsvector` (the PostgreSQL analogue of a Lucene postings list). The seed keeps the Q3 phrase rare (~0.1% of rows) so EXPLAIN shows a GIN Bitmap Index Scan rather than a Seq Scan.\n\n```sql\n-- setup.sql — messages table with secondary indexes for Q1–Q3\nDROP TABLE IF EXISTS messages CASCADE;\n\nCREATE TABLE messages (\n  id          BIGSERIAL    PRIMARY KEY,\n  channel_id  BIGINT       NOT NULL,\n  author_id   BIGINT       NOT NULL,\n  created_at  TIMESTAMPTZ  NOT NULL DEFAULT now(),\n  body        TEXT         NOT NULL,\n  edited_at   TIMESTAMPTZ,\n  deleted_at  TIMESTAMPTZ\n);\n\nINSERT INTO messages (channel_id, author_id, created_at, body, deleted_at)\nSELECT\n  1 + (g % 3),\n  100 + (g % 10),\n  TIMESTAMPTZ '2024-01-01' + ((g % 5000) || ' minutes')::interval,\n  CASE\n    WHEN g % 1000 = 0 THEN 'xenon quarantine protocol ticket ' || g\n    WHEN g % 5 = 1 THEN 'please reset my password soon'\n    WHEN g % 5 = 2 THEN 'meeting notes and follow-ups'\n    WHEN g % 5 = 3 THEN 'deploy rolled back after timeout'\n    WHEN g % 5 = 4 THEN 'hello from channel chat ' || g\n    ELSE 'shipping delay for order ' || g\n  END,\n  CASE WHEN g % 17 = 0 THEN TIMESTAMPTZ '2024-06-01' ELSE NULL END\nFROM generate_series(1, 100000) AS g;\n\n-- Q1: channel timeline — equality (channel_id), then sort (created_at DESC);\n-- INCLUDE covers the SELECT list for Index Only Scan; partial excludes soft-deletes.\nCREATE INDEX messages_channel_timeline_idx\n  ON messages (channel_id, created_at DESC)\n  INCLUDE (id, author_id, body)\n  WHERE deleted_at IS NULL;\n\n-- Q2: author within a channel — composite leading equality columns, then sort.\nCREATE INDEX messages_channel_author_idx\n  ON messages (channel_id, author_id, created_at DESC)\n  WHERE deleted_at IS NULL;\n\n-- Q3: keyword search — GIN over tsvector (term → postings list).\nCREATE INDEX messages_fts_idx\n  ON messages\n  USING GIN (to_tsvector('english', body))\n  WHERE deleted_at IS NULL;\n\nANALYZE messages;\n```\n\nApply and verify:\n\n```bash\ndocker exec -i lab-ch4-p1 psql -v ON_ERROR_STOP=1 -U postgres -d messages_index_lab < setup.sql\n\ndocker exec -i lab-ch4-p1 psql -U postgres -d messages_index_lab -c \"\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, author_id, created_at, body\nFROM   messages\nWHERE  channel_id = 1\n  AND  deleted_at IS NULL\nORDER  BY created_at DESC\nLIMIT  50;\n\"\n\ndocker exec -i lab-ch4-p1 psql -U postgres -d messages_index_lab -c \"\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, created_at, body\nFROM   messages\nWHERE  channel_id = 1 AND author_id = 105\n  AND  deleted_at IS NULL\nORDER  BY created_at DESC\nLIMIT  50;\n\"\n\ndocker exec -i lab-ch4-p1 psql -U postgres -d messages_index_lab -c \"\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, author_id, created_at, ts_headline('english', body, q) AS snippet\nFROM   messages, plainto_tsquery('english', 'xenon quarantine') q\nWHERE  channel_id = 1\n  AND  deleted_at IS NULL\n  AND  to_tsvector('english', body) @@ q\nORDER  BY ts_rank(to_tsvector('english', body), q) DESC\nLIMIT  20;\n\"\n```\n\nOptional lab check if Q3 still Seq Scans on a very small instance:\n\n```sql\nSET enable_seqscan = off;\n-- re-run the Q3 EXPLAIN above; expect Bitmap Index Scan on messages_fts_idx\nRESET enable_seqscan;\n```\n\nExpected signals: Q1 shows Index Only Scan (or Index Scan) on `messages_channel_timeline_idx` with no Sort; Q2 uses `messages_channel_author_idx` with Index Cond on `channel_id` and `author_id`; Q3 shows Bitmap Heap Scan with Bitmap Index Scan on `messages_fts_idx` for the selective `xenon quarantine` query (Sort remains for `ts_rank`).\n\nWhy each shape:\n  1. Q1 leads on equality (`channel_id`) then range/sort (`created_at DESC`). INCLUDE (id, author_id, body) makes the index cover the SELECT list so Postgres can answer from the index alone. The partial `WHERE deleted_at IS NULL` keeps deleted rows out of the hot index.\n  2. Q2 needs `(channel_id, author_id, created_at DESC)` so both equalities seek and the ORDER BY rides the index; the Q1 index alone would filter `author_id` row by row.\n  3. Q3 is full-text search. GIN on `to_tsvector('english', body)` is the chapter's inverted index (term → postings list). A rare query phrase is required so the planner prefers the GIN bitmap path; a common phrase that hits ~20% of rows often falls back to Seq Scan.\n\nWrite-cost note: each INSERT maintains PK + three secondary indexes; GIN is the heaviest because it tokenizes `body`. For a read-heavy timeline that is the right trade-off — 'well-chosen indexes speed up read queries, but every index slows down writes.' For multi-TB tables, partition by `channel_id` (hash) or by month (range) so each B-tree stays manageable.\n"
    },
    {
      "task": "[PostgreSQL] A user-profile API is slow. Diagnose the secondary-index bug in the schema below and provide the fix.",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\n# Start the pre-seeded lab: PostgreSQL 16, database index_shape_lab, table users with\n# 50k rows and the wrongly ordered index users_signup_country_idx already in place\ndocker run -d --name lab-ch4-p2 -p 5432:5432 ghcr.io/anmisttt/ddia-practice:ch4-p2\n\n# Copy setup.sql (the DDL below plus the full seed INSERT) into the current directory\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch4-p2 init\n\n# Re-apply setup.sql after editing it (it re-creates and re-seeds the table)\ndocker exec -i lab-ch4-p2 psql -v ON_ERROR_STOP=1 -U postgres -d index_shape_lab < setup.sql\n\n# Interactive session for the EXPLAIN query\ndocker exec -it lab-ch4-p2 psql -U postgres -d index_shape_lab\n\n# Teardown\ndocker rm -f lab-ch4-p2\n```\n\nA user-profile API is slow on a country + signup-date filter. You will inspect the plan for a wrongly ordered composite index on the pre-seeded table, then fix the index shape.\n\n```sql\n-- setup.sql — users table with a wrongly ordered composite index\nDROP TABLE IF EXISTS users CASCADE;\n\nCREATE TABLE users (\n  id           BIGSERIAL    PRIMARY KEY,\n  email        TEXT         UNIQUE NOT NULL,\n  country      TEXT         NOT NULL,\n  signup_date  DATE         NOT NULL,\n  last_login   TIMESTAMPTZ\n);\n\n-- Seed 50k rows: 5 countries, dates across 2023–2024 (deterministic).\n-- The copied setup.sql holds the seed itself: INSERT INTO users ... FROM generate_series(1, 50000) AS g;\n-- (elided here — it is already loaded in the lab image, and re-applying setup.sql restores it)\n\n-- Wrong column order for the hot query (equality on country, range on signup_date).\n-- fix: equality column first, then range/sort; consider INCLUDE for covering\nCREATE INDEX users_signup_country_idx ON users (signup_date, country);\n\nANALYZE users;\n```\n\nHot query (run in the `psql` session before and after your fix):\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, email, last_login\nFROM   users\nWHERE  country = 'DE'\n  AND  signup_date BETWEEN '2024-01-01' AND '2024-12-31'\nORDER  BY signup_date DESC\nLIMIT  100;\n```\n\nExpected signals:\n  - Before: plan uses `users_signup_country_idx` (often `Index Scan Backward`), with Index Cond driven by the `signup_date` range first; country is not the leading seek key.\n  - After: plan uses an index whose first Index Cond is `country = 'DE'`, then the date range; `ORDER BY signup_date DESC` needs no separate Sort node; with a covering INCLUDE you should see `Index Only Scan` (or Index Scan that does not re-filter country after a wide date walk).\n\nTasks:\n1. Explain why `(signup_date, country)` is the wrong shape for this query (equality vs range/sort column order).\n2. In `setup.sql`, replace the wrong index with a corrected `CREATE INDEX` (drop the old one first). Put equality (`country`) first, then range/sort (`signup_date DESC`), and `INCLUDE (id, email, last_login)` so the SELECT list is covered.\n3. Re-apply the index change — either run the two DDL statements in the `psql` session, or re-apply the whole file with `docker exec -i lab-ch4-p2 psql -v ON_ERROR_STOP=1 -U postgres -d index_shape_lab < setup.sql` (it drops, re-creates and re-seeds `users`, so no rows are lost). Re-run the `EXPLAIN (ANALYZE, BUFFERS)` query and confirm the improved plan matches the expected signals above.",
      "answer": "Problem: the existing index is `(signup_date, country)`. The hot query filters with equality on `country` and a range on `signup_date`, then sorts by `signup_date DESC`. With date leading, the planner walks the date range first instead of seeking to `country = 'DE'` — the phone-book analogy from the chapter (an index ordered by last-name, first-name cannot efficiently start from a given first name).\n\nFix — complete `setup.sql` with the corrected index:\n\n```sql\n-- setup.sql — users table with corrected composite + covering index\nDROP TABLE IF EXISTS users CASCADE;\n\nCREATE TABLE users (\n  id           BIGSERIAL    PRIMARY KEY,\n  email        TEXT         UNIQUE NOT NULL,\n  country      TEXT         NOT NULL,\n  signup_date  DATE         NOT NULL,\n  last_login   TIMESTAMPTZ\n);\n\nINSERT INTO users (email, country, signup_date, last_login)\nSELECT\n  'user' || g || '@example.com',\n  (ARRAY['US', 'DE', 'FR', 'GB', 'IN'])[1 + (g % 5)],\n  DATE '2023-01-01' + ((g % 730) || ' days')::interval,\n  TIMESTAMPTZ '2024-06-01' + ((g % 100) || ' hours')::interval\nFROM generate_series(1, 50000) AS g;\n\n-- Corrected: equality (country), then range/sort (signup_date DESC), INCLUDE for covering.\nDROP INDEX IF EXISTS users_signup_country_idx;\n\nCREATE INDEX users_country_signup_idx\n  ON users (country, signup_date DESC)\n  INCLUDE (id, email, last_login);\n\nANALYZE users;\n```\n\nIf you already loaded the original setup, apply only the index swap:\n\n```sql\nDROP INDEX IF EXISTS users_signup_country_idx;\n\nCREATE INDEX users_country_signup_idx\n  ON users (country, signup_date DESC)\n  INCLUDE (id, email, last_login);\n\nANALYZE users;\n```\n\nWhat changes:\n  - Leading column is the equality predicate (`country`), so the planner seeks to `DE` and walks only that country's date slice.\n  - Second column is the range/sort column (`signup_date DESC`), so `ORDER BY signup_date DESC` is satisfied by index order (no separate Sort).\n  - `INCLUDE (id, email, last_login)` covers the SELECT list for an Index Only Scan when the visibility map allows.\n\nVerification:\n\n```bash\ndocker exec -i lab-ch4-p2 psql -v ON_ERROR_STOP=1 -U postgres -d index_shape_lab < setup.sql\n\ndocker exec -i lab-ch4-p2 psql -U postgres -d index_shape_lab -c \"\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, email, last_login\nFROM   users\nWHERE  country = 'DE'\n  AND  signup_date BETWEEN '2024-01-01' AND '2024-12-31'\nORDER  BY signup_date DESC\nLIMIT  100;\n\"\n```\n\nBefore: Index Scan Backward on `users_signup_country_idx` (date range leads). After: Index Only Scan (or Index Scan) on `users_country_signup_idx` with Index Cond starting at `country = 'DE'`, no Sort node.\n\nOne-line takeaway: composite indexes go equality first, range/sort last; use INCLUDE to cover hot SELECT lists."
    },
    {
      "task": "[MySQL / InnoDB] Reproduce UUIDv4 clustered-index page splits on an orders table, migrate to a monotonic primary key online, and re-verify bloat and insert latency.",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\n# Copy the lab files into the current directory: docker-compose.yml, setup.sql, seed.sql,\n# ch4_orders_insert_load.py, ch4_orders_diag.sql\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch4-p3 init\n\n# Start MySQL 8.0 (root / password lab, database ch4_lab, port 3306) plus the python lab\n# container; seed.sql runs at first start, so ch4_lab.orders already exists (empty)\ndocker compose up -d\n\n# Install Percona Toolkit inside the lab container for the online schema change\ndocker compose exec lab bash -lc \"apt-get update -qq && apt-get install -y -qq percona-toolkit\"\ndocker compose exec lab pt-online-schema-change --version\n\n# Apply a .sql file (setup.sql re-creates ch4_lab.orders from scratch)\ndocker compose exec -T mysql mysql -uroot -plab < setup.sql\n\n# Run the insert harness inside the lab container (it reaches MySQL at host \"mysql\")\ndocker compose exec lab python3 ch4_orders_insert_load.py\n\n# Run the diagnostics file\ndocker compose exec -T mysql mysql -uroot -plab < ch4_orders_diag.sql\n\n# Interactive MySQL session\ndocker compose exec mysql mysql -uroot -plab ch4_lab\n\n# Teardown\ndocker compose down -v\n```\n\nAn e-commerce backend stores orders in MySQL/InnoDB. The team picked UUIDv4 as the primary key three months ago to make IDs unguessable and to avoid coordinating across services. Reproduce the page-split and table-bloat symptoms under insert load, migrate to a monotonic clustered primary key with no downtime, then re-check the same signals.\n\nThe `mysql` service is MySQL 8.0 with InnoDB; the `lab` service is Python 3.12 with `mysql-connector-python` already installed, mounts your working directory at `/work`, and gets `pt-online-schema-change` from the Percona Toolkit install command above (`gh-ost` is an acceptable alternative if you already run it elsewhere). Python 3.12's stdlib has no `uuid.uuid7()`, so implement a small RFC 9562 UUIDv7 helper yourself with `time` + `os.urandom` — do not call `uuid.uuid7()`. All commands below run against the compose stack; nothing is installed on your host.\n\n`init` copies the schema stub as `setup.sql`:\n\n```sql\n-- setup.sql — orders table with a random UUIDv4 clustered primary key\nCREATE DATABASE IF NOT EXISTS ch4_lab;\nUSE ch4_lab;\n\nDROP TABLE IF EXISTS orders;\n\nCREATE TABLE orders (\n  -- TODO: replace random UUIDv4 clustered PK with a monotonic key\n  --       (UUIDv7 / ULID / BIGINT AUTO_INCREMENT); dual-write the new id first\n  id            BINARY(16)    NOT NULL PRIMARY KEY,   -- stores a UUIDv4 (random leaf inserts)\n  customer_id   BIGINT        NOT NULL,\n  status        VARCHAR(32)   NOT NULL,\n  total_amount  DECIMAL(10,2) NOT NULL,\n  created_at    DATETIME(6)   NOT NULL,\n  notes         VARCHAR(500)  NULL\n) ENGINE = InnoDB;\n\nCREATE INDEX orders_customer_recent_idx ON orders (customer_id, created_at);\nCREATE INDEX orders_status_idx ON orders (status);\n```\n\n`init` also copies the insert harness as `ch4_orders_insert_load.py`. It inserts under load so you can watch page splits and table size; run it with `docker compose exec lab python3 ch4_orders_insert_load.py`:\n\n```python\n# ch4_orders_insert_load.py\nimport time\nimport uuid\nfrom datetime import datetime, timezone\n\nimport mysql.connector\n\nBATCH = 500\nTOTAL = 20_000  # raise toward 100_000+ for clearer bloat; keep modest on a laptop\n\nconn = mysql.connector.connect(\n    host=\"mysql\", user=\"root\", password=\"lab\", database=\"ch4_lab\"\n)\ncur = conn.cursor()\n\nsql = (\n    \"INSERT INTO orders (id, customer_id, status, total_amount, created_at) \"\n    \"VALUES (%s, %s, %s, %s, %s)\"\n)\n\nt0 = time.perf_counter()\nfor i in range(TOTAL):\n    # TODO: dual-write a monotonic id here via a hand-rolled UUIDv7 helper\n    #       (time + os.urandom; do NOT call uuid.uuid7() — not in the 3.12 stdlib);\n    #       after migration, insert with the monotonic column as the clustered PK\n    order_id = uuid.uuid4().bytes\n    cur.execute(\n        sql,\n        (order_id, i % 10_000, \"pending\", 19.99, datetime.now(timezone.utc)),\n    )\n    if (i + 1) % BATCH == 0:\n        conn.commit()\n        print(f\"inserted {i + 1}/{TOTAL}\")\nconn.commit()\nelapsed = time.perf_counter() - t0\nprint(f\"done: {TOTAL} rows in {elapsed:.2f}s ({TOTAL / elapsed:.0f} rows/s)\")\ncur.close()\nconn.close()\n```\n\n`init` also copies the diagnostic queries as `ch4_orders_diag.sql`; run them before and after the fix with `docker compose exec -T mysql mysql -uroot -plab < ch4_orders_diag.sql`. Treat `data_mb` / `free_mb` and harness rows/s as the main before/after signals; page-split counters are optional and only meaningful after you enable them. On a laptop, `data_free` / `information_schema` size can be coarse — tablespace file size, insert rows/s, and enabled InnoDB page-split counters are also acceptable verify signals:\n\n```sql\n-- ch4_orders_diag.sql\nUSE ch4_lab;\n\n-- Primary signal: table size vs data — large free_mb / gap vs expected row bytes => bloat\nSELECT table_name, engine,\n       ROUND(data_length / 1024 / 1024, 2) AS data_mb,\n       ROUND(index_length / 1024 / 1024, 2) AS index_mb,\n       ROUND(data_free / 1024 / 1024, 2) AS free_mb,\n       table_rows\nFROM information_schema.tables\nWHERE table_schema = 'ch4_lab' AND table_name = 'orders';\n\n-- Optional page-split counters (often disabled until you turn the monitors on)\nSET GLOBAL innodb_monitor_enable = 'module_index';\nSELECT name, count\nFROM information_schema.innodb_metrics\nWHERE name IN ('index_page_splits', 'index_page_merge_attempts')\nORDER BY name;\n\nSHOW ENGINE INNODB STATUS\\G\n-- Look under FILE I/O / buffer pool sections and compare insert latency from the harness printout.\n```\n\nProduction-scale symptoms (for context; your laptop run will be smaller but the same shape): ~3× table bloat vs raw data, thousands of page splits/min, p99 insert latency drifting from ~2 ms to ~35 ms, buffer-pool hit rate falling, secondary indexes larger than expected.\n\nTasks:\n1. Apply `setup.sql`, run `ch4_orders_insert_load.py`, then run `ch4_orders_diag.sql`. Record `data_mb` / `free_mb` (and optionally page-split counters after enabling the monitors), plus the harness rows/s. In a short paragraph, explain the root cause: how InnoDB's clustered primary index plus random UUIDv4 keys produce page splits, bloat, and the other symptoms.\n2. Change the schema and insert path to dual-write a monotonic id (UUIDv7 recommended; ULID or BIGINT AUTO_INCREMENT also fine). Put the implement-here comments in the SQL PK line and the Python insert site into practice: add the new column + unique key, fill it on every insert with your own `time` + `os.urandom` UUIDv7 helper, and backfill existing rows in batches.\n3. Complete a no-downtime PK swap to the monotonic column with `pt-online-schema-change` (run it as `docker compose exec lab pt-online-schema-change ... h=mysql,u=root,p=lab,D=ch4_lab,t=orders`), or an equivalent online DDL tool. Prefer two OSC steps: (a) drop the old PK / make `order_id_v7` the PK and drop the old `id` (and the now-redundant unique key), then (b) rename `order_id_v7` → `id`. Dry-run with `--print` before `--execute`. Ship the final `CREATE TABLE orders` (with secondary indexes) as it should look after migration.\n4. Truncate or recreate with the fixed schema, re-run the same insert harness against the monotonic PK, re-run the same diagnostics, and confirm free space / insert throughput improved versus step 1 (page-split counters only if you enabled them; tablespace file size is fine if `data_free` looks noisy).",
      "answer": "Root cause: InnoDB stores every table as a B+ tree keyed by the primary key — the PK is the clustered index ('In MySQL's InnoDB storage engine, the primary key of a table is always a clustered index, and secondary indexes refer to the primary key.'). UUIDv4 is uniformly random, so each insert lands on a random leaf. When that leaf is full, InnoDB splits it into two half-full pages and updates the parent — write amplification, fragmentation ('B-tree storage engines leave some disk space unused due to fragmentation'), buffer-pool thrashing, and fatter secondary indexes (each entry stores the 16-byte PK). A monotonic PK appends at the right edge of the tree instead.\n\nFinal schema after migration (monotonic UUIDv7 clustered PK; keep the same secondary indexes; no leftover `uq_order_id_v7` because that column is now the PRIMARY KEY named `id`):\n\n```sql\n-- ch4_orders_final.sql — post-migration artifact\nCREATE DATABASE IF NOT EXISTS ch4_lab;\nUSE ch4_lab;\n\nDROP TABLE IF EXISTS orders;\n\nCREATE TABLE orders (\n  id            BINARY(16)    NOT NULL PRIMARY KEY,  -- UUIDv7 (time-ordered), clustered\n  customer_id   BIGINT        NOT NULL,\n  status        VARCHAR(32)   NOT NULL,\n  total_amount  DECIMAL(10,2) NOT NULL,\n  created_at    DATETIME(6)   NOT NULL,\n  notes         VARCHAR(500)  NULL\n) ENGINE = InnoDB;\n\nCREATE INDEX orders_customer_recent_idx ON orders (customer_id, created_at);\nCREATE INDEX orders_status_idx ON orders (status);\n```\n\nApply it and re-run the harness against the monotonic PK:\n\n```bash\ndocker compose exec -T mysql mysql -uroot -plab < ch4_orders_final.sql\ndocker compose exec lab python3 ch4_orders_insert_load.py\n```\n\nUUIDv7 helper and fixed insert harness (after the PK swap; inserts only the monotonic `id`):\n\n```python\n# ch4_orders_insert_load.py — reference solution (monotonic UUIDv7 inserts)\nimport os\nimport time\nimport uuid\nfrom datetime import datetime, timezone\n\nimport mysql.connector\n\n\ndef uuid7_bytes() -> bytes:\n    \"\"\"RFC 9562 UUIDv7: 48-bit unix-ms timestamp in the high bits + random.\"\"\"\n    ts_ms = int(time.time() * 1000) & ((1 << 48) - 1)\n    rand_a = int.from_bytes(os.urandom(2), \"big\") & 0x0FFF\n    rand_b = int.from_bytes(os.urandom(8), \"big\") & ((1 << 62) - 1)\n    uuid_int = (ts_ms << 80) | (0x7 << 76) | (rand_a << 64) | (0b10 << 62) | rand_b\n    return uuid.UUID(int=uuid_int).bytes\n\n\nBATCH = 500\nTOTAL = 20_000\n\nconn = mysql.connector.connect(\n    host=\"mysql\", user=\"root\", password=\"lab\", database=\"ch4_lab\"\n)\ncur = conn.cursor()\n\nsql = (\n    \"INSERT INTO orders (id, customer_id, status, total_amount, created_at) \"\n    \"VALUES (%s, %s, %s, %s, %s)\"\n)\n\nt0 = time.perf_counter()\nfor i in range(TOTAL):\n    order_id = uuid7_bytes()  # monotonic clustered key — not uuid.uuid4()\n    cur.execute(\n        sql,\n        (order_id, i % 10_000, \"pending\", 19.99, datetime.now(timezone.utc)),\n    )\n    if (i + 1) % BATCH == 0:\n        conn.commit()\n        print(f\"inserted {i + 1}/{TOTAL}\")\nconn.commit()\nelapsed = time.perf_counter() - t0\nprint(f\"done: {TOTAL} rows in {elapsed:.2f}s ({TOTAL / elapsed:.0f} rows/s)\")\ncur.close()\nconn.close()\n```\n\nDual-write insert during migration (while both `id` and `order_id_v7` exist):\n\n```python\n# ch4_orders_insert_load_dualwrite.py — dual-write harness (legacy UUIDv4 PK + order_id_v7)\nimport os\nimport time\nimport uuid\nfrom datetime import datetime, timezone\n\nimport mysql.connector\n\n\ndef uuid7_bytes() -> bytes:\n    \"\"\"RFC 9562 UUIDv7: 48-bit unix-ms timestamp in the high bits + random.\"\"\"\n    ts_ms = int(time.time() * 1000) & ((1 << 48) - 1)\n    rand_a = int.from_bytes(os.urandom(2), \"big\") & 0x0FFF\n    rand_b = int.from_bytes(os.urandom(8), \"big\") & ((1 << 62) - 1)\n    uuid_int = (ts_ms << 80) | (0x7 << 76) | (rand_a << 64) | (0b10 << 62) | rand_b\n    return uuid.UUID(int=uuid_int).bytes\n\n\nBATCH = 500\nTOTAL = 20_000\n\nconn = mysql.connector.connect(\n    host=\"mysql\", user=\"root\", password=\"lab\", database=\"ch4_lab\"\n)\ncur = conn.cursor()\n\nsql = (\n    \"INSERT INTO orders (id, order_id_v7, customer_id, status, total_amount, created_at) \"\n    \"VALUES (%s, %s, %s, %s, %s, %s)\"\n)\n\nt0 = time.perf_counter()\nfor i in range(TOTAL):\n    cur.execute(\n        sql,\n        (\n            uuid.uuid4().bytes,  # legacy random clustered PK during dual-write\n            uuid7_bytes(),       # new monotonic column\n            i % 10_000,\n            \"pending\",\n            19.99,\n            datetime.now(timezone.utc),\n        ),\n    )\n    if (i + 1) % BATCH == 0:\n        conn.commit()\n        print(f\"inserted {i + 1}/{TOTAL}\")\nconn.commit()\nelapsed = time.perf_counter() - t0\nprint(f\"done: {TOTAL} rows in {elapsed:.2f}s ({TOTAL / elapsed:.0f} rows/s)\")\ncur.close()\nconn.close()\n```\n\nFull online migration SQL (dual-write column → backfill → two-step online PK swap → rename). Run these steps in order in an interactive session (`docker compose exec mysql mysql -uroot -plab ch4_lab`), repeating the backfill UPDATE until it reports 0 rows changed before the NOT NULL step; run `ch4_orders_diag.sql` before and after. Prefer `data_mb` / `free_mb` and harness rows/s; enable InnoDB index monitors only if you want optional page-split counters.\n\n```sql\n-- ch4_orders_migrate.sql\nUSE ch4_lab;\n\n-- 1) Dual-write column (app must also write order_id_v7 on every INSERT)\nALTER TABLE orders\n  ADD COLUMN order_id_v7 BINARY(16) NULL AFTER id,\n  ADD UNIQUE KEY uq_order_id_v7 (order_id_v7);\n\n-- 2) Backfill existing rows in batches (repeat until 0 rows updated).\n--    Prefer app-generated UUIDv7; UUID_TO_BIN(UUID(), 1) is a MySQL-side\n--    time-ordered stand-in when you cannot touch old clients yet.\nUPDATE orders\nSET order_id_v7 = UUID_TO_BIN(UUID(), 1)\nWHERE order_id_v7 IS NULL\nLIMIT 1000;\n\n-- After backfill completes:\nALTER TABLE orders\n  MODIFY COLUMN order_id_v7 BINARY(16) NOT NULL;\n```\n\n```bash\n# 3a) Online PK swap: drop old PK, make order_id_v7 the PK, drop legacy id,\n#     and DROP KEY uq_order_id_v7 (redundant once order_id_v7 is PRIMARY KEY).\n#     --no-check-alter is required because the alter includes DROP PRIMARY KEY.\n#     Always --dry-run --print first; only then --execute.\ndocker compose exec lab pt-online-schema-change \\\n  --alter \"DROP PRIMARY KEY, ADD PRIMARY KEY (order_id_v7), DROP COLUMN id, DROP KEY uq_order_id_v7\" \\\n  h=mysql,u=root,p=lab,D=ch4_lab,t=orders \\\n  --no-check-alter \\\n  --dry-run --print\n\ndocker compose exec lab pt-online-schema-change \\\n  --alter \"DROP PRIMARY KEY, ADD PRIMARY KEY (order_id_v7), DROP COLUMN id, DROP KEY uq_order_id_v7\" \\\n  h=mysql,u=root,p=lab,D=ch4_lab,t=orders \\\n  --no-check-alter \\\n  --execute\n\n# 3b) Separate rename so DROP COLUMN id and CHANGE rename are not combined\n#     (avoids pt-osc / MySQL footguns around DROP + CHANGE in one alter).\ndocker compose exec lab pt-online-schema-change \\\n  --alter \"CHANGE COLUMN order_id_v7 id BINARY(16) NOT NULL\" \\\n  h=mysql,u=root,p=lab,D=ch4_lab,t=orders \\\n  --dry-run --print\n\ndocker compose exec lab pt-online-schema-change \\\n  --alter \"CHANGE COLUMN order_id_v7 id BINARY(16) NOT NULL\" \\\n  h=mysql,u=root,p=lab,D=ch4_lab,t=orders \\\n  --execute\n\n# Equivalent two-step idea with gh-ost (also dry-run / cut-over carefully):\n# gh-ost --database=ch4_lab --table=orders \\\n#   --alter=\"DROP PRIMARY KEY, ADD PRIMARY KEY (order_id_v7), DROP COLUMN id, DROP KEY uq_order_id_v7\" \\\n#   --allow-on-master --exact-rowcount --initially-drop-ghost-table \\\n#   --execute\n# gh-ost --database=ch4_lab --table=orders \\\n#   --alter=\"CHANGE COLUMN order_id_v7 id BINARY(16) NOT NULL\" \\\n#   --allow-on-master --exact-rowcount --initially-drop-ghost-table \\\n#   --execute\n```\n\nIf you must use a blocking ALTER on a lab-sized table (not for production 2 TB). PK changes typically need an exclusive lock / full copy — do not rely on LOCK=SHARED:\n\n```sql\n-- Lab-only fallback (locks / copies); prefer pt-online-schema-change above in prod\nUSE ch4_lab;\n\n-- Step A: PK swap + drop legacy id + drop redundant unique key\nALTER TABLE orders\n  DROP PRIMARY KEY,\n  ADD PRIMARY KEY (order_id_v7),\n  DROP COLUMN id,\n  DROP KEY uq_order_id_v7,\n  ALGORITHM=COPY,\n  LOCK=EXCLUSIVE;\n\n-- Step B: rename monotonic column to id (matches ch4_orders_final.sql)\nALTER TABLE orders\n  CHANGE COLUMN order_id_v7 id BINARY(16) NOT NULL,\n  ALGORITHM=INPLACE,\n  LOCK=NONE;\n```\n\nVerify with `docker compose exec -T mysql mysql -uroot -plab < ch4_orders_diag.sql` (same signals as task 1 — `free_mb` / rows/s first; page-split counters only after enabling monitors):\n\n```sql\nUSE ch4_lab;\n\nSELECT table_name,\n       ROUND(data_length / 1024 / 1024, 2) AS data_mb,\n       ROUND(index_length / 1024 / 1024, 2) AS index_mb,\n       ROUND(data_free / 1024 / 1024, 2) AS free_mb,\n       table_rows\nFROM information_schema.tables\nWHERE table_schema = 'ch4_lab' AND table_name = 'orders';\n\n-- Optional:\nSET GLOBAL innodb_monitor_enable = 'module_index';\nSELECT name, count\nFROM information_schema.innodb_metrics\nWHERE name IN ('index_page_splits', 'index_page_merge_attempts')\nORDER BY name;\n```\n\nExpected after recreate + UUIDv7 insert load: `free_mb` near zero relative to the random-UUID run, and higher rows/s from the harness. If you enabled the index monitors, page-split counter growth should also be much lower for the same row count. Secondary indexes stay correct because the clustered key remains 16 bytes but PK leaf pages pack densely.\n\nTradeoffs: online DDL still does a full table copy (hours on multi-TB tables — schedule off-peak, watch replica lag). UUIDv7 leaks millisecond-resolution insert time; use Snowflake/ULID with extra entropy if that is a privacy concern. Propagate the new id to every FK before the swap.\n\nAlternatives that also fix the clustered-index bug: BIGINT AUTO_INCREMENT (or Snowflake) as the PK with the external UUID kept as a UNIQUE secondary; or a hidden AUTO_INCREMENT clustered key with UUIDv4 only as a unique secondary (every logical PK lookup becomes two B-tree walks).\n"
    },
    {
      "task": "[ClickHouse] Build a laptop-scale clickstream MergeTree lab: LowCardinality columns, monthly partitions, sort key, optional bloom index / SummingMergeTree MV, then run DAU and campaign queries.",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\n# Copy the lab files into the current directory: docker-compose.yml and setup.sql\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch4-p4 init\n\n# Start ClickHouse 24.8 (native port 9000, HTTP 8123); it starts empty\ndocker compose up -d\n\n# Apply setup.sql (creates the table and inserts the 200k-row seed)\ndocker compose exec -T clickhouse clickhouse-client --multiquery < setup.sql\n\n# Interactive session for Q1/Q2\ndocker compose exec clickhouse clickhouse-client\n\n# Teardown\ndocker compose down -v\n```\n\nA product analytics service ingests web events. Every event has many columns; analysts typically only touch 4–6 of them in any one query. Complete the ClickHouse stub so Q1/Q2 run with column pruning, LowCardinality bitmap-style compression, monthly partition pruning, and a useful sort key. Scale is laptop-friendly (~2e5 rows), not 1B.\n\n`init` copies the stub below as `setup.sql`. It applies as given (incomplete DDL still creates a table and loads the seed); complete every `-- choose ...` / `-- implement ...` site in the Tasks before re-applying for verification. Because the file starts with `DROP TABLE IF EXISTS` and ends with the seed `INSERT`, re-applying it always rebuilds the full 200k rows. Q1/Q2 at the bottom are the demo checks.\n\n```sql\n-- setup.sql — clickstream analytics stub\nDROP TABLE IF EXISTS events;\nDROP TABLE IF EXISTS events_daily_country_device;\n\nCREATE TABLE events (\n  event_time       DateTime,\n  event_date       Date DEFAULT toDate(event_time),\n  user_id          UInt64,\n  session_id       UUID,\n  country          String,            -- choose LowCardinality(FixedString(2))\n  device_type      String,            -- choose LowCardinality(String)\n  os               String,            -- choose LowCardinality(String)\n  browser          String,            -- choose LowCardinality(String)\n  referrer         String,\n  utm_source       String,            -- choose LowCardinality(String)\n  utm_campaign     String,            -- choose LowCardinality(String)\n  page_path        String,\n  page_load_ms     UInt32\n)\nENGINE = MergeTree\nPARTITION BY tuple()                  -- choose PARTITION BY toYYYYMM(event_date)\nORDER BY tuple()                      -- choose ORDER BY (event_date, country, device_type, user_id)\nSETTINGS index_granularity = 8192;\n\n-- optional: bloom_filter skip index on utm_campaign for Q2\n-- ALTER TABLE events ADD INDEX idx_utm_campaign utm_campaign TYPE bloom_filter GRANULARITY 4;\n\n-- implement daily-by-country/device MV with SummingMergeTree (optional data-cube)\n-- CREATE MATERIALIZED VIEW events_daily_country_device\n-- ENGINE = SummingMergeTree\n-- PARTITION BY ...\n-- ORDER BY ...\n-- AS SELECT ... FROM events GROUP BY ...;\n\n-- Laptop-friendly seed (~200k rows across ~90 days). Production would be ~1B.\nINSERT INTO events\n  (event_time, user_id, session_id, country, device_type, os, browser,\n   referrer, utm_source, utm_campaign, page_path, page_load_ms)\nSELECT\n  now() - toIntervalDay(number % 90) - toIntervalSecond(number % 86400),\n  number % 50000,\n  generateUUIDv4(),\n  ['DE','US','GB','FR','BR','IN','JP','CA','AU','NL'][1 + (number % 10)],\n  ['desktop','mobile','tablet'][1 + (number % 3)],\n  ['macOS','Windows','iOS','Android','Linux'][1 + (number % 5)],\n  ['Chrome','Safari','Firefox','Edge'][1 + (number % 4)],\n  if(number % 7 = 0, 'https://news.example/', ''),\n  ['google','newsletter','direct','partner'][1 + (number % 4)],\n  if(number % 11 = 0, 'spring_sale_2026',\n     ['winter_push','brand_always','retarget'][1 + (number % 3)]),\n  concat('/page/', toString(number % 200)),\n  50 + (number % 950)\nFROM numbers(200000);\n\n-- Q1 — DAU per country, last 30 days, desktop + mobile only\n-- SELECT toDate(event_time) AS day, country, uniq(user_id) AS dau\n-- FROM   events\n-- WHERE  event_time >= now() - INTERVAL 30 DAY\n--   AND  country IN ('DE','US','GB','FR','BR','IN','JP')\n--   AND  device_type IN ('desktop','mobile')\n-- GROUP  BY day, country\n-- ORDER  BY day, dau DESC\n-- LIMIT  20;\n\n-- Q2 — Browser distribution for a single campaign\n-- SELECT browser, count() AS hits\n-- FROM   events\n-- WHERE  utm_campaign = 'spring_sale_2026'\n--   AND  event_time >= now() - INTERVAL 90 DAY\n-- GROUP  BY browser\n-- ORDER  BY hits DESC\n-- LIMIT  10;\n```\n\nTasks:\n1. Edit `setup.sql`: switch the low-cardinality columns, set `PARTITION BY toYYYYMM(event_date)` and `ORDER BY (event_date, country, device_type, user_id)`, and optionally add the bloom_filter index plus the `events_daily_country_device` SummingMergeTree MV. Keep `MergeTree` as the base engine.\n2. Apply it with `docker compose exec -T clickhouse clickhouse-client --multiquery < setup.sql`. Success: no errors; `SELECT count() FROM events` returns `200000`.\n3. Uncomment and run Q1 and Q2 in the `clickhouse-client` session. Success: Q1 returns rows with `day` / `country` / `dau` (desktop+mobile countries in the IN-list); Q2 returns browser rows with positive `hits` for `spring_sale_2026`. Optionally `SELECT * FROM events_daily_country_device FINAL LIMIT 10` shows pre-aggregated daily rows if you built the MV.\n4. In one sentence each, map LowCardinality, `PARTITION BY`, and `ORDER BY` to the chapter optimizations (bitmap-style encoding, partition pruning, sort-key compression / skipping).",
      "answer": "Complete `setup.sql` with LowCardinality columns, monthly partitions, a sort key that clusters the Q1 filters, an optional bloom_filter on `utm_campaign`, and an optional SummingMergeTree daily cube. Column-oriented MergeTree only reads the columns each query touches; LowCardinality is the practical form of the chapter's bitmap encoding; partitions prune the 30-day window; the sort key packs repeated dates/countries for compression and granule skipping.\n\n```sql\n-- setup.sql — completed clickstream analytics schema\nDROP TABLE IF EXISTS events;\nDROP TABLE IF EXISTS events_daily_country_device;\n\nCREATE TABLE events (\n  event_time       DateTime CODEC(DoubleDelta, ZSTD(3)),\n  event_date       Date DEFAULT toDate(event_time),\n  user_id          UInt64,\n  session_id       UUID,\n  country          LowCardinality(FixedString(2)),\n  device_type      LowCardinality(String),\n  os               LowCardinality(String),\n  browser          LowCardinality(String),\n  referrer         String,\n  utm_source       LowCardinality(String),\n  utm_campaign     LowCardinality(String),\n  page_path        String,\n  page_load_ms     UInt32\n)\nENGINE = MergeTree\nPARTITION BY toYYYYMM(event_date)\nORDER BY (event_date, country, device_type, user_id)\nSETTINGS index_granularity = 8192;\n\nALTER TABLE events\n  ADD INDEX idx_utm_campaign utm_campaign TYPE bloom_filter GRANULARITY 4;\n\nCREATE MATERIALIZED VIEW events_daily_country_device\nENGINE = SummingMergeTree\nPARTITION BY toYYYYMM(day)\nORDER BY (day, country, device_type)\nAS\nSELECT toDate(event_time) AS day,\n       country,\n       device_type,\n       count() AS hits\nFROM   events\nGROUP  BY day, country, device_type;\n\nINSERT INTO events\n  (event_time, user_id, session_id, country, device_type, os, browser,\n   referrer, utm_source, utm_campaign, page_path, page_load_ms)\nSELECT\n  now() - toIntervalDay(number % 90) - toIntervalSecond(number % 86400),\n  number % 50000,\n  generateUUIDv4(),\n  ['DE','US','GB','FR','BR','IN','JP','CA','AU','NL'][1 + (number % 10)],\n  ['desktop','mobile','tablet'][1 + (number % 3)],\n  ['macOS','Windows','iOS','Android','Linux'][1 + (number % 5)],\n  ['Chrome','Safari','Firefox','Edge'][1 + (number % 4)],\n  if(number % 7 = 0, 'https://news.example/', ''),\n  ['google','newsletter','direct','partner'][1 + (number % 4)],\n  if(number % 11 = 0, 'spring_sale_2026',\n     ['winter_push','brand_always','retarget'][1 + (number % 3)]),\n  concat('/page/', toString(number % 200)),\n  50 + (number % 950)\nFROM numbers(200000);\n\nSELECT toDate(event_time) AS day, country, uniq(user_id) AS dau\nFROM   events\nWHERE  event_time >= now() - INTERVAL 30 DAY\n  AND  country IN ('DE','US','GB','FR','BR','IN','JP')\n  AND  device_type IN ('desktop','mobile')\nGROUP  BY day, country\nORDER  BY day, dau DESC\nLIMIT  20;\n\nSELECT browser, count() AS hits\nFROM   events\nWHERE  utm_campaign = 'spring_sale_2026'\n  AND  event_time >= now() - INTERVAL 90 DAY\nGROUP  BY browser\nORDER  BY hits DESC\nLIMIT  10;\n\nSELECT day, country, device_type, sum(hits) AS hits\nFROM events_daily_country_device\nGROUP BY day, country, device_type\nORDER BY day, hits DESC\nLIMIT 10;\n```\n\nApply and verify:\n\n```bash\ndocker compose exec -T clickhouse clickhouse-client --multiquery < setup.sql\ndocker compose exec clickhouse clickhouse-client --query \"SELECT count() FROM events\"\n# Expect: 200000\n# Expect: Q1 day/country/dau rows; Q2 browser hits for spring_sale_2026; MV rows present\n```\n\nChapter mapping:\n  - Column files: MergeTree stores each column separately, so Q1 only reads `event_time`, `country`, `device_type`, `user_id`.\n  - LowCardinality: dictionary + per-row codes ≈ bitmap-style low-cardinality encoding; `country IN (...)` and `device_type IN (...)` stay cheap.\n  - PARTITION BY toYYYYMM(event_date): the last-30-days filter opens at most two monthly parts instead of the whole table.\n  - ORDER BY (event_date, country, device_type, user_id): adjacent equal values compress well and skip indexes can drop granules that cannot match.\n  - bloom_filter on utm_campaign: skips granules that cannot contain `spring_sale_2026` for Q2.\n  - SummingMergeTree MV: a small data cube for daily country/device aggregates — faster reads, less flexible than the raw fact table."
    }
  ]
}
