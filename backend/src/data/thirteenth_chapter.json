{
  "number": 13,
  "name": "A Philosophy of Streaming Systems",
  "theory": [
    {
      "question": "Total order broadcast: what it is, how to achieve it, and when a single total order breaks down at scale.",
      "answer": "Total order broadcast means every node delivers the same sequence of messages in the same order — the formal name for deciding one shared event order, which is equivalent to consensus.\n\nPractically you achieve it by funneling writes through one leader (or a consensus-elected leader) that appends to a shared log; followers and derived systems apply that log in order. Single-leader databases and log-based brokers are the common implementations. You can also use logical clocks to assign comparable timestamps without a central sequencer, but recipients must still tolerate out-of-order delivery and carry extra metadata.\n\nLimits appear as soon as one leader cannot keep up or geography/services force multiple independent writers:\n- Sharding the log makes cross-shard order ambiguous.\n- Multi-region leaders avoid cross-datacenter sync latency but leave inter-region event order undefined.\n- Microservices that share no durable state have no defined order between events that originate in different services.\n- Offline/client-side optimistic updates mean clients and servers often see different orders.\n\nWhen events are truly concurrent (no causal link), arbitrary order is fine. When causality matters — unfriend then send a private message — lost ordering can produce wrong side effects (notify the ex). Fixes: route related keys to one shard, record the state the user saw (with an event id) so later events can reference it, or use conflict resolution for state (not for irreversible side effects like notifications).\n\nComment: Prefer one totally ordered log per entity/key; only pay for global total order when the business truly needs a single sequence across all keys."
    },
    {
      "question": "What's the purpose of indexes, caches and materialized views?",
      "answer": "All three move work from read time to write time: they precompute something so queries do less scanning, joining, or recomputation when a user asks.\n\nWrite path: eagerly update derived structures as base data changes (index postings, cached common query results, precomputed aggregates). Read path: serve the user request from that derived structure, doing only the remaining work.\n\nSpectrum for search: no index → grep the corpus (cheap writes, expensive reads); inverted index → update postings on write, Boolean merge on read; cache of top queries → almost no read work for hits, but writes must refresh those result sets; precompute every possible query → impossible (infinite or exponential query space).\n\nSecondary indexes, spreadsheet-style formula cells, UI-shaped denormalized caches, and ML models are the same idea with different derivation functions. Built-in CREATE INDEX covers the cookie-cutter case; application-specific derivations need custom stream/batch code outside the primary OLTP store.\n\nComment: Draw the boundary where your latency SLO and write amplification meet — celebrity fan-out vs ordinary users often use different write/read splits for the same product feature."
    },
    {
      "question": "Checkout needs a FX rate: contrast the microservices (RPC) approach with the dataflow (subscribe + local join) approach.",
      "answer": "Microservices usually fetch the current exchange rate with a synchronous RPC at purchase time; the dataflow approach subscribes to rate-update events ahead of time, keeps a local copy, and joins each purchase to that local state — no network hop on the critical path.\n\nService-oriented REST/RPC scales organizations by deploying teams independently, but every cross-service call adds latency and a failure domain. Caching the rate inside the purchase service without a change feed just recreates stale data unless you poll or subscribe — which is already the dataflow pattern.\n\nComment: Prefer dataflow for reference data that many services need hot and local (rates, entitlements, feature flags); keep RPC for rare, strongly consistent lookups you cannot afford to lag."
    },
    {
      "question": "User retries a timed-out POST and gets charged twice: why TCP and DB transactions are not enough, and how to fix it end-to-end.",
      "answer": "Classic failure: client sends COMMIT (or HTTP POST), connection drops before the response, client reconnects and retries a non-idempotent money transfer — balances move twice. Same pattern for form resubmits after cellular blips.\n\nFix: generate a stable request id in the client (UUID hidden field, or hash of the form fields) and include it on every retry. Persist it under a uniqueness constraint (`UNIQUE(request_id)`) in the same transaction as the side effects, or treat the insert as the event and derive balance updates downstream while still keying idempotence on that id. A second insert with the same id aborts; the first effect stands.\n\nThis is the end-to-end argument: lower layers reduce how often problems appear, but only application-visible identifiers close the loop for user-level operations.\n\nComment: Pair Post/Redirect/Get for UX with server-side idempotency keys for correctness — the browser warning alone is not a protocol."
    },
    {
      "question": "How to enforce username uniqueness (or similar constraints) with a log-based stream processor instead of a cross-shard ACID transaction?",
      "answer": "Shard the log by the value that must be unique, process each shard sequentially, and emit accept/reject — the shared log's total order per shard is enough to decide winners without a multi-shard atomic commit.\n\nUsername claim flow:\n1. Append a claim message to the shard `hash(username)`.\n2. Single-threaded consumer keeps a local set of taken names; first claim wins and emits success; later claims for the same name emit rejection.\n3. Client watches the output stream for its request's outcome.\n\nSame pattern covers seat bookings, non-negative balances, and stock caps when conflicting writes are routed to one shard. Strict uniqueness still needs consensus-equivalent ordering for that key — async multi-leader that accepts conflicting claims on two leaders cannot preserve uniqueness without later merge/apology.\n\nComment: Twitter/X Manhattan-style uniqueness services are this idea in production — partition by key, serialize decisions, return async success/failure."
    },
    {
      "question": "Timeliness vs integrity in streaming systems — define both and say which ACID conflates.",
      "answer": "Timeliness is how up-to-date a read is; integrity is absence of lasting corruption (no lost, duplicated, or contradictory effects). Event-driven dataflows usually weaken timeliness on purpose while fighting hard for integrity.\n\nCredit-card statements may lag a day (timeliness) and that is expected; a statement whose line items do not sum to the balance, or a charge that never reaches the merchant, is an integrity failure and is catastrophic. Waiting and retrying fixes lag; it does not fix silent double-applies or dropped events.\n\nACID transaction suites typically bundle both (linearizability-like visibility plus atomic commit). Stream architectures decouple them: consumers are async (no default timeliness), but exactly-once/effectively-once delivery, idempotent sinks, immutable single-message writes, end-to-end request ids, and occasional reprocessing preserve integrity without distributed transactions.\n\nMany businesses further loosen strict constraints into apology/compensating workflows (overbook, oversell, overdraft fees) while still requiring that every accepted event is processed once and money/items are tracked."
    },
    {
      "question": "Which steps does require CREATE INDEX on a live table?",
      "answer": "Creating a secondary index is itself a derived-view build: scan a consistent snapshot, extract and sort indexed values, write the index, catch up the write backlog that arrived during the build, then apply every later table write to keep the index current.\n\nThe same three phases appear when setting up a new follower replica and when bootstrapping CDC into a streaming pipeline: snapshot (or compacted log rebuild) → catch up the log → continuous apply. Organization-wide ETL and stream jobs play the same role as the database's built-in index/materialized-view maintenance — they just run as separate processes on separate machines."
    },
    {
      "question": "Federated databases vs unbundled databases: what each unifies, and when to pick which.",
      "answer": "Federation unifies reads across heterogeneous stores behind one query interface; unbundling unifies writes by composing storage systems with ordered change logs so every derived store stays in sync.\n\nFederated / polystore (PostgreSQL FDW, Trino, etc.): one high-level language maps queries onto many engines. Apps that need a specialized model can still talk to the underlying store directly; analysts combine data without copying everything into one warehouse first. Implementation complexity lives in the federator (planning, pushdown, type mapping).\n\nUnbundled databases: treat CDC and event logs like the database's own index-maintenance machinery, exported so Kafka consumers, search indexes, caches, and warehouses can follow one write order. Philosophy matches Unix — small tools, uniform low-level pipe (the log), compose with higher-level jobs. Loose coupling: a slow consumer buffers in the log instead of aborting producers; teams evolve services independently.\n\nWrites across technologies still favor an ordered log plus idempotent consumers over XA-style distributed transactions. Use one integrated product when it covers your access patterns; unbundle only when no single system fits — unbundling is about breadth of workloads, not beating one engine's depth on a single workload."
    },
    {
      "question": "Design auditable data systems: what to log, how to verify derived state, and how that differs from mutating rows in place?",
      "answer": "Practices:\n- Keep an append-only event log as the system of record; hash/sign segments so storage bit-rot is detectable.\n- Derive indexes, balances, and caches with deterministic, versioned code; periodically reprocess and diff against live derived state (or run a redundant deriver in parallel).\n- Do not trust disks, replicas, or mature DBs blindly — HDFS/S3 scrubbers reread and compare replicas; restore from backups on a schedule.\n- End-to-end integrity checks that span the whole pipeline catch more classes of bug than component-local checksums alone.\n\nIn-place UPDATEs across many tables rarely reconstruct the business decision. Explicit dataflow gives provenance and time-travel debugging. Cryptographic ledgers and Merkle proofs (certificate transparency style) are heavier tools in the same family; most products need lighter continuous audit jobs first."
    },
    {
      "question": "Derived data via logs vs distributed transactions: how each aims for consistency and what you lose with asynchrony.",
      "answer": "Both try to keep multiple representations consistent. Distributed transactions use atomic commit so all participants accept or none do; log-derived systems use one ordered write plus deterministic, idempotent replay into followers.\n\nTransactions usually give read-your-writes immediately after commit. Derived pipelines are typically async — readers may see lag until consumers catch up. XA-style protocols across heterogeneous stores have poor fault tolerance and performance, so wide adoption of a better cross-system 2PC is unlikely; ordered logs with idempotent sinks are the practical integration default.\n\nAsync also contains faults: a stuck search indexer buffers in the log while the OLTP primary and other consumers continue; sync distributed transactions tend to amplify one participant's outage into a cluster-wide abort storm.\n\nYou can still offer stronger UX on top of async derivation (wait for the output event, read-your-writes routing to the leader, etc.) without paying XA on every write.\n\nComment: Use single-system transactions for local integrity; use the log for cross-technology fan-out."
    }
  ],
  "practice": [
    {
      "task": "[Kafka] Dataflow FX join: subscribe to rates + purchases, enrich locally",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch13-p0 init\ndocker compose up -d\n# Python deps are preinstalled in the lab image; or: pip install kafka-python>=2.0,<3 kafka-python>=2.0\n\n# Teardown\ndocker compose down -v\n```\n\nCheckout priced in foreign currency must be converted to USD. Instead of an RPC to a rates service on every purchase, use the production dataflow pattern: Kafka topics for rate updates and purchases, a stream processor that keeps a local rate table, and a stream–table join that writes enriched purchases to an output topic (same idea as Kafka Streams `KStream`-`KTable` or Flink/ksqlDB enrichment).\n\nPrerequisites: Docker with the daemon running, Python 3.10+ with `pip`/`venv`, and Apache Kafka reachable at `localhost:9092`.\n\n```python\n#!/usr/bin/env python3\n\"\"\"ch13_fx_join.py — Kafka stream–table join: rates KTable × purchases KStream.\"\"\"\nfrom __future__ import annotations\n\nimport json\nimport sys\nimport time\nfrom typing import Any\n\nfrom kafka import KafkaAdminClient, KafkaConsumer, KafkaProducer\nfrom kafka.admin import NewTopic\nfrom kafka.errors import TopicAlreadyExistsError\n\nBOOTSTRAP = \"localhost:9092\"\nTOPIC_RATES = \"fx.rates\"\nTOPIC_PURCHASES = \"fx.purchases\"\nTOPIC_OUT = \"fx.purchase_usd\"\n\n\ndef producer() -> KafkaProducer:\n    return KafkaProducer(\n        bootstrap_servers=BOOTSTRAP,\n        key_serializer=lambda k: k.encode(\"utf-8\") if k is not None else None,\n        value_serializer=lambda v: json.dumps(v).encode(\"utf-8\"),\n        acks=\"all\",\n    )\n\n\ndef ensure_topics() -> None:\n    admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-fx-admin\")\n    topics = [\n        NewTopic(TOPIC_RATES, num_partitions=1, replication_factor=1),\n        NewTopic(TOPIC_PURCHASES, num_partitions=1, replication_factor=1),\n        NewTopic(TOPIC_OUT, num_partitions=1, replication_factor=1),\n    ]\n    try:\n        admin.create_topics(topics, validate_only=False)\n    except TopicAlreadyExistsError:\n        pass\n    admin.close()\n\n\ndef seed_events(p: KafkaProducer) -> None:\n    \"\"\"Publish rate updates then purchases (keys matter for co-partitioning in production).\"\"\"\n    rates = [\n        {\"currency\": \"EUR\", \"usd_per_unit\": 1.10, \"effective_at\": \"2024-06-01T09:00:00Z\"},\n        {\"currency\": \"GBP\", \"usd_per_unit\": 1.25, \"effective_at\": \"2024-06-01T09:00:00Z\"},\n        {\"currency\": \"EUR\", \"usd_per_unit\": 1.12, \"effective_at\": \"2024-06-01T10:00:00Z\"},\n    ]\n    for r in rates:\n        p.send(TOPIC_RATES, key=r[\"currency\"], value=r)\n    purchases = [\n        {\"purchase_id\": \"p1\", \"currency\": \"EUR\", \"amount\": 100.00, \"purchased_at\": \"2024-06-01T09:30:00Z\"},\n        {\"purchase_id\": \"p2\", \"currency\": \"GBP\", \"amount\": 40.00, \"purchased_at\": \"2024-06-01T09:45:00Z\"},\n        {\"purchase_id\": \"p3\", \"currency\": \"EUR\", \"amount\": 50.00, \"purchased_at\": \"2024-06-01T10:15:00Z\"},\n    ]\n    for buy in purchases:\n        p.send(TOPIC_PURCHASES, key=buy[\"purchase_id\"], value=buy)\n    p.flush()\n\n\ndef apply_rate(local_rates: dict[str, dict[str, Any]], event: dict[str, Any]) -> None:\n    \"\"\"Upsert the local rate table (in-memory KTable stand-in).\"\"\"\n    # implement: local_rates[event[\"currency\"]] = event\n    raise NotImplementedError\n\n\ndef enrich_purchase(\n    local_rates: dict[str, dict[str, Any]], purchase: dict[str, Any]\n) -> dict[str, Any]:\n    \"\"\"Stream–table join: attach the current local rate and compute usd_amount.\"\"\"\n    # implement:\n    # - look up purchase[\"currency\"] in local_rates; raise KeyError if missing\n    # - usd_amount = round(amount * usd_per_unit, 2)\n    # - return dict with purchase_id, currency, amount, usd_amount, rate_used, rate_at, purchased_at\n    raise NotImplementedError\n\n\ndef run_join(max_out: int = 3, timeout_s: float = 20.0) -> list[dict[str, Any]]:\n    \"\"\"\n    Consume rates + purchases (subscribe to both topics), maintain local_rates,\n    emit enriched purchases to TOPIC_OUT. Return up to max_out enriched records.\n    \"\"\"\n    # implement:\n    # - consumer on [TOPIC_RATES, TOPIC_PURCHASES],\n    #   group_id unique per run (e.g. f\"fx-joiner-{time.time_ns()}\"),\n    #   auto_offset_reset=\"earliest\", enable_auto_commit=True,\n    #   value_deserializer=json loads, key_deserializer=utf-8\n    # - out = producer(); local_rates = {}; pending = []; results = []\n    # - loop until len(results) >= max_out or timeout — single poll only (no nested polls):\n    #     poll once; first apply every TOPIC_RATES record in the batch\n    #     buffer every TOPIC_PURCHASES record (never drop purchases from a poll)\n    #     wait until fx.rates partitions are at broker end offsets (seeded EUR\n    #       history lands at 1.12) before enriching any purchase\n    #     then enrich pending purchases whose currency is in local_rates;\n    #       out.send(TOPIC_OUT, ...); results.append(...)\n    # - flush/close; return results\n    raise NotImplementedError\n\n\ndef read_output(n: int = 3, timeout_s: float = 10.0) -> list[dict[str, Any]]:\n    c = KafkaConsumer(\n        TOPIC_OUT,\n        bootstrap_servers=BOOTSTRAP,\n        group_id=f\"fx-out-reader-{time.time_ns()}\",\n        auto_offset_reset=\"earliest\",\n        enable_auto_commit=False,\n        consumer_timeout_ms=int(timeout_s * 1000),\n        value_deserializer=lambda b: json.loads(b.decode(\"utf-8\")),\n        key_deserializer=lambda b: b.decode(\"utf-8\") if b else None,\n    )\n    rows = []\n    for msg in c:\n        rows.append(msg.value)\n        if len(rows) >= n:\n            break\n    c.close()\n    return rows\n\n\ndef main() -> None:\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"demo\"\n    if cmd == \"seed\":\n        ensure_topics()\n        p = producer()\n        seed_events(p)\n        p.close()\n        print(\"seeded rates + purchases\")\n        return\n    if cmd == \"demo\":\n        ensure_topics()\n        # reset lab topics for a clean demo\n        admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-fx-reset\")\n        try:\n            admin.delete_topics([TOPIC_RATES, TOPIC_PURCHASES, TOPIC_OUT])\n            time.sleep(2)\n        except Exception:\n            pass\n        admin.close()\n        ensure_topics()\n        p = producer()\n        seed_events(p)\n        p.close()\n        time.sleep(1)\n        results = run_join(max_out=3)\n        for r in sorted(results, key=lambda x: x[\"purchase_id\"]):\n            print(\n                f\"{r['purchase_id']} usd={r['usd_amount']:.2f} rate={r['rate_used']}\"\n            )\n        # expect p1 usd=112.00 rate=1.12; p2 50.00 / 1.25; p3 56.00 / 1.12\n        return\n    if cmd == \"replay-trap\":\n        # After demo rates are applied, publish a new EUR rate and re-seed purchases\n        # with a fresh joiner group to show time-dependence of \"latest local rate\".\n        ensure_topics()\n        p = producer()\n        p.send(\n            TOPIC_RATES,\n            key=\"EUR\",\n            value={\n                \"currency\": \"EUR\",\n                \"usd_per_unit\": 1.50,\n                \"effective_at\": \"2024-06-01T11:00:00Z\",\n            },\n        )\n        for buy in [\n            {\"purchase_id\": \"p1\", \"currency\": \"EUR\", \"amount\": 100.00, \"purchased_at\": \"2024-06-01T09:30:00Z\"},\n            {\"purchase_id\": \"p3\", \"currency\": \"EUR\", \"amount\": 50.00, \"purchased_at\": \"2024-06-01T10:15:00Z\"},\n        ]:\n            p.send(TOPIC_PURCHASES, key=buy[\"purchase_id\"], value=buy)\n        p.flush()\n        p.close()\n        # run_join already uses a unique group_id per call; re-run the joiner\n        # (or call run_join again) to see purchases enrich against EUR=1.50.\n        print(\"published EUR=1.50 and replay purchases; run a joiner with a new group_id\")\n        return\n    print(\"usage: python3 ch13_fx_join.py [seed|demo|replay-trap]\", file=sys.stderr)\n    sys.exit(2)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nTasks:\n1. Start Kafka, create the venv, install `kafka-python`, save `ch13_fx_join.py`.\n2. Implement `apply_rate`, `enrich_purchase`, and `run_join` at the `# implement` sites.\n3. Run `python3 ch13_fx_join.py demo`. Confirm output lines: `p1 usd=112.00 rate=1.12`, `p2 usd=50.00 rate=1.25`, `p3 usd=56.00 rate=1.12` (catch up `fx.rates` to end offsets and apply rate records before purchases in each poll so local EUR ends at 1.12 before enrich).\n4. In 3–5 sentences: map this lab to microservices (per-purchase rates RPC) vs dataflow (subscribe to `fx.rates` + `fx.purchases`, local join). Name the production engines that implement the same pattern (Kafka Streams / Flink / ksqlDB), and explain why replaying old purchases against a newer local EUR rate breaks reproducible billing.",
      "answer": "Subscribe to Kafka topics `fx.rates` and `fx.purchases`, keep an in-memory rate table, and emit enriched purchases to `fx.purchase_usd` — a stream–table join without a rates RPC on the checkout path.\n\n```python\n#!/usr/bin/env python3\n\"\"\"ch13_fx_join.py — complete reference\"\"\"\nfrom __future__ import annotations\n\nimport json\nimport sys\nimport time\nfrom typing import Any\n\nfrom kafka import KafkaAdminClient, KafkaConsumer, KafkaProducer\nfrom kafka.admin import NewTopic\nfrom kafka.errors import TopicAlreadyExistsError\n\nBOOTSTRAP = \"localhost:9092\"\nTOPIC_RATES = \"fx.rates\"\nTOPIC_PURCHASES = \"fx.purchases\"\nTOPIC_OUT = \"fx.purchase_usd\"\n\n\ndef producer() -> KafkaProducer:\n    return KafkaProducer(\n        bootstrap_servers=BOOTSTRAP,\n        key_serializer=lambda k: k.encode(\"utf-8\") if k is not None else None,\n        value_serializer=lambda v: json.dumps(v).encode(\"utf-8\"),\n        acks=\"all\",\n    )\n\n\ndef ensure_topics() -> None:\n    admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-fx-admin\")\n    topics = [\n        NewTopic(TOPIC_RATES, num_partitions=1, replication_factor=1),\n        NewTopic(TOPIC_PURCHASES, num_partitions=1, replication_factor=1),\n        NewTopic(TOPIC_OUT, num_partitions=1, replication_factor=1),\n    ]\n    try:\n        admin.create_topics(topics, validate_only=False)\n    except TopicAlreadyExistsError:\n        pass\n    admin.close()\n\n\ndef seed_events(p: KafkaProducer) -> None:\n    rates = [\n        {\"currency\": \"EUR\", \"usd_per_unit\": 1.10, \"effective_at\": \"2024-06-01T09:00:00Z\"},\n        {\"currency\": \"GBP\", \"usd_per_unit\": 1.25, \"effective_at\": \"2024-06-01T09:00:00Z\"},\n        {\"currency\": \"EUR\", \"usd_per_unit\": 1.12, \"effective_at\": \"2024-06-01T10:00:00Z\"},\n    ]\n    for r in rates:\n        p.send(TOPIC_RATES, key=r[\"currency\"], value=r)\n    purchases = [\n        {\"purchase_id\": \"p1\", \"currency\": \"EUR\", \"amount\": 100.00, \"purchased_at\": \"2024-06-01T09:30:00Z\"},\n        {\"purchase_id\": \"p2\", \"currency\": \"GBP\", \"amount\": 40.00, \"purchased_at\": \"2024-06-01T09:45:00Z\"},\n        {\"purchase_id\": \"p3\", \"currency\": \"EUR\", \"amount\": 50.00, \"purchased_at\": \"2024-06-01T10:15:00Z\"},\n    ]\n    for buy in purchases:\n        p.send(TOPIC_PURCHASES, key=buy[\"purchase_id\"], value=buy)\n    p.flush()\n\n\ndef apply_rate(local_rates: dict[str, dict[str, Any]], event: dict[str, Any]) -> None:\n    local_rates[event[\"currency\"]] = event\n\n\ndef enrich_purchase(\n    local_rates: dict[str, dict[str, Any]], purchase: dict[str, Any]\n) -> dict[str, Any]:\n    rate = local_rates[purchase[\"currency\"]]\n    usd = round(float(purchase[\"amount\"]) * float(rate[\"usd_per_unit\"]), 2)\n    return {\n        \"purchase_id\": purchase[\"purchase_id\"],\n        \"currency\": purchase[\"currency\"],\n        \"amount\": purchase[\"amount\"],\n        \"usd_amount\": usd,\n        \"rate_used\": rate[\"usd_per_unit\"],\n        \"rate_at\": rate[\"effective_at\"],\n        \"purchased_at\": purchase[\"purchased_at\"],\n    }\n\n\ndef run_join(max_out: int = 3, timeout_s: float = 20.0) -> list[dict[str, Any]]:\n    consumer = KafkaConsumer(\n        TOPIC_RATES,\n        TOPIC_PURCHASES,\n        bootstrap_servers=BOOTSTRAP,\n        group_id=f\"fx-joiner-{time.time_ns()}\",\n        auto_offset_reset=\"earliest\",\n        enable_auto_commit=True,\n        value_deserializer=lambda b: json.loads(b.decode(\"utf-8\")),\n        key_deserializer=lambda b: b.decode(\"utf-8\") if b else None,\n    )\n    out = producer()\n    local_rates: dict[str, dict[str, Any]] = {}\n    pending: list[dict[str, Any]] = []\n    results: list[dict[str, Any]] = []\n    deadline = time.time() + timeout_s\n    rates_caught_up = False\n\n    def rates_at_end() -> bool:\n        rate_tps = [tp for tp in consumer.assignment() if tp.topic == TOPIC_RATES]\n        if not rate_tps:\n            return False\n        ends = consumer.end_offsets(rate_tps)\n        return all(consumer.position(tp) >= ends[tp] for tp in rate_tps)\n\n    def flush_pending() -> None:\n        kept: list[dict[str, Any]] = []\n        for purchase in pending:\n            if len(results) >= max_out:\n                kept.append(purchase)\n                continue\n            if purchase[\"currency\"] not in local_rates:\n                kept.append(purchase)\n                continue\n            enriched = enrich_purchase(local_rates, purchase)\n            out.send(TOPIC_OUT, key=enriched[\"purchase_id\"], value=enriched)\n            results.append(enriched)\n        pending[:] = kept\n\n    while len(results) < max_out and time.time() < deadline:\n        polled = consumer.poll(timeout_ms=500)\n        batch_purchases: list[dict[str, Any]] = []\n        # Apply all rate records in this poll before touching purchases.\n        for records in polled.values():\n            for msg in records:\n                if msg.topic == TOPIC_RATES:\n                    apply_rate(local_rates, msg.value)\n                elif msg.topic == TOPIC_PURCHASES:\n                    batch_purchases.append(msg.value)\n        pending.extend(batch_purchases)\n        if not rates_caught_up and consumer.assignment():\n            rates_caught_up = rates_at_end() and bool(local_rates)\n        if rates_caught_up:\n            flush_pending()\n        if len(results) >= max_out:\n            break\n    out.flush()\n    out.close()\n    consumer.close()\n    return results\n\n\ndef read_output(n: int = 3, timeout_s: float = 10.0) -> list[dict[str, Any]]:\n    c = KafkaConsumer(\n        TOPIC_OUT,\n        bootstrap_servers=BOOTSTRAP,\n        group_id=f\"fx-out-reader-{time.time_ns()}\",\n        auto_offset_reset=\"earliest\",\n        enable_auto_commit=False,\n        consumer_timeout_ms=int(timeout_s * 1000),\n        value_deserializer=lambda b: json.loads(b.decode(\"utf-8\")),\n        key_deserializer=lambda b: b.decode(\"utf-8\") if b else None,\n    )\n    rows = []\n    for msg in c:\n        rows.append(msg.value)\n        if len(rows) >= n:\n            break\n    c.close()\n    return rows\n\n\ndef main() -> None:\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"demo\"\n    if cmd == \"seed\":\n        ensure_topics()\n        p = producer()\n        seed_events(p)\n        p.close()\n        print(\"seeded rates + purchases\")\n        return\n    if cmd == \"demo\":\n        ensure_topics()\n        admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-fx-reset\")\n        try:\n            admin.delete_topics([TOPIC_RATES, TOPIC_PURCHASES, TOPIC_OUT])\n            time.sleep(2)\n        except Exception:\n            pass\n        admin.close()\n        ensure_topics()\n        p = producer()\n        seed_events(p)\n        p.close()\n        time.sleep(1)\n        results = run_join(max_out=3)\n        for r in sorted(results, key=lambda x: x[\"purchase_id\"]):\n            print(\n                f\"{r['purchase_id']} usd={r['usd_amount']:.2f} rate={r['rate_used']}\"\n            )\n        return\n    if cmd == \"replay-trap\":\n        ensure_topics()\n        p = producer()\n        p.send(\n            TOPIC_RATES,\n            key=\"EUR\",\n            value={\n                \"currency\": \"EUR\",\n                \"usd_per_unit\": 1.50,\n                \"effective_at\": \"2024-06-01T11:00:00Z\",\n            },\n        )\n        for buy in [\n            {\"purchase_id\": \"p1\", \"currency\": \"EUR\", \"amount\": 100.00, \"purchased_at\": \"2024-06-01T09:30:00Z\"},\n            {\"purchase_id\": \"p3\", \"currency\": \"EUR\", \"amount\": 50.00, \"purchased_at\": \"2024-06-01T10:15:00Z\"},\n        ]:\n            p.send(TOPIC_PURCHASES, key=buy[\"purchase_id\"], value=buy)\n        p.flush()\n        p.close()\n        print(\"published EUR=1.50 and replay purchases; run a joiner with a new group_id\")\n        return\n    print(\"usage: python3 ch13_fx_join.py [seed|demo|replay-trap]\", file=sys.stderr)\n    sys.exit(2)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\n```bash\n# Expected demo signals\np1 usd=112.00 rate=1.12\np2 usd=50.00 rate=1.25\np3 usd=56.00 rate=1.12\n```\n\nWrite-up: A microservice would call a rates API (or DB) on each purchase — latency plus a hard dependency. Here the processor subscribes to `fx.rates` and `fx.purchases` and joins locally, the same shape as Kafka Streams `KStream.join(KTable)`, Flink temporal joins, or ksqlDB `CREATE STREAM ... JOIN TABLE`. Replaying purchases after a later EUR update shows the join is time-dependent: production billing must pin the rate effective at `purchased_at`, rather than the local table's latest contents.\n"
    },
    {
      "task": "[Kafka, PostgreSQL] End-to-end idempotent transfer with client request_id",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch13-p1 init\ndocker compose up -d\n# Python deps are preinstalled in the lab image; or: pip install kafka-python>=2.0,<3 psycopg[binary]>=3.1,<4 kafka-python>=2.0 psycopg[binary]>=3.1\n\n# Apply ch13_idempotent_transfer_setup.sql (Postgres published on host 5433)\ndocker compose exec -T postgres psql -v ON_ERROR_STOP=1 -U postgres -d ch13_idem_lab < ch13_idempotent_transfer_setup.sql\n\n# Interactive session\ndocker compose exec postgres psql -U postgres -d ch13_idem_lab\n\n# Teardown\ndocker compose down -v\n```\n\nA transfer form may be POSTed twice after a timeout. Publish each attempt to a Kafka topic; a consumer applies it to PostgreSQL. Without an end-to-end request id, at-least-once delivery (or a double POST) charges twice. Enforce idempotency with a client `request_id` unique constraint in Postgres — the usual production pairing of a log-based broker and an idempotent DB sink.\n\nPrerequisites: Docker with the daemon running, Python 3.10+ with `pip`/`venv`, Apache Kafka at `localhost:9092`. Apply SQL only via `docker compose exec`, not a host `psql`.\n\n```sql\n-- ch13_idempotent_transfer_setup.sql\nDROP SCHEMA IF EXISTS xfer CASCADE;\nCREATE SCHEMA xfer;\n\nCREATE TABLE xfer.accounts (\n  account_id TEXT PRIMARY KEY,\n  balance_cents BIGINT NOT NULL CHECK (balance_cents >= 0)\n);\nINSERT INTO xfer.accounts VALUES ('alice', 10000), ('bob', 0);\n\nCREATE TABLE xfer.requests (\n  request_id TEXT PRIMARY KEY,\n  from_account TEXT NOT NULL,\n  to_account TEXT NOT NULL,\n  amount_cents BIGINT NOT NULL CHECK (amount_cents > 0),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n```\n\n```python\n#!/usr/bin/env python3\n\"\"\"ch13_idempotent_transfer.py — Kafka transfer requests + Postgres idempotent sink.\"\"\"\nfrom __future__ import annotations\n\nimport json\nimport sys\nimport time\nimport uuid\n\nimport psycopg\nfrom kafka import KafkaAdminClient, KafkaConsumer, KafkaProducer\nfrom kafka.admin import NewTopic\nfrom kafka.errors import TopicAlreadyExistsError\n\nBOOTSTRAP = \"localhost:9092\"\nTOPIC = \"xfer.requests\"\nDSN = \"dbname=ch13_idem_lab\"\n\n\ndef ensure_topic() -> None:\n    admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-xfer-admin\")\n    try:\n        admin.create_topics([NewTopic(TOPIC, num_partitions=1, replication_factor=1)])\n    except TopicAlreadyExistsError:\n        pass\n    admin.close()\n\n\ndef publish(request_id: str, frm: str, to: str, amount: int) -> None:\n    p = KafkaProducer(\n        bootstrap_servers=BOOTSTRAP,\n        key_serializer=lambda k: k.encode(),\n        value_serializer=lambda v: json.dumps(v).encode(),\n        acks=\"all\",\n    )\n    p.send(\n        TOPIC,\n        key=request_id,\n        value={\n            \"request_id\": request_id,\n            \"from_account\": frm,\n            \"to_account\": to,\n            \"amount_cents\": amount,\n        },\n    )\n    p.flush()\n    p.close()\n\n\ndef apply_naive(conn: psycopg.Connection, event: dict) -> str:\n    \"\"\"Always mutate balances — unsafe under retry / redelivery.\"\"\"\n    with conn.cursor() as cur:\n        cur.execute(\n            \"UPDATE xfer.accounts SET balance_cents = balance_cents - %s WHERE account_id = %s\",\n            (event[\"amount_cents\"], event[\"from_account\"]),\n        )\n        cur.execute(\n            \"UPDATE xfer.accounts SET balance_cents = balance_cents + %s WHERE account_id = %s\",\n            (event[\"amount_cents\"], event[\"to_account\"]),\n        )\n    conn.commit()\n    return \"applied\"\n\n\ndef apply_idempotent(conn: psycopg.Connection, event: dict) -> str:\n    \"\"\"Insert request_id first; on unique violation return 'duplicate' without double-charging.\"\"\"\n    # implement:\n    # - try INSERT INTO xfer.requests (...)\n    # - except psycopg.errors.UniqueViolation: (fully qualified — starter only imports psycopg)\n    #     rollback and return 'duplicate'\n    # - else debit/credit in same transaction; commit; return 'applied'\n    raise NotImplementedError\n\n\ndef consume_once(handler_name: str, group_id: str) -> str:\n    \"\"\"Read one message from TOPIC and apply with naive or idempotent handler.\"\"\"\n    # implement:\n    # - KafkaConsumer(TOPIC, group_id=group_id, auto_offset_reset='earliest', ...)\n    # - poll until one record; call apply_naive or apply_idempotent based on handler_name\n    # - commit the offset (consumer.commit(), or enable_auto_commit=True) so a second\n    #   same-group consume advances past the first message\n    # - return the handler result string\n    raise NotImplementedError\n\n\ndef balances() -> dict[str, int]:\n    with psycopg.connect(DSN) as conn:\n        with conn.cursor() as cur:\n            cur.execute(\"SELECT account_id, balance_cents FROM xfer.accounts ORDER BY 1\")\n            return {a: b for a, b in cur.fetchall()}\n\n\ndef main() -> None:\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"help\"\n    ensure_topic()\n    if cmd == \"reset-db\":\n        with psycopg.connect(DSN) as conn:\n            with conn.cursor() as cur:\n                cur.execute(\"UPDATE xfer.accounts SET balance_cents = 10000 WHERE account_id = 'alice'\")\n                cur.execute(\"UPDATE xfer.accounts SET balance_cents = 0 WHERE account_id = 'bob'\")\n                cur.execute(\"TRUNCATE xfer.requests\")\n            conn.commit()\n        print(\"db reset\", balances())\n        return\n    if cmd == \"reset-kafka\":\n        # delete + recreate TOPIC so prior demo messages cannot leak into the next path\n        admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-xfer-reset\")\n        try:\n            admin.delete_topics([TOPIC])\n            time.sleep(2)\n        except Exception:\n            pass\n        admin.close()\n        ensure_topic()\n        print(\"kafka topic reset\")\n        return\n    if cmd == \"publish\":\n        rid = sys.argv[2] if len(sys.argv) > 2 else str(uuid.uuid4())\n        publish(rid, \"alice\", \"bob\", 1100)\n        print(f\"published request_id={rid}\")\n        return\n    if cmd == \"consume-naive\":\n        print(consume_once(\"naive\", sys.argv[2] if len(sys.argv) > 2 else \"xfer-naive\"))\n        print(balances())\n        return\n    if cmd == \"consume-idempotent\":\n        print(consume_once(\"idempotent\", sys.argv[2] if len(sys.argv) > 2 else \"xfer-idem\"))\n        print(balances())\n        return\n    print(\n        \"usage: reset-db | reset-kafka | publish [request_id] | consume-naive [group] | consume-idempotent [group]\",\n        file=sys.stderr,\n    )\n    sys.exit(2)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nTasks:\n1. Start Kafka and Postgres, apply the SQL setup, install Python deps, save the script.\n2. Implement `apply_idempotent` and `consume_once` (including offset commit so same-group double-consume advances).\n3. Naive path: `python3 ch13_idempotent_transfer.py reset-db`, then `python3 ch13_idempotent_transfer.py publish fixed-id` twice, then `python3 ch13_idempotent_transfer.py consume-naive g-naive` twice. Confirm balances become alice `7800` / bob `2200` after two applies of 1100 — double charge.\n4. Idempotent path: `python3 ch13_idempotent_transfer.py reset-kafka` (deletes and recreates `xfer.requests`; `reset-db` alone does not clear Kafka leftovers), then `python3 ch13_idempotent_transfer.py reset-db`, then `python3 ch13_idempotent_transfer.py publish 0286FDB8-D7E1-423F-B40B-792B3608036C` twice, then `python3 ch13_idempotent_transfer.py consume-idempotent g-idem` twice. Confirm first `applied`, second `duplicate`, balances alice `8900` / bob `1100`.\n5. In 3–5 sentences: why Kafka at-least-once (or a double HTTP POST into Kafka) still needs the Postgres `request_id` uniqueness check — relate to the end-to-end argument.",
      "answer": "Kafka carries transfer intents; PostgreSQL enforces a unique `request_id` so retries and redelivery cannot double-apply side effects.\n\n```sql\n-- ch13_idempotent_transfer_setup.sql\nDROP SCHEMA IF EXISTS xfer CASCADE;\nCREATE SCHEMA xfer;\n\nCREATE TABLE xfer.accounts (\n  account_id TEXT PRIMARY KEY,\n  balance_cents BIGINT NOT NULL CHECK (balance_cents >= 0)\n);\nINSERT INTO xfer.accounts VALUES ('alice', 10000), ('bob', 0);\n\nCREATE TABLE xfer.requests (\n  request_id TEXT PRIMARY KEY,\n  from_account TEXT NOT NULL,\n  to_account TEXT NOT NULL,\n  amount_cents BIGINT NOT NULL CHECK (amount_cents > 0),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n```\n\n```python\n#!/usr/bin/env python3\n\"\"\"ch13_idempotent_transfer.py — complete reference\"\"\"\nfrom __future__ import annotations\n\nimport json\nimport sys\nimport time\nimport uuid\n\nimport psycopg\nfrom kafka import KafkaAdminClient, KafkaConsumer, KafkaProducer\nfrom kafka.admin import NewTopic\nfrom kafka.errors import TopicAlreadyExistsError\n\nBOOTSTRAP = \"localhost:9092\"\nTOPIC = \"xfer.requests\"\nDSN = \"dbname=ch13_idem_lab\"\n\n\ndef ensure_topic() -> None:\n    admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-xfer-admin\")\n    try:\n        admin.create_topics([NewTopic(TOPIC, num_partitions=1, replication_factor=1)])\n    except TopicAlreadyExistsError:\n        pass\n    admin.close()\n\n\ndef publish(request_id: str, frm: str, to: str, amount: int) -> None:\n    p = KafkaProducer(\n        bootstrap_servers=BOOTSTRAP,\n        key_serializer=lambda k: k.encode(),\n        value_serializer=lambda v: json.dumps(v).encode(),\n        acks=\"all\",\n    )\n    p.send(\n        TOPIC,\n        key=request_id,\n        value={\n            \"request_id\": request_id,\n            \"from_account\": frm,\n            \"to_account\": to,\n            \"amount_cents\": amount,\n        },\n    )\n    p.flush()\n    p.close()\n\n\ndef apply_naive(conn: psycopg.Connection, event: dict) -> str:\n    with conn.cursor() as cur:\n        cur.execute(\n            \"UPDATE xfer.accounts SET balance_cents = balance_cents - %s WHERE account_id = %s\",\n            (event[\"amount_cents\"], event[\"from_account\"]),\n        )\n        cur.execute(\n            \"UPDATE xfer.accounts SET balance_cents = balance_cents + %s WHERE account_id = %s\",\n            (event[\"amount_cents\"], event[\"to_account\"]),\n        )\n    conn.commit()\n    return \"applied\"\n\n\ndef apply_idempotent(conn: psycopg.Connection, event: dict) -> str:\n    try:\n        with conn.cursor() as cur:\n            cur.execute(\n                \"\"\"\n                INSERT INTO xfer.requests (request_id, from_account, to_account, amount_cents)\n                VALUES (%s, %s, %s, %s)\n                \"\"\",\n                (\n                    event[\"request_id\"],\n                    event[\"from_account\"],\n                    event[\"to_account\"],\n                    event[\"amount_cents\"],\n                ),\n            )\n            cur.execute(\n                \"\"\"\n                UPDATE xfer.accounts\n                SET balance_cents = balance_cents - %s\n                WHERE account_id = %s AND balance_cents >= %s\n                \"\"\",\n                (event[\"amount_cents\"], event[\"from_account\"], event[\"amount_cents\"]),\n            )\n            if cur.rowcount != 1:\n                raise RuntimeError(\"debit failed\")\n            cur.execute(\n                \"\"\"\n                UPDATE xfer.accounts\n                SET balance_cents = balance_cents + %s\n                WHERE account_id = %s\n                \"\"\",\n                (event[\"amount_cents\"], event[\"to_account\"]),\n            )\n            if cur.rowcount != 1:\n                raise RuntimeError(\"credit failed\")\n        conn.commit()\n        return \"applied\"\n    except psycopg.errors.UniqueViolation:\n        conn.rollback()\n        return \"duplicate\"\n\n\ndef consume_once(handler_name: str, group_id: str) -> str:\n    consumer = KafkaConsumer(\n        TOPIC,\n        bootstrap_servers=BOOTSTRAP,\n        group_id=group_id,\n        auto_offset_reset=\"earliest\",\n        enable_auto_commit=False,\n        consumer_timeout_ms=15000,\n        value_deserializer=lambda b: json.loads(b.decode(\"utf-8\")),\n        key_deserializer=lambda b: b.decode(\"utf-8\") if b else None,\n    )\n    try:\n        for msg in consumer:\n            with psycopg.connect(DSN) as conn:\n                if handler_name == \"naive\":\n                    result = apply_naive(conn, msg.value)\n                else:\n                    result = apply_idempotent(conn, msg.value)\n            consumer.commit()\n            return result\n    finally:\n        consumer.close()\n    raise TimeoutError(\"no message\")\n\n\ndef balances() -> dict[str, int]:\n    with psycopg.connect(DSN) as conn:\n        with conn.cursor() as cur:\n            cur.execute(\"SELECT account_id, balance_cents FROM xfer.accounts ORDER BY 1\")\n            return {a: b for a, b in cur.fetchall()}\n\n\ndef main() -> None:\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"help\"\n    ensure_topic()\n    if cmd == \"reset-db\":\n        with psycopg.connect(DSN) as conn:\n            with conn.cursor() as cur:\n                cur.execute(\"UPDATE xfer.accounts SET balance_cents = 10000 WHERE account_id = 'alice'\")\n                cur.execute(\"UPDATE xfer.accounts SET balance_cents = 0 WHERE account_id = 'bob'\")\n                cur.execute(\"TRUNCATE xfer.requests\")\n            conn.commit()\n        print(\"db reset\", balances())\n        return\n    if cmd == \"reset-kafka\":\n        # delete + recreate TOPIC so prior demo messages cannot leak into the next path\n        admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-xfer-reset\")\n        try:\n            admin.delete_topics([TOPIC])\n            time.sleep(2)\n        except Exception:\n            pass\n        admin.close()\n        ensure_topic()\n        print(\"kafka topic reset\")\n        return\n    if cmd == \"publish\":\n        rid = sys.argv[2] if len(sys.argv) > 2 else str(uuid.uuid4())\n        publish(rid, \"alice\", \"bob\", 1100)\n        print(f\"published request_id={rid}\")\n        return\n    if cmd == \"consume-naive\":\n        print(consume_once(\"naive\", sys.argv[2] if len(sys.argv) > 2 else f\"xfer-naive-{time.time_ns()}\"))\n        print(balances())\n        return\n    if cmd == \"consume-idempotent\":\n        print(consume_once(\"idempotent\", sys.argv[2] if len(sys.argv) > 2 else f\"xfer-idem-{time.time_ns()}\"))\n        print(balances())\n        return\n    print(\n        \"usage: reset-db | reset-kafka | publish [request_id] | consume-naive [group] | consume-idempotent [group]\",\n        file=sys.stderr,\n    )\n    sys.exit(2)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\n```bash\n# Naive double-apply (same group; commit advances offset between calls)\npython3 ch13_idempotent_transfer.py reset-db\npython3 ch13_idempotent_transfer.py publish fixed-id\npython3 ch13_idempotent_transfer.py publish fixed-id\npython3 ch13_idempotent_transfer.py consume-naive g-naive\npython3 ch13_idempotent_transfer.py consume-naive g-naive\n# balances alice=7800 bob=2200\n\n# Idempotent (reset Kafka topic first — reset-db does not clear prior publishes)\npython3 ch13_idempotent_transfer.py reset-kafka\npython3 ch13_idempotent_transfer.py reset-db\npython3 ch13_idempotent_transfer.py publish 0286FDB8-D7E1-423F-B40B-792B3608036C\npython3 ch13_idempotent_transfer.py publish 0286FDB8-D7E1-423F-B40B-792B3608036C\npython3 ch13_idempotent_transfer.py consume-idempotent g-idem\n# applied → alice=8900 bob=1100\npython3 ch13_idempotent_transfer.py consume-idempotent g-idem\n# duplicate → balances unchanged\n```\n\nWrite-up: Kafka (and stream-level exactly-once processing inside one framework) does not stop a browser from publishing the same business request twice after a lost HTTP response. The end-to-end fix is a client-minted `request_id` enforced at the system of record — here Postgres `PRIMARY KEY (request_id)` in the same transaction as the balance updates.\n"
    },
    {
      "task": "[Kafka] Username uniqueness via keyed claim log",
      "question": "Prerequisites: Docker Engine 24+ (or Docker Desktop).\n\n```bash\ndocker run --rm -v \"$PWD:/out\" ghcr.io/anmisttt/ddia-practice:ch13-p2 init\ndocker compose up -d\n# Python deps are preinstalled in the lab image; or: pip install kafka-python>=2.0,<3 kafka-python>=2.0\n\n# Teardown\ndocker compose down -v\n```\n\nSeveral clients race to claim the same username. Use a Kafka topic keyed by username (partitioned log) and a single-threaded consumer per partition that accepts the first claim and rejects the rest — the production pattern for uniqueness via total order broadcast on a shard (same construction as a Kafka Streams / Flink uniqueness enforcer).\n\nPrerequisites: Docker with the daemon running, Python 3.10+ with `pip`/`venv`, Apache Kafka at `localhost:9092`.\n\n```python\n#!/usr/bin/env python3\n\"\"\"ch13_username_unique.py — uniqueness via Kafka key=username ordered log.\"\"\"\nfrom __future__ import annotations\n\nimport json\nimport sys\nimport time\nfrom typing import Any\n\nfrom kafka import KafkaAdminClient, KafkaConsumer, KafkaProducer\nfrom kafka.admin import NewTopic\nfrom kafka.errors import TopicAlreadyExistsError\n\nBOOTSTRAP = \"localhost:9092\"\nTOPIC_CLAIMS = \"uname.claims\"\nTOPIC_DECISIONS = \"uname.decisions\"\nPARTITIONS = 3\n\n\ndef ensure_topics() -> None:\n    admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-uname-admin\")\n    topics = [\n        NewTopic(TOPIC_CLAIMS, num_partitions=PARTITIONS, replication_factor=1),\n        NewTopic(TOPIC_DECISIONS, num_partitions=PARTITIONS, replication_factor=1),\n    ]\n    try:\n        admin.create_topics(topics)\n    except TopicAlreadyExistsError:\n        pass\n    admin.close()\n\n\ndef publish_claims(claims: list[tuple[str, str]]) -> None:\n    \"\"\"claims: list of (request_id, username). Key MUST be username for co-location.\"\"\"\n    p = KafkaProducer(\n        bootstrap_servers=BOOTSTRAP,\n        key_serializer=lambda k: k.encode(),\n        value_serializer=lambda v: json.dumps(v).encode(),\n        acks=\"all\",\n    )\n    for request_id, username in claims:\n        p.send(\n            TOPIC_CLAIMS,\n            key=username,\n            value={\"request_id\": request_id, \"username\": username},\n        )\n    p.flush()\n    p.close()\n\n\ndef decide(taken: dict[str, str], claim: dict[str, Any]) -> dict[str, Any]:\n    \"\"\"First claim for a username wins; later claims are rejected.\"\"\"\n    # implement:\n    # - if username in taken: status rejected\n    # - else: taken[username] = request_id; status accepted\n    # - return {request_id, username, status}\n    raise NotImplementedError\n\n\ndef run_enforcer(expected: int = 5, timeout_s: float = 20.0) -> list[dict[str, Any]]:\n    \"\"\"\n    Consume TOPIC_CLAIMS in log order per partition, emit decisions to TOPIC_DECISIONS.\n    Maintain local `taken` map (production: state store / local DB per shard).\n    \"\"\"\n    # implement:\n    # - consumer group_id unique per run, auto_offset_reset earliest\n    # - producer for TOPIC_DECISIONS keyed by username\n    # - for each claim: decision = decide(taken, value); send to TOPIC_DECISIONS\n    # - collect `expected` decisions then return them\n    raise NotImplementedError\n\n\ndef main() -> None:\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"demo\"\n    if cmd == \"demo\":\n        ensure_topics()\n        admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-uname-reset\")\n        try:\n            admin.delete_topics([TOPIC_CLAIMS, TOPIC_DECISIONS])\n            time.sleep(2)\n        except Exception:\n            pass\n        admin.close()\n        ensure_topics()\n        publish_claims(\n            [\n                (\"r1\", \"neo\"),\n                (\"r2\", \"trinity\"),\n                (\"r3\", \"neo\"),\n                (\"r4\", \"morpheus\"),\n                (\"r5\", \"trinity\"),\n            ]\n        )\n        time.sleep(1)\n        decisions = run_enforcer(expected=5)\n        for d in sorted(decisions, key=lambda x: x[\"request_id\"]):\n            print(f\"{d['request_id']} {d['username']} {d['status']}\")\n        # expect: r1 neo accepted; r2 trinity accepted; r3 neo rejected;\n        #         r4 morpheus accepted; r5 trinity rejected\n        return\n    print(\"usage: python3 ch13_username_unique.py demo\", file=sys.stderr)\n    sys.exit(2)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\nTasks:\n1. Start Kafka, install `kafka-python`, save the script.\n2. Implement `decide` and `run_enforcer`.\n3. Run `python3 ch13_username_unique.py demo`. Confirm: `r1 neo accepted`, `r2 trinity accepted`, `r3 neo rejected`, `r4 morpheus accepted`, `r5 trinity rejected`.\n4. In 2–4 sentences: explain why the Kafka message key must be the username (so all races for one name land on one partition), how that lets you scale with more partitions, and why two async multi-leader databases accepting `neo` concurrently cannot preserve uniqueness without a later merge.",
      "answer": "Key claims by username so one Kafka partition totally orders races for that name; the consumer accepts the first and rejects the rest.\n\n```python\n#!/usr/bin/env python3\n\"\"\"ch13_username_unique.py — complete reference\"\"\"\nfrom __future__ import annotations\n\nimport json\nimport sys\nimport time\nfrom typing import Any\n\nfrom kafka import KafkaAdminClient, KafkaConsumer, KafkaProducer\nfrom kafka.admin import NewTopic\nfrom kafka.errors import TopicAlreadyExistsError\n\nBOOTSTRAP = \"localhost:9092\"\nTOPIC_CLAIMS = \"uname.claims\"\nTOPIC_DECISIONS = \"uname.decisions\"\nPARTITIONS = 3\n\n\ndef ensure_topics() -> None:\n    admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-uname-admin\")\n    topics = [\n        NewTopic(TOPIC_CLAIMS, num_partitions=PARTITIONS, replication_factor=1),\n        NewTopic(TOPIC_DECISIONS, num_partitions=PARTITIONS, replication_factor=1),\n    ]\n    try:\n        admin.create_topics(topics)\n    except TopicAlreadyExistsError:\n        pass\n    admin.close()\n\n\ndef publish_claims(claims: list[tuple[str, str]]) -> None:\n    p = KafkaProducer(\n        bootstrap_servers=BOOTSTRAP,\n        key_serializer=lambda k: k.encode(),\n        value_serializer=lambda v: json.dumps(v).encode(),\n        acks=\"all\",\n    )\n    for request_id, username in claims:\n        p.send(\n            TOPIC_CLAIMS,\n            key=username,\n            value={\"request_id\": request_id, \"username\": username},\n        )\n    p.flush()\n    p.close()\n\n\ndef decide(taken: dict[str, str], claim: dict[str, Any]) -> dict[str, Any]:\n    username = claim[\"username\"]\n    request_id = claim[\"request_id\"]\n    if username in taken:\n        status = \"rejected\"\n    else:\n        taken[username] = request_id\n        status = \"accepted\"\n    return {\"request_id\": request_id, \"username\": username, \"status\": status}\n\n\ndef run_enforcer(expected: int = 5, timeout_s: float = 20.0) -> list[dict[str, Any]]:\n    consumer = KafkaConsumer(\n        TOPIC_CLAIMS,\n        bootstrap_servers=BOOTSTRAP,\n        group_id=f\"uname-enforcer-{time.time_ns()}\",\n        auto_offset_reset=\"earliest\",\n        enable_auto_commit=True,\n        value_deserializer=lambda b: json.loads(b.decode(\"utf-8\")),\n        key_deserializer=lambda b: b.decode(\"utf-8\") if b else None,\n    )\n    out = KafkaProducer(\n        bootstrap_servers=BOOTSTRAP,\n        key_serializer=lambda k: k.encode(),\n        value_serializer=lambda v: json.dumps(v).encode(),\n        acks=\"all\",\n    )\n    taken: dict[str, str] = {}\n    decisions: list[dict[str, Any]] = []\n    deadline = time.time() + timeout_s\n    while len(decisions) < expected and time.time() < deadline:\n        polled = consumer.poll(timeout_ms=500)\n        for records in polled.values():\n            for msg in records:\n                decision = decide(taken, msg.value)\n                out.send(TOPIC_DECISIONS, key=decision[\"username\"], value=decision)\n                decisions.append(decision)\n                if len(decisions) >= expected:\n                    break\n    out.flush()\n    out.close()\n    consumer.close()\n    return decisions\n\n\ndef main() -> None:\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"demo\"\n    if cmd == \"demo\":\n        ensure_topics()\n        admin = KafkaAdminClient(bootstrap_servers=BOOTSTRAP, client_id=\"ch13-uname-reset\")\n        try:\n            admin.delete_topics([TOPIC_CLAIMS, TOPIC_DECISIONS])\n            time.sleep(2)\n        except Exception:\n            pass\n        admin.close()\n        ensure_topics()\n        publish_claims(\n            [\n                (\"r1\", \"neo\"),\n                (\"r2\", \"trinity\"),\n                (\"r3\", \"neo\"),\n                (\"r4\", \"morpheus\"),\n                (\"r5\", \"trinity\"),\n            ]\n        )\n        time.sleep(1)\n        decisions = run_enforcer(expected=5)\n        for d in sorted(decisions, key=lambda x: x[\"request_id\"]):\n            print(f\"{d['request_id']} {d['username']} {d['status']}\")\n        return\n    print(\"usage: python3 ch13_username_unique.py demo\", file=sys.stderr)\n    sys.exit(2)\n\n\nif __name__ == \"__main__\":\n    main()\n```\n\n```bash\n# Expected demo signals\nr1 neo accepted\nr2 trinity accepted\nr3 neo rejected\nr4 morpheus accepted\nr5 trinity rejected\n```\n\nWrite-up: Partitioning by username hash assigns every race for `neo` to one ordered partition so a single-threaded consumer can decide the winner — scale by adding partitions for other name ranges. Async multi-leader stores can each accept `neo` before replicating; uniqueness then needs conflict resolution or an apology workflow, not a hard constraint.\n"
    }
  ]
}
