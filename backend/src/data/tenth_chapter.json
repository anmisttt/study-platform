{
  "number": 10,
  "name": "Consistency and Consensus",
  "theory": [
    {
      "question": "Linearizability vs serializability: define each, how they differ, and what \"strict serializability\" combines.",
      "answer": "Linearizability is a single-object recency guarantee: once a write (or a read of the new value) finishes, every later operation sees that value. Serializability is a multi-object isolation guarantee: concurrent transactions behave as if they ran one after another, but that serial order need not match real-time order.\n\n\"Linearizability is easily confused with serializability… However, they are quite different guarantees, and it is important to distinguish between them.\"\n\nSerializability applies to transactions that may touch many rows or documents. It forbids interleaving that would produce a non-serial outcome (including write skew and phantoms under full serializability). It does not require that if transaction A finishes before B starts in wall-clock time, then B sees A's effects — stale snapshot reads can still be serializable.\n\nLinearizability applies to one register (one key, row, or document). It does not group operations into transactions and therefore does not prevent multi-object anomalies like write skew. What it adds is the real-time / recency constraint: if operation A completes before B begins, B must observe a state at least as new as A's. Concurrent overlapping operations may be ordered in any way that still looks like a valid single-copy history, but once a new value has been observed, later reads cannot flip back to an older value.\n\nA system can offer both: that combination is called strict serializability (or strong one-copy serializability). Single-node databases are typically linearizable. Distributed systems diverge: CockroachDB offers serializability with some recency guarantees but not full strict serializability (cross-transaction coordination is expensive); Spanner and FoundationDB offer strict serializability.\n\nComment: In practice, \"strong consistency\" in vendor docs often means linearizability (or something weaker). Ask whether reads can return stale replicas after a successful write from another client — that is the linearizability question."
    },
    {
      "question": "When is linearizability worth the cost, and which replication designs can provide it?",
      "answer": "You need linearizability when clients must not see stale values after another client (or channel) has already observed a newer state — especially for locks, uniqueness constraints, and cross-channel race avoidance. You can often skip it for social feeds, analytics, and offline-capable apps where eventual consistency is acceptable.\n\n\"In a linearizable system, as soon as one client successfully completes a write, all clients reading from the database must be able to see the value just written.\"\n\nWorth it for: distributed locks and leader election; uniqueness (\"claim this username / seat / filename\"); constraints that must reject concurrent duplicates; dual-channel pipelines where a message queue notifies a worker to read an object store — without linearizable storage, the notify can arrive before the object is visible.\n\nUsually not worth it for: read-heavy timelines where slightly stale is fine; multi-leader / offline sync where conflicts are inevitable; latency-critical paths where lower bounds make linearizable ops pay at least a round-trip to a quorum.\n\nImplementation map:\n- Single-leader sync replication: potentially linearizable if failover cannot lose acknowledged writes and reads go to the leader (or are quorum-verified).\n- Consensus (Raft/Zab/Multi-Paxos): designed for linearizable replicated state; still nonlinearizable if you read from followers without checking leadership.\n- Multi-leader: not linearizable.\n- Leaderless quorum: generally not linearizable (see quorum counterexample).\n\nComment: etcd v3 linearizable reads go through the Raft leader path; ZooKeeper historically made writes linearizable but allowed stale reads unless you sync — know which API you call."
    },
    {
      "question": "Why do coordination services need a leader? What functions does the leader have?",
      "answer": "ZooKeeper, etcd, and Consul replicate a shared state machine via consensus (Zab/Raft). A single leader per epoch is the practical way to totally order client operations into one append-only log so every replica applies the same updates in the same order — without that serialization point, concurrent proposers would fork the log and break linearizable locks, leases, and CAS.\n\n\"…a shared log is easy if a single leader is the only node allowed to append log entries.\"\n\nWhy a leader (not every node proposing freely):\n- Single-value consensus and a shared log are easy with one \"dictator\" that decides the next entry; the hard part is electing a new dictator safely when the old one fails.\n- Common algorithms (Raft, Multi-Paxos, Zab) therefore elect one leader per epoch/term and let only that leader propose appends. Followers vote and replicate; they do not independently invent conflicting next-log-entries.\n- Without a unique leader for an epoch, two nodes could accept conflicting writes (split brain) and clients would see non-linearizable coordination state.\n\nWhat the leader does:\n1. Accepts client writes (create lock, set config, register ephemeral node) and turns them into log entries.\n2. Replicates each entry to a quorum of followers and waits for majority ack before committing — so a committed entry survives leader failure.\n3. Advances a monotonic log index / zxid / revision used as fencing tokens for leases.\n4. Applies committed entries to the in-memory key-value state and serves (or coordinates) linearizable reads — etcd-style linearizable reads confirm the node is still the current leader via a quorum check.\n5. Sends heartbeats so followers do not start unnecessary elections; on timeout, followers run a new election with a higher epoch.\n\nFollowers still matter: they vote in elections, store durable copies of the log, and can serve stale/observer reads. Leaderless consensus (e.g., EPaxos) exists, but production coordination services overwhelmingly use leader-based Raft/Zab because the programming model maps cleanly onto \"one totally ordered metadata log.\"\n\nComment: Distinguish the coordination service's internal Raft/Zab leader from application leaders elected via leases inside that service — the former serializes the ensemble's log; the latter is just a key clients race to own."
    },
    {
      "question": "How leader election works in Raft-style consensus: role of epoch/term numbers, the two voting rounds, and how split brain is prevented.",
      "answer": "Consensus algorithms do not require a single leader for all time. They require a unique leader per epoch (ballot / view / term). A candidate starts an election with a new epoch higher than any it has seen; if two leaders conflict, the higher epoch wins. Before appending a log entry, the leader must prove via a quorum vote that no higher-epoch leader exists.\n\n\"Instead, they make a weaker guarantee: they define an epoch number… and guarantee that within each epoch, the leader is unique.\"\n\nFlow:\n1. Followers expect heartbeats from the current leader. On timeout, a follower increments its term/epoch and requests votes (first quorum round — election).\n2. A node votes yes for at most one candidate per epoch, and only if it has not already seen a higher epoch leader. Majority wins → that node is leader for the epoch.\n3. To commit a client write, the leader proposes a log entry and collects another quorum of acceptances (second round). Quorums for election and for proposals must overlap so at least one voter in a successful append also participated in the latest election — revealing any newer epoch.\n4. On failover, the new leader must not overwrite committed entries. Raft only elects a candidate whose log is at least as up-to-date as a majority; Multi-Paxos may elect anyone but forces catch-up before new appends.\n\nThis differs from 2PC: any node can start an election; only a quorum (not every participant) must respond; safety (agreement on committed entries) holds even if the cluster cannot make progress.\n\nLinearizable reads also need care: a node that thinks it is leader must confirm via quorum (or lease + carefully proven timing) that it has not been superseded — otherwise it can serve stale data. \"Unclean\" leader election (Kafka option: allow a lagging replica to become leader) trades safety for availability and leaves consensus theory.\n\nComment: Epoch/term is the same idea as fencing tokens for leadership — storage and followers should reject messages from lower epochs."
    },
    {
      "question": "List at least 7 use cases of coordination services (ZooKeeper, etcd, Consul)",
      "answer": "Coordination services are small, memory-resident, consensus-replicated key-value systems optimized for cluster metadata — not for high-volume application data. They package linearizable primitives plus failure detection and watches so applications do not reimplement Raft.\n\n\"Instead, they are designed to coordinate among nodes of another distributed system.\"\n\nLocks and leases: atomic CAS / create-if-absent so only one client wins a lease when many race. Failure detection via session heartbeats; ephemeral nodes disappear when the session times out, releasing the lease.\n\nFencing: each grant or log entry gets a monotonic token (ZooKeeper zxid/cversion, etcd revision). Storage rejects lower tokens so paused or delayed former holders cannot corrupt state.\n\nChoosing a leader / primary: same lease pattern for databases, schedulers, and stateful jobs — on failure, another node acquires the lease and takes over.\n\nAllocating work to nodes: map shards, partitions, or actors to hosts; rebalance when nodes join or leave using atomic updates + notifications. Keep consensus on a fixed 3–5 node ensemble while thousands of workers watch assignments — far cheaper than running consensus across all workers.\n\nConfiguration: store slow-changing key-value settings and notify on change. Consensus is optional here; a file/URL poll with TTL often suffices, but reusing an existing ensemble is convenient.\n\nService discovery: register IP/port on startup; clients watch membership. Consensus is often overkill — discovery needs high availability and speed more than linearizability, so caches, DNS TTLs, and ZooKeeper observers (non-voting replicas for stale-but-available reads) are preferred. Use the coordination service for discovery mainly when you already depend on it for leases/leader election.\n\nComment: Kubernetes uses etcd; Spark/Flink HA use ZooKeeper. Do not put per-request application state in etcd — use a real database or BookKeeper-style log for high churn."
    },
    {
      "question": "Why requiring quorum reads and writes (w + r > n) still cannot guarantee linearizability in a Dynamo-style leaderless store and how having a leader can guarantee it?",
      "answer": "Quorum intersection (w + r > n) means a completed write and a later read must share a replica — so once the write has finished, a quorum read should see it. The trap is races while the write is still in flight under variable network delay: one client can observe the new value and return, then another client can still read only old replicas.\n\n\"The quorum condition is met (w + r > n), but this execution is nevertheless not linearizable.\"\n\nClassic counterexample (n = 3, w = 3, r = 2): the writer sends x:=1 to all three replicas. Because of network delay, the write has so far been applied on only one replica; the other two still hold 0. Concurrently:\n- Client A reads a quorum of two, sees 1 on one node and 0 on the other, and returns 1.\n- After A's read has already completed, client B reads the other quorum of two — the two nodes that still have 0 — and returns 0.\n\nB started after A finished, yet B saw an older value. Quorum math does not stop that interleaving while replicas temporarily disagree.\n\nHow a leader (Raft/Zab-style) fixes this:\nWrites are not sprayed independently at every replica. Clients send writes to the leader; the leader appends one log entry, replicates it to a majority, and only then commits and applies it to the state machine. Until commit, linearizable readers must not return the new value. After commit, the leader's applied state is the authority for linearizable reads — there is a single place where \"the current value\" flips from 0 to 1, not three replicas racing ahead of each other.\n\nWhy A cannot return 1 and then B return 0 under linearizable leader reads:\n- B is not allowed to satisfy a linearizable read by picking two lagging followers. Linearizable reads go to the current leader (or use a quorum/lease check that the serving node is still leader and caught up) — etcd works this way.\n- If A's linearizable read already returned 1, the write was committed and applied on the leader. Any later linearizable read also hits that same committed state (or a newer one), so it cannot return 0.\n- Followers may still be behind, and if you read from them without the linearizable path you can still get the Aaliyah/Bryce bug — that is a client/API mistake, not what consensus promises.\n- A partitioned ex-leader is handled by epochs: it must lose elections / fail quorum checks before serving, or it risks stale answers; fencing and read-index/lease protocols exist exactly for that.\n\nAdditional failure modes in leaderless stores: LWW with wall-clock timestamps can mis-order writes under clock skew; linearizable CAS cannot be built from plain quorum read/write alone — it needs consensus.\n\nYou can push Dynamo-style quorums closer to linearizability with synchronous read repair and reading a quorum before writing for timestamps — still weaker and slower than a consensus log for CAS and clean recency.\n\nComment: Cassandra waits for read repair on quorum reads but still loses linearizability via wall-clock LWW; Riak often skips sync read repair for performance."
    },
    {
      "question": "State the CAP theorem carefully, then list its practical limitations and what PACELC adds.",
      "answer": "Under a network partition, a system that wants linearizable (\"consistent\") behavior cannot also stay fully available for those linearizable operations — you must choose. Outside partitions, CAP says almost nothing useful about the latency vs consistency trade-off you make every day.\n\n\"Thus, applications that don't require linearizability can be more tolerant of network problems. This insight is popularly known as the CAP theorem.\"\n\nFormal CAP (Gilbert/Lynch) is narrow: one consistency model (linearizability) and one fault class (network partition / message loss between groups of nodes). If some replicas are cut off from the others, either (1) you refuse linearizable reads/writes on the minority (or everywhere until a majority is reachable), preserving linearizability, or (2) you keep serving on both sides and lose linearizability. Eventual consistency systems take the second path and stay more available under partitions.\n\nLimitations that make CAP a poor design checklist:\n- \"Pick two of C, A, P\" is misleading — partitions happen; you always need a story for P. Better phrasing: when partitioned, choose consistency or availability for linearizable ops.\n- CAP ignores latency, throughput, and partial failures that are not clean partitions (slow nodes, GC pauses).\n- Many production systems are neither \"CP\" nor \"AP\" in the cartoon sense: they sacrifice some availability and some linearizability (e.g., unavailable writes, stale reads allowed).\n- CAP was historically useful to push engineers past \"always linearizable shared-disk clusters,\" but it has little precision for modern multi-region design.\n\nPACELC extends the intuition: if there is a Partition, trade Availability vs Consistency; Else (normal operation) trade Latency vs Consistency. That second clause matches why CPUs and multi-leader replicas drop linearizability even with a healthy network — for speed, not for partitions.\n\nComment: Prefer concrete questions — \"What happens to writes in region B if the link to the primary region dies?\" — over labeling a product CP or AP."
    },
    {
      "question": "Compare unique ID generator designs: single-node counter, sharded IDs, preallocated blocks, random UUIDs, and wall-clock-based IDs (Snowflake / UUID v7). For each, say what uniqueness and ordering you get.",
      "answer": "All of these can give uniqueness (or uniqueness with negligible collision probability). Only a single-node linearizable counter gives total order consistent with real-time completion of ID requests; the distributed schemes weaken ordering to gain fault tolerance, locality, or throughput.\n\n\"All these schemes generate IDs that are unique… but they have much weaker ordering guarantees for IDs than the single-node autoincrementing scheme.\"\n\nSingle-node autoincrement (fetch-and-add): compact (64-bit), totally ordered, linearizable — if request A finishes before B starts, id(B) > id(A). Downsides: SPOF, cross-region RTT, throughput bottleneck, and you must persist the counter across crashes.\n\nSharded assignment (even/odd nodes, or shard bits in the ID): still compact and unique; loses global creation order — ID 16 vs 17 may have been minted on different nodes at different paces.\n\nPreallocated blocks (node A owns 1–1000, B owns 1001–2000): good locality and fewer round trips to a central allocator; ordering across nodes is wrong whenever a later event draws from an earlier block range.\n\nRandom UUID v4 / GUID: generate anywhere with no coordination; 128 bits; order is meaningless for causality or time.\n\nWall-clock + uniqueness bits (Snowflake, ULID, UUID v7, MongoDB ObjectIDs): roughly time-sortable and unique; not linearizable — NTP skew, jumps, and non-monotonic clocks can invert order of causally related events, even on one node if the clock steps backward.\n\nFor linearizable IDs without inventing consensus yourself: run a single-leader \"timestamp oracle\" (TiDB/TiKV style) that persists batches of IDs, or use Spanner-style TrueTime uncertainty wait so later requests always get later timestamps without a central counter.\n\nComment: Prefer UUID v7 / Snowflake when you need approximate time order and horizontal ID minting; use a timestamp oracle when MVCC snapshot correctness depends on real-time ID order across shards."
    },
    {
      "question": "Explain Lamport clocks: how timestamps are formed, how they are updated on send/receive, how to compare them, and what they do and do not guarantee versus linearizable IDs.",
      "answer": "A Lamport timestamp is a pair `(counter, nodeId)`. Each node increments its counter when it creates an event; when it learns a remote timestamp with a higher counter, it raises its local counter to match. Comparing counters first, then node IDs, yields a total order consistent with causality — but not with real-time linearizability.\n\n\"Although Lamport clocks provide a total ordering, they do not provide linearizability—that is, they are not a way of ensuring that a value is up-to-date.\"\n\nUpdate rules: on local event, `counter := counter + 1` and stamp the event. On receiving a message stamped `(c_remote, _)`, set `counter := max(counter, c_remote)` then usually increment again before stamping your next event (exact increment placement varies by presentation; the invariant is that you never mint a stamp ≤ any stamp you have already observed). Two concurrent events can share the same counter and are ordered arbitrarily by node ID.\n\nGuarantees: if A happened-before B (causal path of messages/events), then ts(A) < ts(B). Timestamps are compact and unique. They do not require synchronized wall clocks.\n\nNon-guarantees: if A completed before B started on different machines that never exchanged messages, Lamport does not force ts(B) > ts(A). A photos shard that never read the accounts shard can assign a lower stamp to a later photo upload than to an earlier \"set account private\" write — breaking permission-sensitive MVCC snapshots. Hybrid logical clocks (HLCs) blend physical time with the same \"catch up to max seen\" rule so stamps stay near wall-clock time while preserving causal order; CockroachDB uses HLCs. Vector clocks detect concurrency explicitly but cost one counter per node.\n\nLogical clocks (or even linearizable ID generators) are also not enough alone for fault-tolerant locks: picking the lowest timestamp as the winner still requires hearing from every possible proposer, so a crashed node stalls the decision. That gap is why consensus is needed.\n\nComment: Use Lamport/HLC for causal transaction IDs and event ordering; use consensus (or a linearizable oracle) when \"finished before\" across unconnected components must be reflected in IDs."
    },
    {
      "question": "Pros and cons of deploying consensus-based replication in production.",
      "answer": "Consensus is \"single-leader replication done right\": automatic failover, no split brain, no silent loss of acknowledged writes — at the price of majority quorums, latency, and operational sensitivity to timeouts and flaky links.\n\n\"Consensus is essentially 'single-leader replication done right,' with automatic failover on leader failure, ensuring that no committed data is lost and split brain is not possible.\"\n\nPros: proven safety under the crash-recovery / non-Byzantine model; linearizable state machine replication; fencing-friendly monotonic indices; reusable coordination building blocks.\n\nCons: need 3 nodes to tolerate 1 failure, 5 to tolerate 2; every linearizable op talks to a quorum — adding nodes does not increase write throughput and often slows elections; minority side of a partition is unavailable; timeout tuning is hard across regions (too long → slow failover; too short → election storms). Raft can livelock on a single bad link without extensions like pre-vote; leader-based protocols are sensitive to a slow leader (Egalitarian Paxos and relatives reduce that).\n\nAny automatic failover that is not a proven consensus algorithm is likely unsafe under the fault modes of real networks and process pauses. Using Raft/etcd correctly still does not make the whole application correct — but it is the right substrate for the agreement problem.\n\nComment: Measure p99 cross-AZ RTT before promising linearizable multi-region writes; many products keep a regional Raft group and accept cross-region async replication instead."
    }
  ],
  "practice": [
    {
      "task": "[Python] Reproduce the Dynamo quorum non-linearizable read, then fix it with a leader",
      "question": "Build a tiny 3-replica register simulator that reproduces the Aaliyah/Bryce race (client A returns the new value, then client B returns the old value), then make reads linearizable by routing them through a leader.\n\nPrerequisites: Python 3.10+\n\nSetup — save as `ch10_quorum_race.py` and run it. It should print `BUG: nonlinearizable`:\n\n```python\n#!/usr/bin/env python3\n\"\"\"n=3, w=3, r=2 leaderless register with delayed replica applies.\"\"\"\nfrom dataclasses import dataclass, field\nfrom typing import List, Optional\n\n@dataclass\nclass Replica:\n    name: str\n    value: int = 0\n    inbox: List[int] = field(default_factory=list)  # pending writes\n    durable: int = 0\n\n    def apply_one(self):\n        if self.inbox:\n            self.value = self.inbox.pop(0)\n\nclass LeaderlessStore:\n    def __init__(self):\n        self.replicas = [Replica(\"r0\"), Replica(\"r1\"), Replica(\"r2\")]\n\n    def write_async(self, v: int):\n        # w=3: send to all; network delay = do not apply yet\n        for r in self.replicas:\n            r.inbox.append(v)\n\n    def quorum_read(self, idxs: List[int]) -> int:\n        vals = [self.replicas[i].value for i in idxs]\n        return max(vals)  # naive \"latest\" among quorum\n\ndef reproduce_bug():\n    s = LeaderlessStore()\n    s.write_async(1)\n    s.replicas[0].apply_one()          # only r0 has 1 so far\n    a = s.quorum_read([0, 1])          # sees 1 and 0 -> 1\n    b = s.quorum_read([1, 2])          # sees 0 and 0 -> 0  (after A finished)\n    print(f\"A={a} B={b}\")\n    assert a == 1 and b == 0, \"expected nonlinearizable schedule\"\n    print(\"BUG: nonlinearizable (A saw 1 then B saw 0)\")\n\nclass LeaderStore:\n    \"\"\"Single-leader register; fill in the stub methods below.\"\"\"\n\n    def __init__(self):\n        self.replicas = [Replica(\"r0\"), Replica(\"r1\"), Replica(\"r2\")]\n        self.leader = 0\n        self.epoch = 1\n\n    def write(self, v: int):\n        # implement majority-durable write\n        raise NotImplementedError\n\n    def linearizable_read(self) -> int:\n        # implement linearizable_read with quorum/lease leadership check\n        raise NotImplementedError\n\n    def follower_read(self, idx: int) -> int:\n        # diagnostic follower read (intentionally stale)\n        raise NotImplementedError\n\nif __name__ == \"__main__\":\n    reproduce_bug()\n```\n\nTasks:\n1. Run the setup; confirm A=1 then B=0 with w+r>n still true (r=2).\n2. Implement `LeaderStore` with one elected leader: `write(v)` applies on the leader only after a majority of replicas have the value in durable storage (you can model durability as `replica.durable = v`); `linearizable_read()` must read from the leader (or fail if this node is not leader) and, before returning, confirm that it is still the current leader for its term/epoch using a quorum check or a valid leader lease.\n3. Replay the same schedule: partially deliver the write to one follower, have A then B call `linearizable_read()`. Both must return the same value (either both still 0 if not committed, or both 1 if committed) — never A=1 then B=0.\n4. Implement `follower_read` and add a test that reading from a lagging follower without going through the leader still returns stale 0 — document why that API is not linearizable.\n5. One-paragraph write-up: explain why quorum intersection alone does not stop this race, and briefly note the trade-offs: the leader can become a bottleneck, and a minority partition cannot serve linearizable operations.",
      "answer": "Solution:\n\n```python\n#!/usr/bin/env python3\n\"\"\"Quorum-read race and a leader-based linearizable register.\"\"\"\nfrom dataclasses import dataclass, field\nfrom typing import List\n\n\n@dataclass\nclass Replica:\n    name: str\n    value: int = 0\n    inbox: List[int] = field(default_factory=list)\n    durable: int = 0\n\n    def apply_one(self):\n        if self.inbox:\n            self.value = self.inbox.pop(0)\n\n\nclass LeaderlessStore:\n    def __init__(self):\n        self.replicas = [Replica(\"r0\"), Replica(\"r1\"), Replica(\"r2\")]\n\n    def write_async(self, v: int):\n        for replica in self.replicas:\n            replica.inbox.append(v)\n\n    def quorum_read(self, idxs: List[int]) -> int:\n        return max(self.replicas[i].value for i in idxs)\n\n\ndef reproduce_bug():\n    store = LeaderlessStore()\n    store.write_async(1)\n    store.replicas[0].apply_one()\n    a = store.quorum_read([0, 1])\n    b = store.quorum_read([1, 2])\n    print(f\"A={a} B={b}\")\n    assert a == 1 and b == 0\n    print(\"BUG: nonlinearizable (A saw 1 then B saw 0)\")\n\n\nclass LeaderStore:\n    \"\"\"A three-replica register whose committed reads go through r0.\"\"\"\n\n    def __init__(self):\n        self.replicas = [Replica(\"r0\"), Replica(\"r1\"), Replica(\"r2\")]\n        self.leader = 0\n        self.epoch = 1\n        self._install_leadership()\n\n    def _install_leadership(self):\n        for replica in self.replicas:\n            replica.leader = self.leader\n            replica.epoch = self.epoch\n\n    def _has_leadership_quorum(self) -> bool:\n        acknowledgements = sum(\n            getattr(replica, \"leader\", None) == self.leader\n            and getattr(replica, \"epoch\", None) == self.epoch\n            for replica in self.replicas\n        )\n        return acknowledgements >= 2\n\n    def write(self, v: int):\n        if not self._has_leadership_quorum():\n            raise RuntimeError(\"leader is not confirmed for this epoch\")\n\n        for replica in self.replicas:\n            replica.inbox.append(v)\n        for replica in self.replicas[:2]:\n            replica.durable = v\n        if sum(replica.durable == v for replica in self.replicas) < 2:\n            raise RuntimeError(\"write did not reach a durable majority\")\n        self.replicas[self.leader].value = v\n\n    def linearizable_read(self) -> int:\n        if not self._has_leadership_quorum():\n            raise RuntimeError(\"this node is not leader for the current epoch\")\n        return self.replicas[self.leader].value\n\n    def follower_read(self, idx: int) -> int:\n        \"\"\"Diagnostic API only: it intentionally permits stale values.\"\"\"\n        return self.replicas[idx].value\n\n\ndef replay_with_leader():\n    store = LeaderStore()\n    store.write(1)\n    store.replicas[1].apply_one()  # Partial delivery; r2 still has value 0.\n    a = store.linearizable_read()\n    b = store.linearizable_read()\n    print(f\"leader reads: A={a} B={b}\")\n    assert a == b == 1\n\n    stale = store.follower_read(2)\n    print(f\"lagging follower read: {stale}\")\n    assert stale == 0\n\n\nif __name__ == \"__main__\":\n    reproduce_bug()\n    replay_with_leader()\n```\n\nWrite-up:\n\nQuorum intersection alone does not impose one read order: different quorums can observe different replica apply states. The leader serializes reads and writes after epoch confirmation, but can become a bottleneck, and a minority partition cannot serve linearizable operations.\n\n`follower_read` is not linearizable because it bypasses the leader and can return a stale replica value.\n"
    },
    {
      "task": "[Python] Fix split decisions: implement single-value consensus with epoch leaders",
      "question": "Three nodes each try to become \"the\" winner for a seat booking. The buggy setup lets two nodes decide different values. Fix it with single-value consensus (epoch + quorum).\n\nPrerequisites: Python 3.10+\n\nSetup — save as `ch10_single_value.py` and run it. It should print `BUG: nodes decided different values`. The `ConsensusCluster` stubs raise `NotImplementedError` until you implement them:\n\n```python\n#!/usr/bin/env python3\nfrom dataclasses import dataclass, field\nfrom typing import Any, Dict, Optional\n\n@dataclass\nclass Node:\n    id: str\n    decided: Optional[Any] = None\n    crashed: bool = False\n    voted_epoch: int = -1\n    voted_for: Optional[str] = None\n\ndef buggy_decide(nodes, proposals: Dict[str, str]):\n    # each node \"decides\" its own proposal locally — split brain\n    for n in nodes:\n        n.decided = proposals[n.id]\n    return {n.id: n.decided for n in nodes}\n\n\nclass ConsensusCluster:\n    \"\"\"Single-value consensus with epoch-based leadership — implement the stubs.\"\"\"\n\n    def __init__(self, nodes, proposals: Optional[Dict[str, Any]] = None):\n        self.nodes = list(nodes)\n        self.by_id = {node.id: node for node in self.nodes}\n        self.proposals = dict(proposals or {})\n        self.epoch = 0\n        self.leader_id: Optional[str] = None\n        self.leader_epoch = -1\n\n    def request_votes(self, candidate_id: str, epoch: Optional[int] = None) -> bool:\n        # implement epoch election needing majority; higher epoch wins, one vote per epoch\n        raise NotImplementedError\n\n    def decide(self, proposal: Any, caller_id: str) -> Any:\n        # enforce caller_id vs leader_id/leader_epoch; validate proposal; replicate to majority\n        raise NotImplementedError\n\n\nif __name__ == \"__main__\":\n    nodes = [Node(\"n1\"), Node(\"n2\"), Node(\"n3\")]\n    out = buggy_decide(nodes, {\"n1\": \"alice\", \"n2\": \"bob\", \"n3\": \"carol\"})\n    print(out)\n    assert len(set(out.values())) > 1\n    print(\"BUG: nodes decided different values\")\n```\n\nTasks:\n1. Run the setup; show disagreement.\n2. Implement `ConsensusCluster` with `epoch` and leader election (`request_votes` needing majority). Give `decide(proposal, caller_id)` a mandatory `caller_id` argument and enforce leadership inside the method by checking `caller_id` against the cluster's own internally tracked `leader_id`/`leader_epoch` — never trust an externally supplied \"I am the leader\" flag, and never let a call that omits or fakes the caller's identity finalize a decision. On success, replicate the decision to a majority so all voters learn the same value.\n3. If two candidates start elections, higher epoch wins; lower-epoch leader must step down, and a `decide()` call carrying that stale leader's id must be rejected, not finalized.\n4. Enforce validity inside `decide()` itself — reject (raise) any proposal that is not present in the original proposals map before committing anything, not only in a test assertion afterward. Then assert agreement, integrity (no node changes its mind), and that with one node crashed, the other two can still decide.\n5. Briefly list the four consensus properties next to your asserts.",
      "answer": "Solution:\n\n```python\n#!/usr/bin/env python3\nfrom dataclasses import dataclass, field\nfrom typing import Any, Dict, Optional\n\n@dataclass\nclass Node:\n    id: str\n    decided: Optional[Any] = None\n    crashed: bool = False\n    voted_epoch: int = -1\n    voted_for: Optional[str] = None\n\ndef buggy_decide(nodes, proposals: Dict[str, str]):\n    # each node \"decides\" its own proposal locally — split brain\n    for n in nodes:\n        n.decided = proposals[n.id]\n    return {n.id: n.decided for n in nodes}\n\n\nclass ConsensusCluster:\n    \"\"\"A small single-value consensus cluster with epoch-based leadership.\"\"\"\n\n    def __init__(self, nodes, proposals: Optional[Dict[str, Any]] = None):\n        self.nodes = list(nodes)\n        self.by_id = {node.id: node for node in self.nodes}\n        self.proposals = dict(proposals or {})\n        self.epoch = 0\n        self.leader_id: Optional[str] = None\n        self.leader_epoch = -1\n\n    @property\n    def majority(self) -> int:\n        return len(self.nodes) // 2 + 1\n\n    def request_votes(self, candidate_id: str, epoch: Optional[int] = None) -> bool:\n        \"\"\"Start an election and return whether the candidate won a majority.\"\"\"\n        if candidate_id not in self.by_id or self.by_id[candidate_id].crashed:\n            return False\n        requested_epoch = self.epoch + 1 if epoch is None else epoch\n        if requested_epoch < self.epoch:\n            return False\n        if requested_epoch > self.epoch:\n            self.epoch = requested_epoch\n            self.leader_id = None\n            self.leader_epoch = -1\n\n        votes = 0\n        for voter in self.nodes:\n            if voter.crashed:\n                continue\n            if voter.voted_epoch != self.epoch:\n                voter.voted_epoch = self.epoch\n                voter.voted_for = candidate_id\n                votes += 1\n            elif voter.voted_for == candidate_id:\n                votes += 1\n\n        if votes >= self.majority:\n            self.leader_id = candidate_id\n            self.leader_epoch = self.epoch\n            return True\n        return False\n\n    def decide(self, proposal: Any, caller_id: str) -> Any:\n        \"\"\"Commit a proposed value; only the current epoch leader may call this.\n\n        ``caller_id`` is mandatory and is always checked against the\n        cluster's own tracked ``leader_id``/``leader_epoch`` — there is no\n        way to omit or spoof it to bypass leader enforcement.\n        \"\"\"\n        if (\n            self.leader_id is None\n            or self.leader_epoch != self.epoch\n            or caller_id != self.leader_id\n        ):\n            raise RuntimeError(\"only the current epoch leader may decide\")\n        if proposal not in self.proposals.values():\n            raise ValueError(\"decision must be one of the proposed values\")\n\n        voters = [node for node in self.nodes if not node.crashed]\n        if len(voters) < self.majority:\n            raise RuntimeError(\"not enough live voters for a majority\")\n        if any(node.decided is not None and node.decided != proposal for node in voters):\n            raise RuntimeError(\"a voter already decided a conflicting value\")\n\n        # First persist to a quorum; then let every live voter learn that value.\n        quorum = voters[:self.majority]\n        for node in quorum:\n            node.decided = proposal\n        for node in voters[self.majority:]:\n            if node.decided is not None and node.decided != proposal:\n                raise RuntimeError(\"a voter already decided a conflicting value\")\n            node.decided = proposal\n        return proposal\n\n\nif __name__ == \"__main__\":\n    nodes = [Node(\"n1\"), Node(\"n2\"), Node(\"n3\")]\n    proposals = {\"n1\": \"alice\", \"n2\": \"bob\", \"n3\": \"carol\"}\n    out = buggy_decide(nodes, proposals)\n    print(\"buggy:\", out)\n    assert len(set(out.values())) > 1  # setup demonstrates disagreement\n    print(\"BUG: nodes decided different values\")\n\n    nodes = [Node(\"n1\"), Node(\"n2\"), Node(\"n3\")]\n    cluster = ConsensusCluster(nodes, proposals)\n    assert cluster.request_votes(\"n1\", epoch=1)\n    assert cluster.leader_id == \"n1\"\n    assert not cluster.request_votes(\"n2\", epoch=1)\n    assert cluster.request_votes(\"n2\", epoch=2)  # higher epoch supersedes n1\n    assert cluster.leader_id == \"n2\"\n    try:\n        cluster.decide(\"alice\", caller_id=\"n1\")  # lower-epoch n1 cannot finalize\n    except RuntimeError:\n        pass\n    else:\n        raise AssertionError(\"stale leader finalized a decision\")\n    # Leader enforcement: a node that was never elected cannot finalize either,\n    # even though it isn't a stale ex-leader with a conflicting decision yet.\n    try:\n        cluster.decide(\"bob\", caller_id=\"n3\")\n    except RuntimeError:\n        pass\n    else:\n        raise AssertionError(\"non-leader caller finalized a decision\")\n    cluster.decide(\"bob\", caller_id=\"n2\")\n\n    # Agreement: every live voter learns one identical value.\n    assert {node.decided for node in nodes} == {\"bob\"}\n    # Integrity: deciding again cannot change an already decided value.\n    before = [node.decided for node in nodes]\n    try:\n        cluster.decide(\"carol\", caller_id=\"n2\")\n    except RuntimeError:\n        pass\n    else:\n        raise AssertionError(\"a node changed its mind\")\n    assert [node.decided for node in nodes] == before\n    # Validity: the decided value was proposed by a participant, and decide()\n    # itself rejects an un-proposed value rather than just asserting on it.\n    assert all(node.decided in proposals.values() for node in nodes)\n    try:\n        cluster.decide(\"dave\", caller_id=\"n2\")\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"un-proposed value was accepted\")\n    # Termination: a majority can decide despite one crashed node.\n    nodes = [Node(\"a\"), Node(\"b\"), Node(\"c\", crashed=True)]\n    available = ConsensusCluster(nodes, {\"a\": \"seat-a\", \"b\": \"seat-b\", \"c\": \"seat-c\"})\n    assert available.request_votes(\"a\")\n    available.decide(\"seat-a\", caller_id=\"a\")\n    assert [node.decided for node in nodes[:2]] == [\"seat-a\", \"seat-a\"]\n    print(\"consensus: agreement, integrity, validity, termination\")\n```\n\nConsensus properties:\n\nAgreement, integrity, validity, and termination are asserted next to the tests in the script above.\n"
    },
    {
      "task": "[Python] Fix divergent replica logs: build a Raft-style leader-based shared log",
      "question": "Each replica appends locally; clients see different histories (not a shared log). Fix this with a small Raft-style model: an elected leader serializes appends, replicas store term-tagged entries, and only a majority-replicated prefix is committed and readable.\n\nPrerequisites: Python 3.10+\n\nSetup — save as `ch10_divergent_log.py` and run it. The `__main__` block should print `BUG: logs diverged — not total order broadcast`. Fill in the `SharedLog` stubs (and keep using the Raft-style `Replica` fields) for the later tasks:\n\n```python\n#!/usr/bin/env python3\nfrom dataclasses import dataclass, field\nfrom typing import List, Optional, Tuple\n\n\n@dataclass(frozen=True)\nclass Entry:\n    term: int\n    value: str\n\n\n@dataclass\nclass DivergentReplica:\n    \"\"\"Buggy local-append demo only — no terms or shared log.\"\"\"\n    id: str\n    log: List[str] = field(default_factory=list)\n\n    def append_local(self, entry: str):\n        self.log.append(entry)\n\n\n@dataclass\nclass Replica:\n    id: str\n    # term-tagged log; students append Entry(term, value), not bare strings\n    log: List[Entry] = field(default_factory=list)\n    current_term: int = 0\n    voted_for: Optional[str] = None\n    commit_index: int = -1  # highest locally known committed index; start at -1\n    live: bool = True\n\n    def last_position(self) -> Tuple[int, int]:\n        if not self.log:\n            return (0, -1)\n        return (self.log[-1].term, len(self.log) - 1)\n\n\nclass SharedLog:\n    def __init__(self, replicas: List[Replica]):\n        self.replicas = {replica.id: replica for replica in replicas}\n        self.current_term = 0\n        self.leader_id: Optional[str] = None\n\n    def elect(self, candidate: str, term: int) -> str:\n        # implement per-voter election: reject term <= cluster.current_term\n        # (no same-term re-election); fail if any live voter has a higher\n        # current_term than the election term; bump voter terms, at most one\n        # vote per term, require majority, and enforce candidate\n        # (last_entry_term, last_index) >= each voter's last_position()\n        raise NotImplementedError\n\n    def _catch_up(self, follower: Replica, leader: Replica) -> None:\n        # implement prefix repair: compare full Entry equality (term AND value)\n        # at each index — not terms alone — so same-term/different-value\n        # conflicts are detected; truncate conflicting uncommitted suffix;\n        # copy leader suffix; refuse overwrite/truncate through\n        # follower.commit_index\n        raise NotImplementedError\n\n    def append(\n        self,\n        value: str,\n        acknowledgements: Optional[List[str]] = None,\n        leader_id: Optional[str] = None,\n    ) -> int:\n        # implement leader-only append: always count the leader's own local\n        # append as one ack even if leader id is omitted from acknowledgements;\n        # replicate Entry + matching prefix via _catch_up; if any live ack\n        # target has current_term higher than the leader/cluster term, treat\n        # that as evidence the leader is stale — do not count that follower\n        # as a successful ack; reject the append (raise) and preferably step\n        # down (clear leader_id); commit only after majority ack; after\n        # majority, advance each successful replica's commit_index through\n        # the newly committed prefix (not only a single step when\n        # commit_index == new_index - 1); on minority failure leave commit\n        # indexes unchanged and raise (do not report success)\n        raise NotImplementedError\n\n    def read_from(self, node: str, offset: int) -> List[str]:\n        # implement local committed-prefix read using that node's commit_index\n        # only (never expose uncommitted entries via a cluster-global index)\n        raise NotImplementedError\n\n\nif __name__ == \"__main__\":\n    a, b, c = DivergentReplica(\"a\"), DivergentReplica(\"b\"), DivergentReplica(\"c\")\n    a.append_local(\"W1\")\n    b.append_local(\"W2\")  # concurrent, no agreement\n    c.append_local(\"W1\")\n    print(a.log, b.log, c.log)\n    assert a.log != b.log\n    print(\"BUG: logs diverged — not total order broadcast\")\n```\n\nTasks:\n1. Run the setup and show the divergent logs.\n2. Implement `SharedLog.elect(candidate, term)` as described in the setup comment. Every replica must track its own `current_term` and `voted_for`; process each live voter's term, prior vote, and log-freshness check separately. A voter grants at most one vote per term, election requires a majority, and the candidate's `(last_entry_term, last_index)` must be at least the voter's. Do not model election as selecting an arbitrary first-N quorum. Reject `term <= cluster.current_term` (no same-term re-election). If any live voter already has `current_term` higher than the election term, the election must fail (raise), not succeed by skipping that voter.\n3. Implement `SharedLog.append` and `_catch_up` as described in the setup comments. `_catch_up` must compare full `Entry` equality (term AND value), not terms alone. Only the live elected leader for the current term may append; explicitly reject non-leader and stale-leader calls. Always count the leader's own local append as one acknowledgement even when the leader id is omitted from the `acknowledgements` list. If any live ack target has `current_term` higher than the leader/cluster term, treat that as evidence the leader is stale: do not count that follower as a successful ack; reject the append (raise) and preferably step down. Replicate the new entry and its matching prefix to individual replicas. Advance commit state and return the offset only after acknowledgements from a majority; if no majority is reached, `append` must fail, leave all local commit indexes unchanged, and never report success for that entry. After a majority succeeds, advance each successful replica's `commit_index` through the newly committed prefix so a lagging follower that catches up multiple entries in one round still exposes that committed prefix via `read_from` — do not advance only when `commit_index == new_index - 1`.\n4. Implement `read_from(node, offset) -> list` as described in the setup comment, using that node's local `commit_index`, so it exposes only the node's locally known committed prefix and never an uncommitted entry. A cluster-global or leader commit index must not make a lagging follower expose more of its log. Once an offset is committed, its value is append-only and cannot change.\n5. Extend `__main__` (or one runnable demo) into a complete executable assert suite — not comments/snippets. It must actually execute asserts for all of: the divergent bug; ordered W1 then W2 (or W2 then W1, identical across caught-up replicas) with `commit_index` advancing without holes; minority failure leaving commit indexes unchanged; non-leader append rejection and stale-leader rejection after a higher-term election; omitted-leader ack still succeeding because the leader's local append counts; lagging multi-entry catch-up in one round; an append rejected because an ack target has a higher `current_term` than the leader (commits unchanged); majority-only failover with a conflicting uncommitted suffix repaired by full-Entry `_catch_up`; refuse overwrite/truncate through `commit_index`; and single-value consensus via `read_from(node, 0)[0]`.\n6. Make failover part of that suite: commit an entry to a majority only (not all replicas — leave at least one follower lagging without that entry), crash the leader, elect a higher-term leader using the per-voter up-to-date-log rule, then catch up the lagging follower and show it can `read_from` the majority-committed entry. Assert that the new leader does not overwrite the majority-replicated/committed entry. During catch-up, compare full Entry equality (term AND value), truncate conflicting uncommitted suffixes before copying the leader suffix, and refuse any repair that would overwrite or truncate through a replica's local `commit_index`. Include a conflicting uncommitted suffix (same term, different value is enough) and show that it is removed.\n7. Show that single-value consensus is `read_from(node, 0)[0]`, the first locally committed entry. Briefly explain why an in-memory mutex or majority counter without terms, per-voter elections, per-replica logs/commit indexes, prefix repair, and failover does not establish a fault-tolerant shared log.\n",
      "answer": "Solution:\n\n```python\n#!/usr/bin/env python3\n\"\"\"A small, executable Raft-style shared-log model.\"\"\"\nfrom dataclasses import dataclass, field\nfrom typing import List, Optional, Tuple\n\n\n@dataclass(frozen=True)\nclass Entry:\n    term: int\n    value: str\n\n\n@dataclass\nclass DivergentReplica:\n    \"\"\"Buggy local-append demo only — no terms or shared log.\"\"\"\n    id: str\n    log: List[str] = field(default_factory=list)\n\n    def append_local(self, entry: str):\n        self.log.append(entry)\n\n\n@dataclass\nclass Replica:\n    id: str\n    # term-tagged log; students append Entry(term, value), not bare strings\n    log: List[Entry] = field(default_factory=list)\n    current_term: int = 0\n    voted_for: Optional[str] = None\n    commit_index: int = -1  # highest locally known committed index; start at -1\n    live: bool = True\n\n    def last_position(self) -> Tuple[int, int]:\n        if not self.log:\n            return (0, -1)\n        return (self.log[-1].term, len(self.log) - 1)\n\n\nclass SharedLog:\n    def __init__(self, replicas: List[Replica]):\n        self.replicas = {replica.id: replica for replica in replicas}\n        self.current_term = 0\n        self.leader_id: Optional[str] = None\n\n    def elect(self, candidate: str, term: int) -> str:\n        # Reject same-term re-election and any older cluster term.\n        if term <= self.current_term:\n            raise ValueError(\"stale election term\")\n        node = self.replicas[candidate]\n        if not node.live:\n            raise ValueError(\"dead candidate\")\n\n        live_voters = [v for v in self.replicas.values() if v.live]\n        # Fail before mutating votes if any live voter is ahead of this term.\n        if any(voter.current_term > term for voter in live_voters):\n            raise ValueError(\"candidate term is stale for a voter\")\n\n        # Each live voter handles prior vote and freshness checks\n        # independently; there is no arbitrary first-N quorum.\n        votes = 0\n        candidate_position = node.last_position()\n        for voter in live_voters:\n            if voter.current_term < term:\n                voter.current_term = term\n                voter.voted_for = None\n            if voter.voted_for is not None and voter.voted_for != candidate:\n                continue\n            if candidate_position < voter.last_position():\n                continue\n            voter.voted_for = candidate\n            votes += 1\n        if votes <= len(self.replicas) // 2:\n            raise ValueError(\"election has no majority\")\n        self.current_term = term\n        self.leader_id = candidate\n        node.current_term = term\n        return candidate\n\n    def _catch_up(self, follower: Replica, leader: Replica) -> None:\n        if not follower.live:\n            return\n        # Full Entry equality (term AND value), not term-only matching.\n        common = 0\n        while common < len(follower.log) and common < len(leader.log):\n            if follower.log[common] != leader.log[common]:\n                break\n            common += 1\n        # Anything before common is matching. A mismatch at or before the\n        # local commit point can never be repaired by overwriting history.\n        if common <= follower.commit_index:\n            if (common >= len(leader.log)\n                    or common >= len(follower.log)\n                    or follower.log[common] != leader.log[common]):\n                raise ValueError(\"repair would overwrite committed history\")\n        if len(follower.log) > common:\n            if common - 1 < follower.commit_index:\n                raise ValueError(\"repair would truncate committed history\")\n            follower.log = follower.log[:common]\n        follower.log.extend(leader.log[common:])\n\n    def append(self, value: str, acknowledgements: Optional[List[str]] = None,\n               leader_id: Optional[str] = None) -> int:\n        if self.leader_id is None:\n            raise ValueError(\"no elected leader\")\n        if leader_id is not None and leader_id != self.leader_id:\n            raise ValueError(\"caller is not the elected leader\")\n        leader = self.replicas[self.leader_id]\n        if not leader.live:\n            raise ValueError(\"leader is not live\")\n        if leader.current_term != self.current_term:\n            raise ValueError(\"leader has stale term\")\n        entry = Entry(self.current_term, value)\n        leader.log.append(entry)\n        new_index = len(leader.log) - 1\n        targets = (set(acknowledgements) if acknowledgements is not None\n                   else {r.id for r in self.replicas.values() if r.live})\n        # Leader's local append always counts as one ack, even if omitted.\n        acked = {leader.id}\n        # Higher-term ack target => leader is stale; do not count it, reject.\n        for node_id in targets:\n            if node_id == leader.id or node_id not in self.replicas:\n                continue\n            follower = self.replicas[node_id]\n            if follower.live and follower.current_term > self.current_term:\n                self.leader_id = None  # step down\n                raise ValueError(\"stale leader: ack target has higher current_term\")\n        for node_id in targets:\n            if node_id == leader.id or node_id not in self.replicas:\n                continue\n            follower = self.replicas[node_id]\n            if not follower.live:\n                continue\n            follower.current_term = self.current_term\n            try:\n                self._catch_up(follower, leader)\n            except ValueError:\n                continue\n            if follower.log and follower.log[-1] == entry:\n                acked.add(follower.id)\n        if len(acked) <= len(self.replicas) // 2:\n            # No commit index was changed. The leader's suffix is deliberately\n            # left uncommitted, as it would be in a real failed replication.\n            raise ValueError(\"append did not reach a majority\")\n        # Advance each successful replica through the newly committed prefix\n        # (covers lagging catch-up of multiple entries in one round).\n        for node_id in acked:\n            replica = self.replicas[node_id]\n            replica.commit_index = max(replica.commit_index, new_index)\n        return new_index\n\n    def read_from(self, node: str, offset: int) -> List[str]:\n        replica = self.replicas[node]\n        end = min(replica.commit_index, len(replica.log) - 1)\n        return [entry.value for entry in replica.log[offset:end + 1]]\n\n\nif __name__ == \"__main__\":\n    # 1. Divergent bug\n    a0, b0, c0 = DivergentReplica(\"a\"), DivergentReplica(\"b\"), DivergentReplica(\"c\")\n    a0.append_local(\"W1\")\n    b0.append_local(\"W2\")\n    c0.append_local(\"W1\")\n    print(a0.log, b0.log, c0.log)\n    assert a0.log != b0.log\n    print(\"BUG: logs diverged — not total order broadcast\")\n\n    a, b, c = Replica(\"a\"), Replica(\"b\"), Replica(\"c\")\n    cluster = SharedLog([a, b, c])\n    assert cluster.elect(\"a\", 1) == \"a\"\n    # Same-term re-election is rejected.\n    try:\n        cluster.elect(\"b\", 1)\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"same-term re-election unexpectedly succeeded\")\n    # A live voter with a higher term fails the election (not skipped).\n    c.current_term = 5\n    try:\n        cluster.elect(\"a\", 2)\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"election ignored a live voter with higher term\")\n    c.current_term = 1\n    c.voted_for = None\n\n    # 2. Ordered W1/W2 with commit_index advancing without holes\n    first = cluster.append(\"W1\")\n    second = cluster.append(\"W2\")\n    assert (first, second) == (0, 1)\n    assert cluster.read_from(\"a\", 0) == [\"W1\", \"W2\"]\n    assert cluster.read_from(\"b\", 0) == [\"W1\", \"W2\"]\n    assert a.commit_index == 1 and b.commit_index == 1\n    assert c.commit_index == 1\n    assert [r.commit_index for r in (a, b, c)] == [1, 1, 1]\n\n    # 3. Omitted-leader ack still succeeds (leader auto-counted)\n    idx = cluster.append(\"W3\", acknowledgements=[\"b\"])\n    assert idx == 2\n    assert cluster.read_from(\"a\", 0) == [\"W1\", \"W2\", \"W3\"]\n    assert cluster.read_from(\"b\", 0) == [\"W1\", \"W2\", \"W3\"]\n\n    # 4. Minority failure leaves commit indexes unchanged\n    before = [r.commit_index for r in (a, b, c)]\n    try:\n        cluster.append(\"UNCOMMITTED\", acknowledgements=[])\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"minority append unexpectedly committed\")\n    assert [r.commit_index for r in (a, b, c)] == before\n    del a.log[a.commit_index + 1:]\n\n    # 5. Non-leader rejection\n    try:\n        cluster.append(\"NONLEADER\", acknowledgements=[\"a\", \"b\"], leader_id=\"b\")\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"non-leader append was accepted\")\n\n    # 6. Higher-term ack target: reject append, do not count as ack, step down\n    before = [r.commit_index for r in (a, b, c)]\n    b.current_term = 99\n    try:\n        # Would be a majority (leader + b + c) if the higher-term follower counted\n        cluster.append(\"STALE\", acknowledgements=[\"b\", \"c\"])\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"higher-term ack target did not reject append\")\n    assert [r.commit_index for r in (a, b, c)] == before\n    assert cluster.leader_id is None  # stepped down\n    del a.log[a.commit_index + 1:]\n    b.current_term = 1\n    b.voted_for = None\n    c.current_term = 1\n    c.voted_for = None\n    a.voted_for = None\n    # Re-elect after step-down so remaining tests can continue\n    assert cluster.elect(\"a\", 2) == \"a\"\n\n    # 7. Lagging multi-entry catch-up in one round\n    c.log = []\n    c.commit_index = -1\n    caught = cluster.append(\"W4\", acknowledgements=[\"c\"])  # leader a + c\n    assert caught == 3\n    assert cluster.read_from(\"c\", 0) == [\"W1\", \"W2\", \"W3\", \"W4\"]\n    assert c.commit_index == 3\n\n    # 8. Majority-only failover + conflicting uncommitted suffix repair\n    a2, b2, c2 = Replica(\"a\"), Replica(\"b\"), Replica(\"c\")\n    cluster2 = SharedLog([a2, b2, c2])\n    assert cluster2.elect(\"a\", 1) == \"a\"\n    maj = cluster2.append(\"MAJ\", acknowledgements=[\"b\"])  # leader a + b only\n    assert maj == 0\n    assert cluster2.read_from(\"a\", 0) == [\"MAJ\"]\n    assert cluster2.read_from(\"b\", 0) == [\"MAJ\"]\n    assert cluster2.read_from(\"c\", 0) == []\n    assert len(c2.log) == 0 and c2.commit_index == -1\n\n    a2.live = False\n    assert cluster2.elect(\"b\", 2) == \"b\"\n    # Stale-leader rejection after higher-term election\n    try:\n        a2.live = True\n        cluster2.append(\"OLD-LEADER\", acknowledgements=[\"a\", \"b\", \"c\"],\n                       leader_id=\"a\")\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"old/dead leader was accepted\")\n    a2.live = False\n\n    # Same-term / different-value conflict must be detected by full Entry equality\n    c2.log.append(Entry(1, \"BAD\"))\n    c2.commit_index = -1\n    cluster2._catch_up(c2, b2)\n    assert [e.value for e in c2.log] == [\"MAJ\"]\n    assert cluster2.append(\"NEXT\", acknowledgements=[\"c\"]) == 1\n    assert cluster2.read_from(\"c\", 0) == [\"MAJ\", \"NEXT\"]\n    assert cluster2.read_from(\"b\", 0) == [\"MAJ\", \"NEXT\"]\n\n    # 9. Refuse overwrite through commit_index\n    saved = c2.log[0]\n    c2.log[0] = Entry(9, \"CORRUPT\")\n    c2.commit_index = 0\n    try:\n        cluster2._catch_up(c2, b2)\n    except ValueError:\n        pass\n    else:\n        raise AssertionError(\"repair overwrote committed prefix\")\n    c2.log[0] = saved\n    cluster2._catch_up(c2, b2)\n\n    # 10. Single-value consensus via read_from(node, 0)[0]\n    assert cluster2.read_from(\"b\", 0)[0] == \"MAJ\"\n    print(\"PASS: ordered majority log, term elections, repair, and failover\")\n```\n\nWrite-up:\n\nAn in-memory mutex or majority counter alone does not establish a fault-tolerant shared log: it lacks terms, per-voter elections, local logs/commit indexes, prefix repair, and safe failover.\n"
    },
    {
      "task": "[Python] Fix a racy username claim: implement linearizable CAS on top of the shared log",
      "question": "Two clients concurrently claim `alice`. The buggy register lets both succeed (lost update / no atomic compare). Reproduce the race, then implement CAS via the shared log so exactly one wins.\n\nPrerequisites: Python 3.10+ (stdlib threading only; no extra packages).\n\nSetup — `ch10_cas_race.py`:\n\n```python\n#!/usr/bin/env python3\nimport threading\n\nclass RacyRegister:\n    def __init__(self):\n        self.value = None\n\n    def claim(self, name):\n        # check-then-act without atomicity\n        if self.value is None:\n            # yield window for the race\n            threading.Event().wait(0.01)\n            self.value = name\n            return True\n        return False\n\n\nclass SharedLog:\n    \"\"\"Append-only state machine: CAS and reads must go through the log.\"\"\"\n\n    def __init__(self):\n        self.entries = []\n        self._state = None\n\n    def cas(self, key, expected, new):\n        # implement CAS as ordered state-machine command (append then apply)\n        raise NotImplementedError\n\n    def read(self, key):\n        # linearizable read of current register value via the log\n        raise NotImplementedError\n\n\ndef consensus_via_cas(log, proposal):\n    \"\"\"Worker entrypoint: claim via CAS, then learn the winner by reading.\"\"\"\n    # implement consensus-via-CAS: cas(None, proposal) then read; return decision\n    raise NotImplementedError\n\n\nif __name__ == \"__main__\":\n    reg = RacyRegister()\n    results = []\n\n    def worker(name):\n        results.append(reg.claim(name))\n\n    t1 = threading.Thread(target=worker, args=(\"alice\",))\n    t2 = threading.Thread(target=worker, args=(\"bob\",))\n    t1.start(); t2.start(); t1.join(); t2.join()\n    print(\"results\", results, \"value\", reg.value)\n    if results.count(True) == 2:\n        print(\"BUG: both claims succeeded\")\n    else:\n        print(\"re-run; timing dependent — increase wait or use barriers\")\n```\n\nIf the race is flaky on your machine, replace the sleep with an explicit barrier so both threads pass the `is None` check before either writes.\n\nTasks:\n1. Reproduce two successful claims (or force it with a barrier).\n2. Implement `cas(key, expected, new) -> ok` as a state-machine command appended to your shared log: apply entries in order; CAS succeeds only if current == expected.\n3. Implement consensus-via-CAS: the register starts `None`, and each participant worker must call `cas(None, its_proposal)`, then perform its own linearizable state-machine read and return that read value as its consensus decision. Do not have the main thread read the register on the participants' behalf. Fill in `consensus_via_cas` (or an equivalent worker) as the participant entrypoint.\n4. Assert exactly one of alice/bob's CAS calls succeeds. The loser may receive `False`/`error` from its CAS, but both participant workers must return the same non-`None` winning proposal. State explicitly that this construction demonstrates the equivalence between linearizable CAS and single-value consensus: CAS chooses the one `None -> proposal` transition, and every participant learns that choice by reading it.\n5. Add a small repeated concurrent stress test (for example, 50–100 fresh two-participant rounds). In every round, assert exactly one CAS succeeds and both participants' own reads return the same proposed value.\n6. Explain in comments why quorum intersection alone cannot make the read and conditional write one atomic operation: concurrent clients can observe the same old value and write conflicting proposals without a single agreed order, so plain quorum read/write without a consensus log cannot provide linearizable CAS.",
      "answer": "Solution:\n\n```python\n#!/usr/bin/env python3\nimport threading\n\n\nclass RacyRegister:\n    \"\"\"The check and write are deliberately separated to reproduce the bug.\"\"\"\n\n    def __init__(self):\n        self.value = None\n        self.checked = threading.Barrier(2)\n\n    def claim(self, name):\n        if self.value is None:\n            # Force both clients past the check before either writes.\n            self.checked.wait()\n            self.value = name\n            return True\n        return False\n\n\nclass SharedLog:\n    \"\"\"A tiny consensus log whose append-and-apply operation is linearizable.\"\"\"\n\n    def __init__(self):\n        self._lock = threading.Lock()\n        self.entries = []\n        self._state = None\n\n    def _append_and_apply(self, command):\n        with self._lock:\n            self.entries.append(command)\n            kind, *args = command\n            if kind == \"cas\":\n                expected, new = args\n                ok = self._state == expected\n                if ok:\n                    self._state = new\n                return ok\n            if kind == \"read\":\n                return self._state\n            raise ValueError(f\"unknown command: {kind}\")\n\n    def cas(self, key, expected, new):\n        # This register has one logical key; retaining the key argument makes\n        # the CAS interface explicit and lets the log validate misuse.\n        if key != \"username\":\n            raise KeyError(key)\n        return self._append_and_apply((\"cas\", expected, new))\n\n    def read(self, key):\n        if key != \"username\":\n            raise KeyError(key)\n        return self._append_and_apply((\"read\",))\n\n\ndef consensus_via_cas(log, proposal):\n    \"\"\"Worker entrypoint: claim via CAS, then learn the winner by reading.\"\"\"\n    won = log.cas(\"username\", None, proposal)\n    # The participant, rather than the caller, learns the decision.\n    decision = log.read(\"username\")\n    return won, decision\n\n\ndef reproduce_race():\n    reg = RacyRegister()\n    results = []\n\n    def worker(name):\n        results.append(reg.claim(name))\n\n    t1 = threading.Thread(target=worker, args=(\"alice\",))\n    t2 = threading.Thread(target=worker, args=(\"bob\",))\n    t1.start()\n    t2.start()\n    t1.join()\n    t2.join()\n    assert results.count(True) == 2, (results, reg.value)\n    print(\"racy results\", results, \"value\", reg.value)\n\n\ndef consensus_round(proposals):\n    log = SharedLog()\n    outcomes = []\n    outcomes_lock = threading.Lock()\n\n    def participant(proposal):\n        won, decision = consensus_via_cas(log, proposal)\n        with outcomes_lock:\n            outcomes.append((proposal, won, decision))\n\n    threads = [\n        threading.Thread(target=participant, args=(proposal,))\n        for proposal in proposals\n    ]\n    for thread in threads:\n        thread.start()\n    for thread in threads:\n        thread.join()\n\n    assert sum(won for _, won, _ in outcomes) == 1, outcomes\n    decisions = [decision for _, _, decision in outcomes]\n    assert len(decisions) == 2\n    assert decisions[0] == decisions[1] != None, outcomes\n    assert decisions[0] in proposals, outcomes\n    return outcomes, log\n\n\nif __name__ == \"__main__\":\n    reproduce_race()\n\n    outcomes, log = consensus_round((\"alice\", \"bob\"))\n    print(\"consensus outcomes\", outcomes, \"log\", log.entries)\n\n    for _ in range(75):\n        outcomes, _ = consensus_round((\"alice\", \"bob\"))\n        assert sum(won for _, won, _ in outcomes) == 1\n        assert {decision for _, _, decision in outcomes} in ({\"alice\"}, {\"bob\"})\n\n    print(\"stress rounds: 75 passed\")\n```\n\nWrite-up:\n\nQuorum intersection alone cannot make read + conditional write atomic: concurrent clients can observe the same old value and write conflicting proposals without a single agreed order. Plain quorum read/write therefore cannot provide linearizable CAS; the ordered consensus log supplies the missing order.\n\nLinearizable CAS is single-value consensus: the log chooses the one `None -> proposal` transition, and every participant learns it by reading.\n"
    },
    {
      "task": "[Python] Fix lost increments and show why fetch-and-add alone is not full consensus",
      "question": "Concurrent `+1` ops lose updates on a racy counter. Fix with CAS-retry or log-backed fetch-and-add. Then demonstrate the consensus-number limit: with three proposers, fetch-and-add alone cannot finish consensus if the winner crashes before announcing.\n\nPrerequisites: Python 3.10+ (stdlib `threading` only; no extra packages).\n\nSetup — save as `ch10_fetch_and_add.py` and run it:\n\n```python\n#!/usr/bin/env python3\nimport threading\n\n\nclass RacyCounter:\n    def __init__(self):\n        self.value = 0\n\n    def incr(self):\n        tmp = self.value\n        threading.Event().wait(0.001)\n        self.value = tmp + 1\n        return tmp\n\n\nclass CASCounter:\n    \"\"\"Linearizable counter via compare-and-swap.\"\"\"\n\n    def __init__(self):\n        self.value = 0\n        self._lock = threading.Lock()\n\n    def compare_and_swap(self, expected, replacement):\n        # implement linearizable CAS under self._lock\n        raise NotImplementedError\n\n    def fetch_and_add(self, delta):\n        # implement FAA as CAS-retry: read old, CAS(old, old+delta), return old\n        raise NotImplementedError\n\n\nclass LogBackedCounter:\n    \"\"\"Optional: counter defined by committed add entries on a shared log.\"\"\"\n\n    def __init__(self):\n        self.log = []  # list of {\"op\": \"add\", \"delta\": int}\n        self._lock = threading.Lock()\n\n    def append_add(self, delta):\n        # append {\"op\":\"add\",\"delta\":delta} under the lock; return log index\n        raise NotImplementedError\n\n    def apply(self):\n        # return sum of committed add deltas (state-machine apply)\n        raise NotImplementedError\n\n\nclass FetchAndAddOnly:\n    \"\"\"FAA primitive only — no publish/read-of-others API.\"\"\"\n\n    def __init__(self):\n        self._value = 0\n        self._lock = threading.Lock()\n\n    def fetch_and_add(self, delta):\n        with self._lock:\n            old = self._value\n            self._value += delta\n            return old\n\n\nclass Proposer:\n    def __init__(self, proposal, primitive):\n        self.proposal = proposal\n        self.primitive = primitive\n        self.ticket = None\n        self.published = False\n        self.crashed = False\n\n    def acquire_ticket(self):\n        # call FAA(1); store returned ticket locally only\n        raise NotImplementedError\n\n    def publish(self):\n        # mark published=True (survivors must not learn crashed proposal this way)\n        raise NotImplementedError\n\n\ndef show_faa_consensus_limit():\n    # three proposers; ticket-0 crashes before publish; survivors use only local state\n    raise NotImplementedError\n\n\nclass ConsensusLog:\n    def __init__(self):\n        self.entries = []\n        self._lock = threading.Lock()\n\n    def append(self, proposal):\n        # append proposal; return committed revision (1-based length)\n        raise NotImplementedError\n\n    def first_committed(self):\n        # return first committed proposal (decision after a client crash post-commit)\n        raise NotImplementedError\n\n\nclass ResourceServer:\n    def __init__(self):\n        self.max_seen = 0\n        self.value = None\n\n    def write(self, token, value):\n        # accept iff token > max_seen; update max_seen and value; else reject\n        raise NotImplementedError\n\n\nif __name__ == \"__main__\":\n    c = RacyCounter()\n    threads = [threading.Thread(target=c.incr) for _ in range(50)]\n    for t in threads:\n        t.start()\n    for t in threads:\n        t.join()\n    print(\"final\", c.value)\n    if c.value != 50:\n        print(\"BUG: lost updates\", c.value)\n```\n\nTasks:\n1. Run until you see `final < 50` (lost updates).\n2. Implement `CASCounter.compare_and_swap` and `fetch_and_add` (CAS-retry loop); assert 50 increments → final 50.\n3. Alternatively, implement `LogBackedCounter.append_add` / `apply` so the counter is the sum of committed `{\"op\":\"add\",\"delta\":1}` entries (or apply fetch-and-add in the state machine).\n4. Implement `Proposer.acquire_ticket` / `publish` and `show_faa_consensus_limit`: three proposers with private proposals and only fetch-and-add plus ordinary local state; the proposer receiving ticket `0` crashes before publishing. From the two survivors' local observations, show why they cannot safely determine the crashed proposal or decide a valid winner. Do not give them a central map of all return values/proposals or centrally compute the winner. State explicitly that fetch-and-add has consensus number 2.\n5. Implement `ConsensusLog` so three proposers agree even if a client crashes after its proposal is committed; state explicitly that CAS and a consensus log support consensus for an arbitrary number of participants (consensus number ∞).\n6. Implement `ResourceServer.write` using the committed log offset/revision of a successful acquire as a fencing token: track `max_seen`, accept a write carrying a newer token, and reject a delayed former holder's write carrying a lower stale token.\n",
      "answer": "Complete runnable reference for `ch10_fetch_and_add.py`:\n\n```python\n#!/usr/bin/env python3\nimport threading\n\n\nclass RacyCounter:\n    def __init__(self):\n        self.value = 0\n\n    def incr(self):\n        tmp = self.value\n        threading.Event().wait(0.001)\n        self.value = tmp + 1\n        return tmp\n\n\nclass CASCounter:\n    \"\"\"A counter with a linearizable compare-and-swap operation.\"\"\"\n\n    def __init__(self):\n        self.value = 0\n        self._lock = threading.Lock()\n\n    def compare_and_swap(self, expected, replacement):\n        with self._lock:\n            if self.value != expected:\n                return False\n            self.value = replacement\n            return True\n\n    def fetch_and_add(self, delta):\n        while True:\n            old = self.value\n            if self.compare_and_swap(old, old + delta):\n                return old\n\n\nclass LogBackedCounter:\n    \"\"\"Counter defined by committed add entries on a shared log.\"\"\"\n\n    def __init__(self):\n        self.log = []\n        self._lock = threading.Lock()\n\n    def append_add(self, delta):\n        with self._lock:\n            self.log.append({\"op\": \"add\", \"delta\": delta})\n            return len(self.log) - 1\n\n    def apply(self):\n        with self._lock:\n            return sum(e[\"delta\"] for e in self.log if e.get(\"op\") == \"add\")\n\n\ndef show_lost_updates():\n    \"\"\"Run the supplied racy program until its lost-update bug is visible.\"\"\"\n    while True:\n        counter = RacyCounter()\n        threads = [threading.Thread(target=counter.incr) for _ in range(50)]\n        for thread in threads:\n            thread.start()\n        for thread in threads:\n            thread.join()\n        print(\"racy final\", counter.value)\n        if counter.value < 50:\n            print(\"BUG: lost updates\", counter.value)\n            return counter.value\n\n\ndef show_fixed_counter():\n    counter = CASCounter()\n    threads = [\n        threading.Thread(target=counter.fetch_and_add, args=(1,))\n        for _ in range(50)\n    ]\n    for thread in threads:\n        thread.start()\n    for thread in threads:\n        thread.join()\n    assert counter.value == 50\n    print(\"CAS fetch-and-add final\", counter.value)\n\n\ndef show_log_backed_counter():\n    counter = LogBackedCounter()\n    threads = [\n        threading.Thread(target=counter.append_add, args=(1,))\n        for _ in range(50)\n    ]\n    for thread in threads:\n        thread.start()\n    for thread in threads:\n        thread.join()\n    assert counter.apply() == 50\n    print(\"log-backed counter final\", counter.apply())\n\n\nclass FetchAndAddOnly:\n    def __init__(self):\n        self._value = 0\n        self._lock = threading.Lock()\n\n    def fetch_and_add(self, delta):\n        with self._lock:\n            old = self._value\n            self._value += delta\n            return old\n\n\nclass Proposer:\n    def __init__(self, proposal, primitive):\n        self.proposal = proposal\n        self.primitive = primitive\n        self.ticket = None\n        self.published = False\n        self.crashed = False\n\n    def acquire_ticket(self):\n        self.ticket = self.primitive.fetch_and_add(1)\n\n    def publish(self):\n        self.published = True\n\n\ndef show_faa_consensus_limit():\n    primitive = FetchAndAddOnly()\n    p0 = Proposer(\"A\", primitive)\n    p1 = Proposer(\"B\", primitive)\n    p2 = Proposer(\"C\", primitive)\n\n    p0.acquire_ticket()\n    p1.acquire_ticket()\n    p2.acquire_ticket()\n    assert (p0.ticket, p1.ticket, p2.ticket) == (0, 1, 2)\n\n    # The ticket-0 winner crashes before its proposal is published.\n    p0.crashed = True\n    survivors = (p1, p2)\n    assert all(not proposer.published for proposer in survivors)\n    survivor_observations = tuple(\n        (proposer.ticket, proposer.proposal) for proposer in survivors\n    )\n\n    # The survivors have no operation that reveals p0's private value.\n    alternate_crashed_proposal = \"different-hidden-value\"\n    assert survivor_observations == ((1, \"B\"), (2, \"C\"))\n    assert p0.ticket == 0 and all(proposer.ticket != 0 for proposer in survivors)\n    print(\"FAA survivors observe\", survivor_observations)\n    print(\"alternate crashed proposal\", alternate_crashed_proposal)\n    print(\"fetch-and-add has consensus number 2\")\n\n\nclass ConsensusLog:\n    def __init__(self):\n        self.entries = []\n        self._lock = threading.Lock()\n\n    def append(self, proposal):\n        with self._lock:\n            self.entries.append(proposal)\n            return len(self.entries)  # committed log revision\n\n    def first_committed(self):\n        with self._lock:\n            return self.entries[0]\n\n\nclass ResourceServer:\n    def __init__(self):\n        self.max_seen = 0\n        self.value = None\n\n    def write(self, token, value):\n        if token <= self.max_seen:\n            return False\n        self.max_seen = token\n        self.value = value\n        return True\n\n\ndef show_consensus_log_and_fencing():\n    log = ConsensusLog()\n    p0_revision = log.append(\"A\")\n    p0_crashed_after_commit = True\n    assert p0_crashed_after_commit\n    log.append(\"B\")\n    log.append(\"C\")\n\n    decided = log.first_committed()\n    assert decided == \"A\"\n    print(\"consensus-log decision\", decided)\n    print(\"CAS and consensus log have consensus number infinity\")\n\n    resource = ResourceServer()\n    assert resource.write(p0_revision, \"write from A\")\n    newer_token = log.append(\"acquire by B\")\n    assert newer_token > p0_revision\n    assert resource.write(newer_token, \"write from B\")\n    assert not resource.write(p0_revision, \"delayed write from A\")\n    assert resource.value == \"write from B\"\n    print(\"fencing max_seen\", resource.max_seen, \"value\", resource.value)\n\n\nif __name__ == \"__main__\":\n    show_lost_updates()\n    show_fixed_counter()\n    show_log_backed_counter()\n    show_faa_consensus_limit()\n    show_consensus_log_and_fencing()\n```\n\nFetch-and-add has consensus number 2: after the ticket-0 winner crashes before publishing, survivors see only tickets 1 and 2 and cannot safely determine the crashed proposal or decide a valid winner.\n\nCAS and a consensus log have consensus number ∞: three proposers still agree on the first committed proposal even if that client crashes after commit. The committed log revision is used as a fencing token; the resource rejects delayed writes with a lower stale token.\n"
    },
    {
      "task": "[etcd] Use a linearizable CAS and a lease to elect a single leader, then show a stale leader is fenced",
      "question": "Run etcd in Docker (or an optional local single-node etcd) and practice compare-and-set leadership, lease failure detection, and revision-based fencing.\n\nPrerequisites: Docker Desktop (or Docker Engine) with `docker` on your PATH, or Homebrew `etcd`/`etcdctl` on macOS; Python 3.10+.\n\n```bash\n# Docker Desktop / Docker Engine (preferred)\n# Install from https://www.docker.com/products/docker-desktop/ (or your OS package manager), then start Docker Desktop / the daemon\ndocker info\n\n# Alternative on macOS without Docker\n# brew install etcd\n# Start a local single-node etcd in a separate terminal (foreground):\n# etcd\n# Confirm:\n# etcdctl version\n```\n\nSetup — start single-node etcd and alias `e` (Docker). If you used brew etcd, skip `docker run` and use the commented alias instead:\n\n```bash\ndocker run -d --name etcd-dev -p 2379:2379 \\\n  quay.io/coreos/etcd:v3.5.15 \\\n  /usr/local/bin/etcd \\\n  --advertise-client-urls http://0.0.0.0:2379 \\\n  --listen-client-urls http://0.0.0.0:2379\n\nexport ETCDCTL_API=3\nalias e='docker exec -i etcd-dev etcdctl'\n\n# brew / local etcd instead of Docker:\n# export ETCDCTL_API=3\n# alias e='etcdctl --endpoints=http://127.0.0.1:2379'\n```\n\nAlso save this incomplete fencing stub as `fence_resource.py` (you will finish the TODOs in task 3):\n\n```python\n#!/usr/bin/env python3\n\"\"\"Protected resource with a persisted monotonic fencing token. Complete the TODOs.\"\"\"\nimport json\nimport pathlib\nimport sys\n\nSTATE = pathlib.Path(\"resource-state.json\")\ntoken = int(sys.argv[1])\npayload = sys.argv[2]\n\nif STATE.exists():\n    max_seen = int(json.loads(STATE.read_text())[\"maxSeen\"])\nelse:\n    max_seen = 0\n\n# TODO: if token < max_seen, print REJECT line and exit 1\nraise NotImplementedError(\"compare token against maxSeen\")\n\n# TODO: if token > max_seen, set max_seen = token; then persist STATE and print ACCEPT\nraise NotImplementedError(\"update maxSeen and accept write\")\n```\n\nConcrete `e txn` CAS shape (blank line ends each section: compares, then success requests, then failure requests). After `LEASE=$(e lease grant 10 | awk '{print $2}')`:\n\n```bash\nprintf 'version(\"/app/leader\") = \"0\"\\n\\nput /app/leader node-a --lease=%s\\nget /app/leader\\n\\nget /app/leader\\n\\n' \"$LEASE\" | e txn\n```\n\nPlain `e txn` prints `SUCCESS` or `FAILURE` (not a `succeeded:` field). On `SUCCESS`, the success-path put and get run. On `FAILURE` (key already held), the put is skipped, the failure-path get shows the current holder, and the leader must not be overwritten. Equivalently you may compare `create_revision(\"/app/leader\") = \"0\"`.\n\nTasks:\n1. Grant `node-a` a 10s lease. Acquire `/app/leader` only through an explicit `e txn` using the compare/success/failure shape above (`version(\"/app/leader\") = \"0\"` then `put /app/leader node-a --lease=<lease-id>`). Keep the lease alive with `e lease keep-alive <lease-id>`.\n2. From a second client, run the same transaction for `node-b` with its own lease. Confirm the txn prints `FAILURE` while `node-a` owns the key. Watch with `e watch /app/leader` so contenders notice a `DELETE` and retry with backoff; a leader whose keepalive fails must immediately stop protected writes.\n3. Finish `fence_resource.py`: persist `maxSeen`, accept a write only when `token >= maxSeen`, update `maxSeen` when accepting a higher token, and reject a lower token. Use the successful leader key's `create_revision` from `e get /app/leader -w json` as the fencing token; do not rely on lease expiry alone to stop a paused process.\n4. Demonstrate stale-leader fencing end to end with `fence_resource.py`: (a) stop `node-a`'s keepalive without revoking the lease; (b) wait for TTL so the key expires and the watch shows `DELETE`; (c) let `node-b` acquire via the same txn (expect `SUCCESS`) and read its fencing token with `e get /app/leader -w json` (`create_revision`); (d) have `node-b` write first: `python3 fence_resource.py <TOKEN_B> \"node-b accepted write\"` expecting `ACCEPT`; (e) resume `node-a` with its old token: `python3 fence_resource.py <TOKEN_A> \"node-a stale write\"` expecting `REJECT` and a non-zero exit.\n5. Briefly explain why the txn is linearizable CAS, why watches/keepalive failure control leadership attempts and writes, and why fencing must be enforced by the protected resource. Optional: repeat with a 3-node etcd cluster and pause or kill an etcd member/leader.\n\nNote: etcd transactions (`txn`) provide the linearizable CAS primitive; leases provide failure detection, while the resource-side monotonic token check fences stale clients.",
      "answer": "Complete `fence_resource.py`:\n\n```python\n#!/usr/bin/env python3\n\"\"\"Tiny protected resource with a persisted monotonic fencing token.\"\"\"\n\nimport json\nimport pathlib\nimport sys\n\n\nSTATE = pathlib.Path(\"resource-state.json\")\ntoken = int(sys.argv[1])\npayload = sys.argv[2]\n\nif STATE.exists():\n    max_seen = int(json.loads(STATE.read_text())[\"maxSeen\"])\nelse:\n    max_seen = 0\n\nif token < max_seen:\n    print(f\"REJECT token={token} < maxSeen={max_seen}: {payload}\")\n    raise SystemExit(1)\n\nif token > max_seen:\n    max_seen = token\nSTATE.write_text(json.dumps({\"maxSeen\": max_seen, \"lastWrite\": payload}) + \"\\n\")\nprint(f\"ACCEPT token={token}, maxSeen={max_seen}: {payload}\")\n```\n\n`run_exercise.sh` (end-to-end demo; defaults to the Docker `e`/`etcdctl` path, with a commented local alternative):\n\n```bash\n#!/bin/sh\nset -eu\n\nexport ETCDCTL_API=3\nE=\"docker exec -i etcd-dev etcdctl\"\n# Local brew/etcd instead of Docker:\n# E=\"etcdctl --endpoints=http://127.0.0.1:2379\"\n\nrm -f resource-state.json\n$E del /app/leader >/dev/null\n\nLEASE_A=\"$($E lease grant 10 | awk '/lease/ {print $2}')\"\nLEASE_B=\"$($E lease grant 10 | awk '/lease/ {print $2}')\"\necho \"lease_a=$LEASE_A\"\necho \"lease_b=$LEASE_B\"\n\necho \"node-a transaction:\"\nprintf 'version(\"/app/leader\") = \"0\"\\n\\nput /app/leader node-a --lease=%s\\nget /app/leader\\n\\nget /app/leader\\n\\n' \"$LEASE_A\" |\n  $E txn\n\necho \"node-a key after acquisition:\"\nNODE_A_JSON=\"$($E get /app/leader -w json)\"\nprintf '%s\\n' \"$NODE_A_JSON\" > node-a-leader.json\nprintf '%s\\n' \"$NODE_A_JSON\"\nTOKEN_A=\"$(printf '%s\\n' \"$NODE_A_JSON\" | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"kvs\"][0][\"create_revision\"])')\"\n\necho \"node-b competing transaction (expected FAILURE):\"\nprintf 'version(\"/app/leader\") = \"0\"\\n\\nput /app/leader node-b --lease=%s\\nget /app/leader\\n\\nget /app/leader\\n\\n' \"$LEASE_B\" |\n  $E txn\n\necho \"watching leader deletion:\"\n$E watch /app/leader --rev \"$($E get /app/leader -w json | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"header\"][\"revision\"]+1)')\" > watch.log 2>&1 &\nWATCH_PID=$!\n\necho \"keeping node-b lease available for retry:\"\n$E lease keep-alive \"$LEASE_B\" > keepalive-b.log 2>&1 &\nKEEPALIVE_B_PID=$!\n\necho \"keeping node-a alive:\"\n$E lease keep-alive \"$LEASE_A\" > keepalive-a.log 2>&1 &\nKEEPALIVE_PID=$!\nsleep 2\nkill \"$KEEPALIVE_PID\"\necho \"node-a keepalive stopped; process did not revoke lease\"\n\nsleep 12\necho \"leader after node-a lease expiry:\"\n$E get /app/leader -w json || true\n\necho \"node-b retry transaction (expected SUCCESS):\"\nprintf 'version(\"/app/leader\") = \"0\"\\n\\nput /app/leader node-b --lease=%s\\nget /app/leader\\n\\nget /app/leader\\n\\n' \"$LEASE_B\" |\n  $E txn\n\nLEADER_JSON=\"$($E get /app/leader -w json)\"\nprintf '%s\\n' \"$LEADER_JSON\" > node-b-leader.json\necho \"node-b key and fencing revision:\"\nprintf '%s\\n' \"$LEADER_JSON\"\n\nkill \"$WATCH_PID\" 2>/dev/null || true\nkill \"$KEEPALIVE_B_PID\" 2>/dev/null || true\n\nTOKEN_B=\"$(printf '%s\\n' \"$LEADER_JSON\" | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"kvs\"][0][\"create_revision\"])')\"\necho \"node-a stale token=$TOKEN_A\"\necho \"node-b current token=$TOKEN_B\"\n\npython3 fence_resource.py \"$TOKEN_B\" \"node-b accepted write\"\nif python3 fence_resource.py \"$TOKEN_A\" \"node-a stale write\"; then\n  echo \"ERROR: stale write unexpectedly accepted\" >&2\n  exit 1\nelse\n  echo \"stale write rejected as expected\"\nfi\n```\n\nObserved outputs — node-a leader JSON:\n\n```json\n{\"header\":{\"cluster_id\":14841639068965178418,\"member_id\":10276657743932975437,\"revision\":14,\"raft_term\":2},\"kvs\":[{\"key\":\"L2FwcC9sZWFkZXI=\",\"create_revision\":14,\"mod_revision\":14,\"version\":1,\"value\":\"bm9kZS1h\",\"lease\":7587896270734334280}],\"count\":1}\n```\n\nObserved outputs — node-b leader JSON:\n\n```json\n{\"header\":{\"cluster_id\":14841639068965178418,\"member_id\":10276657743932975437,\"revision\":16,\"raft_term\":2},\"kvs\":[{\"key\":\"L2FwcC9sZWFkZXI=\",\"create_revision\":16,\"mod_revision\":16,\"version\":1,\"value\":\"bm9kZS1i\",\"lease\":7587896270734334282}],\"count\":1}\n```\n\nObserved outputs — watch.log (expect a DELETE when the lease expires, then PUT for node-b):\n\n```\nDELETE\n/app/leader\n\nPUT\n/app/leader\nnode-b\n```\n\nObserved outputs — resource-state.json after the new leader's write:\n\n```json\n{\"maxSeen\": 16, \"lastWrite\": \"node-b accepted write\"}\n```\n\nWrite-up:\n1. Acquire via explicit linearizable txn: `version(\"/app/leader\") = \"0\"` then success-path `put --lease=...` + `get`. Plain `txn` prints `SUCCESS` or `FAILURE`; on `SUCCESS`, read `create_revision` from a follow-up `get -w json` as the fencing token.\n2. Contenders watch `/app/leader` and retry the txn with backoff after deletion. A leader whose keepalive fails or is uncertain must immediately stop protected writes — the watch is only a retry signal, not proof of ownership.\n3. Stale-leader demo: stop node-a keepalive without revoking; after TTL the key expires; node-b acquires with higher `create_revision`; resumed node-a write with old token is rejected by the resource (`ACCEPT` for TOKEN_B, `REJECT` for TOKEN_A).\n4. Resource persists `maxSeen`: accept only `token >= maxSeen`, raise `maxSeen` on higher tokens. Lease expiry alone cannot fence a paused process; the protected resource must enforce the token check. On acquisition, the new leader may send an immediate heartbeat write carrying `r2` so `maxSeen` rises before useful work."
    }
  ]
}
