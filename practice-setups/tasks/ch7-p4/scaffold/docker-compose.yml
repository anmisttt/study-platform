x-citus: &citus
  image: citusdata/citus:13.2.0
  environment:
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: citus
    POSTGRES_HOST_AUTH_METHOD: trust

services:
  coordinator:
    <<: *citus
    container_name: ch7-schema-coordinator
    ports: ["5433:5432"]
  worker1:
    <<: *citus
    container_name: ch7-schema-worker1
  worker2:
    <<: *citus
    container_name: ch7-schema-worker2
